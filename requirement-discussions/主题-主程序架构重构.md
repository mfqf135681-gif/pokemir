# 主题 — 录制主程序架构重构(PipelineOrchestrator 上帝类 → 模块化)

> **2026-06-08 立**。基于全量深读 50 个方法 + 与用户的设计共识。**2026-06-12 深读复核**(见末章增量):六修+动作改革后 58 方法/3358 行,地图增量已补,方向结论不变。
> **作用**:把现有 orchestrator(Path A 活系统)从上帝类**渐进重构**成内聚模块(原地,不换持久化、不上 event-sourcing)。
> **状态:BLUEPRINT,不实施**。门槛核对(2026-06-23 刷新):耐久验收 ✅ / 识别层冻结 ✅(红线 R-5)/ **#227 接口契约 🟡 草案在案**(见末章 2026-06-23 §B,待求解器定型)/ Win ✅ → **3/4 推进中**(仅剩"求解器在场")。
> **📅 排程**:见末章《执行排程(2026-06-23 定稿,含真值地基)》——**取代旧序**(旧序 #226→#227→解冻 仍正确,但 2026-06-23 复盘揪出它"拿自己验自己"=圆形验证未闭,故前置【真值地基】层)。旧"求解器+审计=行为保持 oracle"成立但**只验"不回归"、不验"对"**;真值地基补上"对"那一维。
> **性能提升 ≈ 0**(已与用户确认,不以提速为由;目的=正确性/可维护/可测/4模式切换)。
> **关联**:`[[主题-DDD架构]]`(见 §0 关系澄清)· #238/#226/#227 · `contracts/recognition-freeze.md`(R-5)· `[[constraint-solver-paradigm]]` · `[[pillar-confidence-doctrine]]`

---

## §0 与「主题-DDD架构」的关系 + 「Path A 不重构」立场更新

- **两条不同路线,不矛盾**:`主题-DDD架构` = greenfield **Path B**(event-sourcing / aggregates,全栈重写,按 #LR15 触发);**本主题 = 渐进重构现有 Path A orchestrator**(同一份代码原地模块化,不换持久化、不上 event-sourcing)。
- **🔴 reconcile 一处冲突**:`主题-DDD架构` scope 写「❌ Path A 已完成代码不重构(沉没成本)」。**该立场据此更新**:立那条时 Path A 被设想为"将被 Path B 取代的临时实现";但实际 **Path A 是且持续是唯一在跑的活系统**(观战 live + MVP 逐手复盘都靠它)→ 它不是沉没成本,是主资产。
- **更新后的口径**:Path A 在【识别层冻结 + 契约 #227】之后**可做"行为保持的渐进模块化"(本蓝图)**;但**仍不上 Path B 的 event-sourcing 重写**(那条按 #LR15 触发,走 `主题-DDD架构`)。两文各管一条路线。

---

## §1 现状全貌(任务一:全量深读结论)

`pipeline/orchestrator.py` = **3143 行、50 方法的上帝类 PipelineOrchestrator**。

**生命周期(单线程同步):**
```
__init__(150-309:建识别器/载模板/载phash参考/读桌规/建~20状态字段)
  → start(): while running: _tick(); sleep(250ms)
  → _shutdown()
_tick = 总指挥,顺序调 12 步;_start_new_hand / _end_current_hand 各自是子编排
```

**状态散在两对象、无归属规律(耦合根源):**
- `self._xxx`(~20):_btn_confirmed/_btn_pending/_last_button_seat、_active_set/_hand_dealt_seats/_seat_gone_ticks、_blinds_pending/_blinds_attempts、_table_blinds、_hand_win_seats/_hand_start_tick、_empty_refs/_action_phash/_zone_readers/_digit_reader、_fold_read_profile、_avatar_swap_count…
- `tracker._xxx`(~15):current_hand、_prev_stack、_street_to_call/_street_has_bet、_folded_seats/_empty_seats/_seats_with_events_this_hand、_last_action_at、_pending_decision_time/_timer_state/_used_timebank、player_id_map/_avatar_fingerprints、latest_pot_bb/_pot_before_tick/_hand_pot_peak、_idle_avatar_hash/_showdown_captured_this_hand/_seat_pred_history/_showdown_last_cnn_at、_position_map、_hero_seat_idx_cache、_last_roi_img/_last_roi_text/_roi_force_refresh_at…

**🔴 跨切面共享状态(模块切不干净的真因,务必看):**
| 状态 | 写者 | 读者 |
|---|---|---|
| `is_skippable_seat`(folded∪empty) | — | ~6 处(seat循环/pre_batch/showdown/blinds/forced) |
| `_folded_seats` | ActionInterpreter、_rescue_silent_fold | ShowdownCapturer、is_skippable、blinds |
| `_prev_stack` | ActionInterpreter(+continue路径)、blinds | ActionInterpreter(stack_before)、blinds、forced |
| `_idle_avatar_hash` | seat循环读fold_area顺带写、_initialize_avatar_baselines | ShowdownCapturer |
| `_street_to_call/_has_bet` | ActionInterpreter(本tick前座) | ActionInterpreter(本tick后座)= **同tick跨座依赖** |
| `latest_pot_bb/_pot_before_tick` | PotTracker | ActionInterpreter(pot_delta) |
| `player_id_map` | PlayerID | 几乎所有 event 创建处 |
| `button_seat_index` | HandSegmenter | blinds、blind_levels |

→ **任意方法可摸任意状态**;"每模块独占状态"是目标,**现实里这 8 个状态天生横跨多模块**(见 §5 修正)。

---

## §2 每方法/模块的重构判断(任务二)

判定:🟢 原样迁移(逻辑不改,只挪进模块)/ 🟡 轻度(拆分/小重构)/ 🔴 需重构(真纠缠,需分解)。

| 目标模块 | 方法 | 判定 | 理由 |
|---|---|---|---|
| **CaptureService** | refresh_frame*/capture_roi*/_get_tick_frame_grey/_recognize_only_roi/_capture_with_diff_trigger | 🟢 | 内聚、纯基建 |
| **Recognizers(已是独立类)** | _ocr_stack_chips/_reader_for/_stack_chips_recipe/_stack_text_to_chips | 🟢 | 薄包装,内聚 |
| **util** | _avg_hash_int/_hamming64/_slot_has_card/_hero_cards_present | 🟢 | 纯静态 |
| **HandSegmenter** | _scan_button_white_frac/_predict_next_button/_detect_button_position(+button_move_online已纯) | 🟢 | 自包含;状态仅 _btn_*/_last_button_seat |
| **PotTracker** | _process_pot | 🟡 | 清,但**内嵌"总底池"换手触发**(混了切手)→ 抽出归 Segmenter |
| **CommunityTracker** | _process_community_cards | 🟢 | 内聚 |
| **WinDetector(+xx)** | _scan_win_amount | 🟢 | 内聚;状态 _hand_win_seats |
| **SeatLifecycle** | _detect_empty_seats/_classify_occupancy/_detect_active_set | 🟢 | 内聚 |
| 同上(fold 救援) | _rescue_silent_fold | 🟡 | 跨界:写 _folded_seats + 合成 FOLD event(碰持久化) |
| **PlayerID** | _capture_player_ids | 🔴 | 150行:hash+swap去抖+缓存锁+指纹+OCR共识+TempUser+模糊别名 一坨 → 拆子步 |
| 同上 | _upgrade_tempuser_to_real/_canonicalize_player_id_map | 🟢 | DB-sync 助手,内聚 |
| **BlindsModule** | _detect_blind_levels/_read_table_blinds/_inject_forced_from_blinds/_inject_post_events/_emit_forced_event | 🟡 | 内聚但双路径(桌规/OCR);状态 _table_blinds/_blinds_pending |
| **TimerModule** | _process_timer/_finalize_timer | 🟢 | 小、内聚(喂 decision_time) |
| **ShowdownCapturer** | _detect_hero_seat_index/_try_capture_showdown_live/_capture_showdown_cards/_dump_showdown_crop/_initialize_avatar_baselines | 🟡 | 簇内聚;唯一外依赖 `_idle_avatar_hash` 由 seat循环写 → 来源要显式交接 |
| **ActionInterpreter ★** | _process_seat_actions | 🔴🔴 | 575行 observe+interpret+persist+7处状态突变 = 主战场(#238) |
| 编排里的 3-way 包 | _tick_active_set_and_blinds | 🔴 | 一方法捆了 活跃集 + silent-fold救援 + 盲注注入 三责 → 必拆 |
| **HandReconstructor(手末)** | _infer_insurance(+_end 内联 reconstruct_hand_chips) | 🟢 | reconstruct.py 已半解耦;保险纯逻辑 |
| 诊断探针 | _emit_silent_fold_profiles | 🟢(或删) | VERBOSE_DIAG 探针,调查已结;可删 |
| **Orchestrator(瘦)** | __init__/start/stop/_tick/_start_new_hand/_end_current_hand/_shutdown/_seat_position/_build_facing_action | 🟡 | __init__ 按模块拆 setup;_tick/_start/_end 留作"接线+生命周期",变薄 |

**结论:50 个里真需【分解】的只有 4 个**(_process_seat_actions、_tick_active_set_and_blinds、_capture_player_ids、_process_pot 抽切手触发);其余多是**原样迁移(🟢)或轻度拆分(🟡)**。所以这次本质是**模块切分(迁移+定状态归属)**,不是大改逻辑。

---

## §3 需重构部分的具体计划(任务三,只列 🔴/🟡)

### 3.1 ★ ActionInterpreter:拆 _process_seat_actions(#238)
**现状 29 步**:每座 跳过守卫 → 读stack → 读fold_area(弃牌/allin/timer/基线/探针) → 读action(phash/OCR) → 读amount → check_change → parse → 算delta → infer_action_from_delta → P3覆盖 → reconcile金额 → 建raw_data → compute_confidence → item2 → 7处状态突变 → 5s去重 → low_tier门 → 落库 → 复采artifact。
**拆法(三层)**:
- **observe_seat(seat) → SeatReads**{stack_now, action_text, amount, fold_text}:纯读像素(复用 CaptureService + Recognizers + pre_batch)。
- **interpret_seat(reads, state) → (event|None, state')**:把 算delta/infer/override/reconcile/confidence/item2 + 7处状态突变 收进一个**有状态 reducer**。⚠️ 含 `time.time()` 去重 → **注入 clock 才可纯测**(见 §5 修正⑥)。
- **persist(event)**:落库 + 低置信 artifact(artifact 复采像素的脏行为改为传入已抓图)。
- 验:reducer 喂合成 reads+state 断言 event+state' → **Linux 单测**(Win 关也能验)。

### 3.2 拆 _tick_active_set_and_blinds(3-way 包)
拆成 3 个独立调用,由 _tick 顺序编排:`SeatLifecycle.update_active_set()` → `SeatLifecycle.rescue_silent_folds()` → `BlindsModule.inject_if_pending()`。各自独立可测。

### 3.3 拆 _capture_player_ids(150行)
拆子步(同一 PlayerID 模块内):`_avatar_hash_and_swap()`(swap去抖)→ `_cache_lock_or_fingerprint()`(缓存锁+指纹匹配)→ `_ocr_consensus()`(双读)→ `_fuzzy_alias()`(别名归并)→ `_fallback_tempuser()`。逻辑不改,可读性+可测性升。

### 3.4 PotTracker 抽出内嵌切手触发
`_process_pot` 里的"总底池 label → end+start hand"(L2851)**移到 HandSegmenter**(它是切手职责,不该埋在读底池里)。PotTracker 只剩纯读底池 + 单调性护栏。

### 3.5 ShowdownCapturer 基线交接显式化
`_idle_avatar_hash` 现由 seat 循环读 fold_area 顺带写。拆模块后,**基线写入改由 SeatLifecycle 显式产出 → 传给 ShowdownCapturer**(不再隐式跨模块写)。

---

## §4 主程序优化蓝图(任务四:结合共识)

### 4.1 目标分层(一条数据对象穿过)
```
CaptureService → 抓帧/缓存/裁ROI/diff缓存
Observation    → 各识别器吐【原始观测】TickObservation{button,community,pot,seats:{stack,marker,fold,allin,timer,action,amount}}
HandState      → 单一所有者(清理后的 tracker),模块经【定义接口】读写,杜绝"谁都摸"
Segmenter/Blinds/SeatLifecycle/ActionInterpreter/Showdown/Win/Reconstructor
               → 消费 Observation + 读/改 HandState(经接口)
Persist        → 落库
Orchestrator   → 瘦接线 + 生命周期(_tick/_start/_end)
```
**关键缝:** ① TickObservation(观测与解释解耦)② HandState 单一所有者(治"状态散两处、谁都摸")③ ActionInterpreter reducer(解释与落库解耦)。

### 4.2 配置/模式存储方案(已与用户定稿)
- **一个自包含大 JSON / 每个(客户端 × 座型 × 观战/上桌)**:几何 + `num_seats` + `observer` 字段 + **全部 phash 参考**(card_marker_ref 已在内;再纳入 action_refs / empty_refs / digit_templates,与 card_marker 同模式:工具外科手术式更新各自 section,不碰几何)。
- **observer 折进 JSON**(现为 `--observer` flag,仅 1 处功能分叉 L1480 → 搬入零风险),loader 从 JSON 读 → **加载一个 JSON = 原子无损切模式**。
- **4 模式切换 = 选 profile → 带新 profile 重启 pipeline**(干净状态);dashboard 一键 = 写 profile 名 + 触发重启。
- **接受同客户端 4 模式间的重复**(用户拍板:重复必要、占空间小);**重复去重(base+overlay merge)留作逃生口,YAGNI 不预先上**。
- **性能影响:无**——所有参考 `__init__` 一次性载入内存,热路径只用内存结构,不读盘;profile 运行时只读不重写。

### 4.3 迁移顺序(每刀行为保持 + 可验)
1. **HandSegmenter**(最自包含、button_move_online 已纯)— 安全第一刀;
2. **PotTracker / CommunityTracker / WinDetector**(高内聚易迁);
3. **CaptureService**(基建抽出);
4. **ActionInterpreter reducer**(最值、灭怪兽,纯逻辑 Linux 单测;但先钉死跨座/跨tick状态契约 + 注入 clock);
5. **SeatLifecycle / PlayerID / Blinds**(拆 3-way 包、拆 150 行);
6. **ShowdownCapturer**(基线交接显式化);
7. **HandState 单一所有者** + **Orchestrator 收薄**(最后,因为它依赖前面模块的接口定型);
8. **存储整合 + observer 折进 JSON + 4 模式 schema**(可与 1-7 并行,因只动加载层)。

---

## §5 全面复审与修正(任务五:拆自己的台)

**站得住的:** 现状地图(50方法/14簇/8个跨切面状态)准确;分层方向(观测↔解释↔落库 + HandState 单一所有者)与 #226/#227 一致;"真需分解的只有 4 个"这个判断降低了想象中的工作量。

**必须修正/警示的:**
1. **🔴 时机与序(最重):** 本重构动的是**未冻结的识别承载层**。识别层接下来必动(#235-B、#239…)→ 现在拆好会返工。**铁律:重构排在「识别层冻结 + 契约 #227」之后** —— #227 把"观测接口"定死,那时拆才一次到位。
2. **🔴 "每模块独占状态"是愿景,非现实:** §1 那 8 个跨切面状态(_folded_seats / _prev_stack / _idle_avatar_hash / _street_* …)天生横跨 ≥3 模块。**所以 §4.1 的 HandState 必须是「单一所有者 + 定义接口」,不是"切碎给各模块"** —— 否则切不干净。这是分解的真难点。
3. **🔴 行为保持风险高 + 当前不可验:** 代码满是 diag / `mirror_seat_state` 影子写 / 5s墙钟去重 / `_prev_stack` 双处更新 / artifact 复采像素 等跨切面副作用,搬动易引入**编译/单测看不出、仅 live 暴露**的漂移。**Win 关着是最不能验的时候** —— 之前"Win 关着是纯逻辑重构好时机"是自我合理化,撤回。
4. **"自包含安全第一刀"部分修正:** ShowdownCapturer 的基线由 seat 循环写,非真自包含 → 拆它必须先做 §3.5 的交接;故 HandSegmenter 才是真第一刀。
5. **PotTracker 内嵌切手触发(§3.4)** 是个隐藏耦合,复审才显式拎出 —— 切手逻辑散在两处(button + 总底池),迁移时要合并到 Segmenter。
6. **"reducer 可 Linux 单测"打折:** 含 `time.time()` 墙钟去重 → 非纯函数,**须注入 clock** 才可测。
7. **存储整合的代价复核:** 大 JSON 的几何被机器 blob 埋深、git diff 变吵 —— 用户已接受(占空间小、重复必要);**性能影响经代码核实为 0**(载入期事件,非热路径)。

**复审结论:方向对、地图对、存储方案对;唯一被否的是"现在就重构"** —— 它早了、序错了、≈0 收益、且此刻不可验。**本文档定格为蓝图,实施门槛 = 识别层冻结 + #227。**

---

## §6 实施前必满足(检查清单)
- [ ] 识别层耐久验收通过(读那 1 小时长局:垃圾累积/守恒稳/分辨率不漂)；
- [ ] 识别层冻结(#235-B / #239 等识别改动 要么做完要么明确放弃)；
- [ ] 契约 #227 落地(定义 TickObservation / HandState 接口的不变量)；
- [ ] Win 可用(每刀行为保持需 live 回归)；
- [ ] 然后按 §4.3 顺序逐刀,每刀:Linux 编译 + 纯逻辑单测 → Win live 回归。

---

## §7 减法清单(2026-06-08 起;做减法【优先于】搬运)

**原则**:死/孤立代码**先删再重构**(把死代码搬进新模块是白干)。识别无关的减法**不受冻结门槛约束、随时可安全删**;碰识别 fallback 的才排冻结后。**延迟探针(phase_ms/sub_ms/tick_stats/_phase_durations)= 后期性能优化的钥匙,任何减法绝不碰。**

**✅ 已删(行为保持:dormant/无消费方;Linux 验 compile+AST+零引用,运行时待 Win):**
- **fold-miss 探针**(T117:`_fold_read_profile` + `_emit_silent_fold_profiles` + 累积块)— VERBOSE_DIAG dormant、调查已结束、唯一消费方=自身。
- **seat_lifecycle 影子子系统**(T91:`mirror_seat_state` ×5 调用 + 方法 + 实例 + `reset_for_new_hand` + 过时测试)— **只写无人读**(消费方 ATTENTION_MODE is_skippable 已 #236 删)。保留 hand_phase / is_skippable;`pipeline/state/seat_lifecycle.py` 类留作孤立模块(日后随手删)。

**⏸ 审慎 HOLD(判断后【不删】):**
- **OCR_RECOGNIZE_ONLY**(#237 parked)— 是 parked 的【性能机制】(跳 CRAFT 检测),属"后期提速的钥匙"(用户明示留性能钥匙)→ **保留**。
- **非-BUTTON_CUT 切手回退**(hero换牌 / 公共牌reset,`not BUTTON_CUT` 门控)— BUTTON_CUT=0 的安全网,且与"首手启动"(L417-422,两模式都用)缠绕;删=去安全网 + 需 Win 验切手没坏 → **留到能 live 验时**,或与"BUTTON_CUT 永远开"决策一起定。

**🗑️ 死/半死 ROI + timer 弃用(2026-06-12 信源调查;冻结范围 → 解冻窗口一次清):**
> profile 每座定义 13 区 + 6 全局区,live 实测消费核对后,以下不在主线 —— 删它们【碰冻结文件】
> (orchestrator/roi.py),不单独开解冻窗,等架构重构解冻窗口连同 §3 拆分一起清。
- **hand_type**(牌型文字)— **纯死**:仅 `capture/roi.py` 数据类定义,orchestrator/detector 零消费。
- **fold_text**(专读弃牌区)— **已退役**:#235 删其 OCR,代码只剩注释墓碑;框留 profile。
- **name**(桌名全局区)— **死**:定义了,live 零消费。
- **pot_size_previous / 上街池**— **半死**:production 不读,仅 `LABEL_SIGNAL=potprev` 验证态 tap;
  早已判定砍为派生量(从总底池轨迹推),框留但不独立读。
- **timer OCR 弃用**(2026-06-12 用户提,数据坐实)— decision_time **零消费方 + 仅 21% 动作覆盖**
  (508 真动作 109 带 dt,圈梁 D-Time1 soft-signal 暂不接);将来若需决策时长 → **改用 action_events
  相邻 timestamp 差派生**(免费、已有,精度对 soft-signal 足够;用户指路)。
  ⚠️ **弃用前必验 timer 兼的两个隐藏职责**:① `timer_handled` 闸(思考态→跳过该座,#235 删
  fold_ocr 后大概率冗余但要验不误记)② 吞街修补漏(`过牌→timer→过牌` 清 prev,空白游程≥2 已
  覆盖大部,timer 清是"无空白直接切timer"盲区的补)→ 删 timer 须补回这点保险或验证盲区可接受。
  ⚠️ **非提速项**:timer OCR 性能探针≈0ms(需 benchmark 确认),收益=减死代码/简化,不是提速。

**待评估(产品/策略决策,非纯清理,需你拍板):**
- `_infer_insurance`(投机启发式,rebuy 已搁置)· 摊牌训练采集 `_dump_showdown_crop`(CNN 已 production-ready,采集或已完成)· 重叠 OCR 路径收敛 · 冗余 seat 状态集合(`_folded_seats`/`_empty_seats`/`_seats_with_events_this_hand` 三套并存)。

---

## 章节 2026-06-12: 深读增量更新(六修+动作改革后的地图修订)+ 排程定稿

> 蓝图立后 4 天,识别层经历六修(吞街/ID冻结/闸序A″/all-in分层/result双信源/退还)+ 动作首字框改革
> + 冻结落成(R-5)。深读复核:**§4 方向、§5 复审结论、§2"真需分解只有 4 个"全部仍成立**;以下为地图增量。

### Δ1 规模与新方法(50→58 方法,3143→3358 行,巨兽 575→630 行)

| 新方法 | 归属模块(补进 §2 表) | 判定 |
|---|---|---|
| `_hand_player_name`(ID 手内冻结) | PlayerID(或 HandState 接口方法) | 🟢 内聚小件 |
| `allin_stackzero_step`(模块级纯函数)+ `_detect_allin_stackzero` + broken 看护 | ActionInterpreter 内 AllinDetector 子件 | 🟢 纯逻辑已拆 |
| `_flush_pending_allins`(手末过闸写库) | ActionInterpreter persist 层的**手末钩子** | 🟡 跨 tick 生命周期 |
| `_pot_label_color`(总底池颜色判定) | PotTracker;**§3.4 修订:切手触发已实现为颜色 latch**(原 L2851 文字版行号过期) | 🟢 |
| `LabelCapturer`(pipeline/label_capture.py) | 已是独立旁路模块(蓝图未提,无需迁移) | 🟢 |

### Δ2 新增跨切面状态(补进 §1 表;模块切分时的新缝)

| 状态 | 写者 | 读者 | 备注 |
|---|---|---|---|
| `_pot_label_latched` | PotTracker | ActionInterpreter(fold 压制)+ SeatLifecycle(救援闸)+ AllinDetector(broken 看护) | **新重耦合点:实质是"结算开始"信号,重构时应升格为 HandState 的显式 phase 字段** |
| `_allin_pending` + `_az_state` | ActionInterpreter | `_end_current_hand`(flush) | 新增的"tick→手末"缝 |
| `_street_amt_max` / `_seat_street_amt`(detector) | 盲注注入 + ActionInterpreter 过门后 + all-in stash | ActionInterpreter 三重闸(A/A′/A″) | 同 tick 跨座依赖加深(同 `_street_to_call` 一族) |
| `_hand_id_lock` | `_hand_player_name` 首用 | 动作/盲注/救援三处归属 | 内聚于 PlayerID,缝浅 |
| `_hand_win_amounts` | WinDetector | `_end_current_hand`(result 双信源) | 手末缝 +1 |

**推论**:`_end_current_hand` 因 flush + result 双信源进一步加厚 → §4.3 第 7 刀(Orchestrator 收薄)的工作量比原估大;
"结算 phase"(`_pot_label_latched`/`_showdown_latched` 双 latch)值得在 HandState 里立为一等状态,三个消费方改读它。

### Δ3 §7 减法清单状态更新

- fold_ocr 已剥(#235 part2 落地):fold_area 仅留 capture 喂头像基线,`read_text` 已删——§2 表该行描述过期。
- first_char 切字已删(动作首字框改革);旧 HOLD 项不变。
- **新规:冻结生效后,任何对冻结文件的减法(含 §7 待评估项)一并挪入"解冻窗口",不再单独动**。

### Δ4 §5 修正清单追加

8. **识别层已冻结(R-5)**:重构=批量修改冻结文件 → 必须走 `contracts/recognition-freeze.md` §4 解冻流程,
   开窗口一次做完回冻;冻结一周即开闸会拆冻结的台,故排程压在求解器之后。
9. **求解器+审计 = 行为保持 oracle**(§5.3 的解药):每刀后跑 30-90 分钟,closure 分层 + 六仪表盘
   (洞数/GAP_NEG/ID漂移/幽灵/latch/all-in)即回归判据——"当前不可验"已不成立,但要等 #226 在场。

---

## 章节 2026-06-23:delta 刷新(A)+ #227 接口契约草案(B)

> 用户重提"屎山"焦虑 + 要"根据现有代码梳理逻辑图纸+设计清单"。复核发现本蓝图已是该图纸 → 不另起,
> 本章 = ①把 06-12 之后的代码漂移并入地图(A);②补齐 2/4 门槛里缺的 **#227 接口契约**(B,缺它无法切净)。
> **离线网认定更新**:`tools/replay_reconstruct.py` 实测**只覆盖 reconstruct/solver,不跑 orchestrator** →
> §Δ4-9 的回归 oracle 实为"每刀 Win live soak",**对增量成立、对大爆炸致命** → 再次坐实"走增量、否大爆炸"。

### A. 地图 delta(2026-06-12 → 2026-06-23)

**规模**:3358 → **3496 行**(巨兽 `_process_seat_actions` 630 → ~664 行)。新增 5 项变更,归属如下:

| 变更 | 归属模块(补 §2 表) | 判定 | 架构含义 |
|---|---|---|---|
| **TableFeed 人话流**(`_feed_raw`/`_feed_line`/`_feed_result`,独立 logger `pokemir.table_feed` + `table_feed.log`) | **新模块 OutputSink/TableFeed**(冻结豁免:纯日志) | 🟢 内聚 | **§4.1 分层图要加第二个输出 sink**(与 Persist 并列)。被 ActionInterpreter(实时)+ HandReconstructor(手末赢家)+ all-in flush **三处调用** → 应做成 **Observer 订阅者**,解释模块不该自持 logger |
| **rebuy 赢家否决**(`_is_round_rebuy` util 谓词 + `_end_current_hand` 内交叉 `_hand_win_seats`×reconstruct flags) | HandReconstructor / result 归属 | 🟡 | 延续 Δ2 的"`_end_current_hand` 持续加厚"趋势(见下) |
| **dedup 统一**(60s 窗 + 栈门 `stack_frozen`,读 raw_data stack_before/after) | ActionInterpreter reducer 内 | 🟡 | 强化 §5.6:reducer **须注入 clock**;新依赖=dedup 现读栈态 → reducer 的 dedup 子状态须干净自持 |
| **all-in 实时化**(检测瞬间 `_feed_line(...⏳待核)` 于 seat 循环内;`_flush_pending_allins` 补 ✓确认/⚠撤销) | ActionInterpreter + 手末钩子 | 🟢 | 证实 TableFeed **跨 tick + 手末双入口**(Observer 模型的依据) |
| **8 座 ROI 重框**(`rois/*.json` id 122×27 / fold 76×77 + `anchor_rel`) | 数据运维(冻结豁免) | — | 不动代码地图;属抗漂自动配准主题 |

**"4 个真需分解方法"复核 → 仍成立**,但两处微调:
- `_end_current_hand`:flush + result 双信源(Δ2)+ 本轮 rebuy 否决 → **从 🟡 逼近 🔴 边缘**;§4.3 **第 7 刀(Orchestrator 收薄)工作量再上调一档**。手末已成第二战场(仅次于 `_process_seat_actions`)。
- **新模块 TableFeed** 进 §2 表(🟢);它是 06-08 蓝图未预见的**新关注点(可观测/展示层)**,目标架构里应与 Persist 同层、走订阅而非内嵌。

**门槛核对更新(§头)**:识别冻结 ✅ / Win ✅ / **#227 → 本章 B 起草中(draft)** / 求解器在场 ❌ → **2/4 → 推进中,B 落地后达 3/4**。

### B. #227 接口契约草案

> **职责边界**:本契约管【**模块间数据流 + 行为保持**】。**扑克域不变量(P)/ UI 域(U)仍归 `contracts/invariants.md`,不重复**。
> 本章新立 **B 域 = 行为保持不变量**——把散在墓碑注释里的"承重防御"契约化,使重构**不会把它们当累赘简化掉**(直接回应"屎山"焦虑:债不是删干净,是把隐性约束显性化)。
>
> **覆盖率(2026-06-23 二次:全量测绘已补)**:首版仅深读 4 单元 → 自复盘揪出"覆盖幻觉" → 派 **4 个并行只读探子**全量测绘:①解释层 `reconstruct.py`/`solver.py`/`conservation.py` ②PlayerID 簇 ③pot/community/blinds/活跃集 簇 ④手末/摊牌/赢家/all-in 簇。B.2 reset 表已**逐字核对完整**;B.3 已从 12 条扩到 **48 条、分 5 层**(B.3.1-3.5)+ 容差常量登记表(B.3.6)+ 测绘新发现(B.3.7)。
> ⚠️ **残余限制(诚实)**:B.3.2-3.5 的**出处行号来自测绘探子、未由我逐条亲验**(探子自报有过期行号案例)→ **实施每刀前必 `grep -n` 复核对应锚点**。本章仍是 BLUEPRINT,定型仍需"求解器当真实消费者"收口接口。

#### B.1 `TickObservation`(观测层产物 —— 纯原始、可空、带时戳;无扑克语义)

```
TickObservation{
  tick_id: int, wall_clock: float,           # 注入 clock 的唯一时间来源
  button:    {white_frac_argmax_seat: int|None}              ← HandSegmenter 读
  community: {count: int, cards: list[str]}                  ← CommunityTracker 读
  pot:       {value: float|None, label_color: 'white'|'teal'|None}  ← PotTracker 读
  showdown_corner: bool
  seats: { sidx: SeatObservation }
}
SeatObservation{
  stack: float|None,            # 数字配方主读;all-in %→None
  action_word: str|None,        # phash 二元桩(无→"" 即 idle,非回退 OCR)
  amount: float|None,           # 各区 amount 模板
  fold_text: None,              # #235 已退役,恒 None(占位待删)
  occupancy: bool, win_xx_count: int,
  avatar_hash: str, card_marker_present: bool,   # card_marker=活跃集金丝雀
}
```
**不变量(观测层)**:① 全字段**可空**(读空≠错,是一等状态);② **无扑克语义**(不判 fold/call,只给像素读数);③ 每字段**单一 reader**(上表右列),杜绝多模块各读各的;④ 一个 TickObservation **不可变**,解释层只读不改。

#### B.2 `HandState`(单一所有者接口 —— 治"状态散两处、谁都摸")

**字段按生命周期分三组,reset 策略作为数据声明**(治 §5.2 那张"手工 reset 清单漏一个"的雷,L307 就是漏清补丁的活案):

> ⚠️ **现状(已逐字核对 2026-06-23)**:reset **散在两个方法**——`detector.start_new_hand`(tracker 侧)+ `orchestrator._start_new_hand`(self 侧),**手工逐字段清、非自动**,极易两处 desync(这正是重构要消灭的)。下表为**核对后的完整手内集**;目标态=单一 `reset_for_hand()` 按组遍历。
```
# HAND_SCOPED(每手清;★=tracker侧 / ◆=orchestrator侧)
★ prev_stack, prev_action_texts, last_action_at, prev_community_count,
★ latest_pot_bb, pot_before_tick, hand_pot_peak,
★ street_to_call, street_has_bet, street_amt_max, seat_street_amt,
★ went_all_in, folded, empty, seats_with_events,
★ showdown_captured, showdown_last_cnn_at
◆ az_state, showdown_latched, pot_label_latched, pot_debounce,
◆ action_amt_wait, action_blank_run, hand_id_lock, allin_pending,
◆ hand_win_seats, hand_win_amounts, hand_start_tick,
◆ blinds_pending, blinds_attempts, blinds_prev_n, blinds_stable_since,
◆ blinds_pending_since, active_set, hand_dealt_seats, seat_gone_since
  + settlement_phase(=合并 showdown_latched+pot_label_latched 的目标字段)
PERSISTENT = {player_id_map, avatar_fingerprints, idle_avatar_hash, seat_pred_history,
             last_roi_*, btn_confirmed/btn_pending/btn_pending_count, last_button_seat,
             canon_seen, capture_ids_retry_counter,
             avatar_swap_count}   # ⚠️ avatar_swap_count 跨手持久但【无过期】:座位消失(table shrink)后陈旧计数不清→潜在泄漏(测绘探子②发现,B.3.7)
DEAD_PENDING_DELETE = {timer_state, pending_decision_time, used_timebank,
                       pointer_state, hand_phase}  # §7 解冻窗清
```
**暴露方法、不暴露裸字段**(示例):`is_skippable(sidx)` · `stack_before(sidx)` · `record_action(event)` · `advance_street(count)` · `latch_settlement(source)` · `mark_winner(sidx, amount)` · `reset_for_hand()`。
**🔑 settlement_phase 升格**(Δ2 建议落地):合并 `_pot_label_latched` + `_showdown_latched` 为**一等枚举** `{betting, settling, showdown}`,三个消费方(fold 压制 / 救援闸 / all-in broken 看护)改读它,不再各摸两个 bool。
**不变量(状态层)**:① **单一所有者**,外部模块只经接口读写;② reset 策略=数据(上表),`reset_for_hand()` 遍历 HAND_SCOPED 自动清,新增字段必登记其组 → 编译期/单测可断言"该清的清、该留的留"。

#### B.3 B 域 —— 行为保持不变量(重构必须保住;= 墓碑防御契约化)

> 每条都有墓碑出处 + 验证手段。**重构后逐条过 = 这些历史 bug 不复发的保证**。验证分两类:🧪 Linux 纯逻辑单测(注入 clock/state)/ 🖥️ Win live soak(仪表盘判据)。**48 条按 5 层组织(B.3.1-3.5)**;⚠️ B.3.2-3.5 行号来自测绘探子,实施前 `grep -n` 复核。

##### B.3.1 座动作层(`_process_seat_actions`)— 本会话亲读

| ID | 命题 | 墓碑出处 | 验证 |
|---|---|---|---|
| **B-1** | `prev_stack` 更新**不对称**(行为事实):7 个 continue 中仅"无动作文本"(2694)与"循环到底"(3097)更新;判重/等帧/低桩/parse失败/摊牌压制路径**不更新**。⚠️ 仅"等帧"(2843)有注释证明是【刻意冻结基准】;其余各路是否刻意**未逐条证实**——重构须**保持现状行为**,不得想当然统一 | 本会话 B 风险分析 | 🧪 reducer 各路径断言 |
| **B-2** | tick 阶段序:`pot → active_set/blinds → win_scan → seat_actions → showdown → ids` | L493/L501 注释 | 🧪 接线测 + 🖥️ seq |
| **B-3** | stack 突跳 ≥9× 拒收,**例外 stack→0**(all-in 合法) | 2026-05-27 根因D(L2515) | 🧪 |
| **B-4** | text 优先于 stack-derived,仅两类物理矛盾覆盖(text=fold/check 但投钱 / text≠allin 但栈≈0) | T16 | 🧪 |
| **B-5** | 金额三闸 A/A′/A″ 在 commit 前;stack 兜底**仅封顶后**(闸序倒置) | 2026-06-11 毒地板230案 | 🧪 |
| **B-6** | dedup 键 `(player,street,action)`+amount(±2)+stack_frozen,窗 60s,每手清 | bafca031 + 06-23 统一 | 🧪(注入clock)+🖥️幽灵率 |
| **B-7** | fold 在 settlement latch 后压制(settling∨showdown) | #235 + 幽灵fold修 | 🖥️ |
| **B-8** | all-in 延迟写 + broken veto(结算前栈读回>5→broken→手末拒写) | #243 + 罗湖1401/东胜57 | 🧪+🖥️ |
| **B-9** | 街转移清 street_to_call/has_bet/amt_max/seat_street_amt(community count 驱动) | check_community_change | 🧪 |
| **B-10** | ID 手内冻结(首用锁);player_id_map 跨手不清 | #2 cache lock | 🖥️ |
| **B-11** | pot_size_final 用 hand_pot_peak 非 latest(避手末瞬态) | finalize_hand | 🧪 |
| **B-12** | 低桩(conf<0.7)不入主表走 diag | T47-V(7手非法dup) | 🧪 |

##### B.3.2 解释层(`reconstruct.py` / `solver.py` / `conservation.py`)— 测绘探子①

> 🟢 **结构性好消息**:解释层三件**已是无状态纯函数**(每手一调、手末无残留)→ **Linux 全可单测、refactor 近零风险**。屎山集中在 orchestrator,**不在解释层**。这把 §4.3 第 4 刀(ActionInterpreter)的难度下调:reducer 要抄的纯逻辑这边已有范本。

| ID | 命题 | 出处(待grep复核) | 防的bug/铁律 | 验证 |
|---|---|---|---|---|
| **B-13** | 解释层=无状态纯函数(reconstruct/solver/conservation 不记忆前手) | 三文件全局 | 重构可放心抽,不会丢隐藏状态 | 🧪 |
| **B-14** | 强制注排除:ante簇(≥`min_ante_seats`同窗同跌~ante)+ BB合并(ante+bb 每座只剔【最早】一笔,`sim_win`窗) | reconstruct 148-176 BUG1/§19.12① | limp 被误剔 / "bet 8"假阳 | 🧪 |
| **B-15** | all-in 反解去重:同座同手只一笔(多帧%标记是噪声) | reconstruct 217-238 BUG2 | 524×3 假阳 | 🧪 |
| **B-16** | 全下封盘线:全下后 `lock_margin`(0.5s)内晚跌=回声→删 | reconstruct 203-215 T140 | 晚回声当新下注 | 🧪+🖥️ |
| **B-17** | 结算锚=首派彩 rise(stack涨);但 **0→X(补码/重买)不锚**(只正持有上升才算派彩) | reconstruct 137-146/240-250 §19.12/+ | 手中段真下注被误删(hand5 turn74被吞) | 🧪 |
| **B-18** | 街边界:先跳到首次清板(n≤2)再起算街 | reconstruct 81-95 | 残留板压没 preflop、limp全判flop | 🧪 |
| **B-19** | 动作分类:`mx==0`→bet / `C≤mx+tol`→call / `C>mx+tol`→raise | reconstruct 183-191 | 注级语义 | 🧪 |
| **B-20** | `to_amount`=本街累计(非增量)= call-to 口径,solver R2/R3 基础 | reconstruct 178-194 | 用户口径 + 合法性裁决依赖 | 🧪 |
| **B-21** | 端点重建**只可靠定 净额/赢家/rake**,不定赢家投入/绝对底池;`tol=max(2,0.5bb)`、`rake_ceiling=max(3bb,8%pot,30)` | reconstruct 485-533 #226 | 端点是唯一承重锚,但语义有界 | 🧪+🖥️ |
| **B-22** | solver:R1全下终局 / R2无新增资金 / R3不能自加 —— **删幻影不补漏** | solver 55-68 | 结算/街错位残影(170343#1 自加到200) | 🧪 |
| **B-23** | pot守恒:`gap≤max(tol,5%pot+5)` | reconstruct 251-255 | 漏抓/all-in反解错 | 🖥️ |
| **B-24** | amount漏读兜底 `reconcile_underread`:`stack_delta-amount≥8 且 ≥2×`→用 stack_delta | reconstruct 449-468 | 末位跟注瞬态(可乐25读2/神啊30读4) | 🧪+🖥️ |
| **B-25** | 切手:按钮顺时针单调+去抖(min_hold 3s / online debounce 2帧)+ 多信号交叉印证(含anchor 或 ≥2信号) | reconstruct 369-426/549-567 T139 | 误切/漏手 | 🧪 |
| ⚠️**冲突** | **solver `infer_p6_calls` 是加法补跟,但 `contracts/invariants.md` P-6 已降级"仅守门不自动补"** | solver 81-132 ↔ invariants.md P-6 | **代码与契约冲突,定型时须 reconcile**(留是删?) | 决策 |

##### B.3.3 PlayerID 簇 — 测绘探子②

| ID | 命题 | 出处(待grep复核) | 防的bug | 验证 |
|---|---|---|---|---|
| **B-26** | 缓存锁:`player_id_map` 跨手不清,非TempUser座 continue 不重读 | #2 / orch 1253-1259, detector 348 | OCR漂移分裂同一玩家画像 | 🧪 |
| **B-27** | 头像 swap 去抖:hamming>12 **且连续≥`SWAP_MIN`(2)** 才 evict | #7 / orch 1234,1241-1251 | overlay瞬态抖动→误判换人(16TempUser+36重复名) | 🖥️ |
| **B-28** | 多帧OCR共识选**最长**文本 | #5 / orch 1282-1292 | 单帧截断/残缺 | 🧪 |
| **B-29** | 纯数字/`len<2` 名护栏→TempUser、**不进模糊合并** | #239 / orch 1330-1340 | 筹码数字当名抢真名 | 🧪 |
| **B-30** | 模糊别名合并 `cutoff=0.75`(case-insensitive) | #3 / orch 1343-1356 | OCR变体当两玩家 | 🧪 |
| **B-31** | TempUser 可重读升级 + 升级触发**DB sync** `action_events` | T41 / orch 1254-1259,1357-1361,1520-1545 | 永久TempUser + 历史画像分裂 | 🖥️ |
| **B-32** | canonicalize 增量 `O(新×N)` 非 `O(N²)`(播种 `_canon_seen`) | 2026-06-15性能 / orch 1558-1576 | 2654名→64s→帧率崩 | 🧪 |
| **B-33** | avatar匹配 hamming≤6 短路,**但拒匹 TempUser**(防自循环) | T43 / orch 1263-1278 | TempUser匹到自己→永不升级 | 🧪 |

##### B.3.4 底池/公共牌/盲注/活跃集 簇 — 测绘探子③

| ID | 命题 | 出处(待grep复核) | 防的bug | 验证 |
|---|---|---|---|---|
| **B-34** | 底池只增不减,drop>10% 拒(+防抖≥2帧) | T2/#243 / orch 3178-3186 | OCR漏位(1234→234)尖峰锁死 | 🧪 |
| **B-35** | 公共牌只认 `{0,3,4,5}`,中间态(1/2张)不 emit 街 + 新街清 street_* 四件 | detector 224-248 | 翻牌动画垃圾街 + 跨街金额继承 | 🧪 |
| **B-36** | 盲注**等活跃集稳定≥0.4s 才派**,座数变则重置,5s 兜底超时 | orch 1005-1020 | 高帧抢发牌前→漏派/错SB/BB | 🧪 |
| **B-37** | 桌规确定性派**优先** OCR(`_table_blinds` 非空→inject_forced) | #230 / orch 1104-1105,3271-3296 | OCR盲注抖(SB 5/10混读) | 🧪 |
| **B-38** | 活跃集=card_marker hamming≤8;空座**双确认**(phash全0 + stack无数字)+ dup hash≥2 全标空 | #228/T46-A/T63 / orch 3317-3332,2222-2236 | 占座≠发牌 / 背景hash假占用 | 🧪 |
| **B-39** | silent-fold 救援:card_marker消失≥0.4s **且非river且未latch**(摊牌/总底池/runout) | #235 P2a / orch 996-1004 | 摊牌期掉牌背→假弃牌 | 🖥️ |
| **B-40** | 盲注/救援/POST 取名走 `_hand_player_name`(手内冻结);名缺→skip 不伪造 | orch 909,1035,1040 | 手内名漂分裂 / null名 | 🧪 |

##### B.3.5 手末/摊牌/赢家/all-in 簇 — 测绘探子④

| ID | 命题 | 出处(待grep复核) | 防的bug | 验证 |
|---|---|---|---|---|
| **B-41** | `_end_current_hand` **第一步必 `_flush_pending_allins`**(金额已settle、broken已定稿) | orch 1372 | 顺序错→写未定稿金额 | 🧪 |
| **B-42** | all-in 三闸(活跃集 + `was_active` latch + `run_th`首零fire)+ broken veto(结算前栈回>5→拒写) | #243 / orch 31-49,2012-2045,2531-2537 | 座位易主瞬态0假阳(罗湖1401/东胜57) | 🧪+🖥️ |
| **B-43** | 摊牌**必 river 期实时抓**(非手末;手末UI已消失→CNN域外 mean_R 40 vs 205) | #235 / orch 1679-1707 | 全盘域外幻觉 | 🖥️ |
| **B-44** | 摊牌多闸:亮度≥160 + 纹理≥50 + CNN conf≥0.9 + 物理约束(无重牌) + 幻觉抑制(5窗≥3压) | #235 / orch 1758-1851 | CNN致幻/跨座重复 | 🧪+🖥️ |
| **B-45** | +xx 黄像素**数量**>35(非占比)+ 跳发牌窗 `_xx_deal_skip` + settle 才锁金额 | #241 / orch 2079-2118 | avg_hash过火/s0误报75% | 🧪 |
| **B-46** | 赢家**双信源**:+xx ∩ 端点;rebuy(`_is_round_rebuy`)则只留+xx确认,否则标 ⚠待核 | #241part2 / orch 1394-1432 | rebuy筹码注入→假赢家 | 🖥️ |
| **B-47** | `pot_size_final` 用 `_hand_pot_peak` 非 latest | detector finalize_hand | 手末瞬态污染 | 🧪 |
| **B-48** | `_showdown_latched` 压假弃牌(摊牌期 card_marker 消失=亮牌非弃) | #235part3 / orch 952-955 | 摊牌假弃(假弃大头) | 🖥️ |

##### B.3.6 容差/魔法常量登记表(重构必须原样搬,改一个数=改行为)

| 常量 | 默认 | 出处 | 敏感度 | 用途 |
|---|---|---|---|---|
| `tol` | 2.0 | reconstruct 125 | 🔴 | stack跌幅/注级/to_amount 通用容差 |
| `ante_sim_win` | 1.0 | reconstruct 161 | 🔴 | ante排除窗(每座只剔最早,区分limp) |
| `pot守恒tol` | max(tol,5%pot+5) | reconstruct 253 | 🔴 | 底池守恒判级 |
| `settle_guard` | 2.0 | reconstruct 243 | 🟡 | 派彩前判噪声窗 |
| `lock_margin` | 0.5 | reconstruct 210 | 🟡 | 全下回声窗 |
| `min_hold`/`debounce` | 3.0s/2帧 | reconstruct 387/400 | 🟡 | 按钮切手去抖 |
| `reconcile margin/ratio` | 8/2× | reconstruct 449 | 🟡 | amount漏读兜底阈 |
| swap evict | hamming>12 & 连续≥2 | orch 1234/SWAP_MIN | 🔴 | 头像换人去抖 |
| alias cutoff | 0.75 | orch 1343 | 🔴 | 别名合并 |
| occupancy/active th | 18/8 | orch 255/3320 | 🟡 | 占用/活跃集 hamming |
| win_yellow_count | 35 | orch 258 | 🟡 | +xx黄像素数量阈 |
| 摊牌门 | 亮160/纹50/conf0.9 | orch 1758-1807 | 🟡 | 摊牌卡接受 |
| `_AMT_SETTLE_MAX` | 3帧 | orch 311 | 🟡 | 金额settle等待上限 |
| dedup窗 | 60s | orch 2944 | 🟡 | 动作去重(每手清) |

##### B.3.7 测绘新发现(非不变量 —— 潜在 bug + 死代码,归 §7 解冻窗处理)

- 🐛 **PlayerID 垃圾名护栏零副作用洞**(orch 1330-1340):纯数字/超短名 + **无 avatar_hash** 时,既不注册名也不占 TempUser → 该座被"遗忘"(`player_id_map` 无此座),后续事件无名。**潜在 latent bug**,需验真实触发率。
- 🐛 **`_avatar_swap_count` 无过期**(见 B.2):table shrink 后陈旧计数不清理 → 缓慢泄漏 + 误判。重构时归 HandState 生命周期管理。
- ⚠️ **P-6 代码↔契约冲突**(见 B-23 行):`solver.infer_p6_calls` 加法补跟 vs `invariants.md` P-6 已降"仅守门"。**定型必 reconcile**。
- 🗑️ **timer 字段残骸**(detector 70-72/322-324):逻辑已退役(2026-06-15),字段仍初始化/部分reset、无 reader → DEAD_PENDING_DELETE。
- 🗑️ **`_pot_label_color` white/teal shadow**(orch 315 注释):仅 shadow 计数,production 走颜色 latch;potprev 同属半死(仅 LABEL_SIGNAL tap)。
- ✅ **总底池文字OCR路径已纯死**(BUTTON_CUT 权威切手后,75min/94手 fire=0)— 可直接删。

#### B.4 开工闸门(更新)

- [x] 识别层冻结(R-5 active)
- [x] Win 可用
- [ ] **#227 接口契约**:B.1/B.2/B.3 **草案在案 → 待"求解器当 TickObservation 首个真实消费者"后定型**(接口按真实需求收口,见 §头 📅)
- [ ] 求解器(#226)在场作行为保持 oracle
- 达成后 → 走 R-5 §4 解冻流程开重构窗口 → §4.3 八刀逐刀(每刀:🧪 Linux 单测过 B 域相关项 + 🖥️ Win soak 仪表盘)→ 回冻。

---

## 执行排程(2026-06-23 定稿,含真值地基)

> **取代头部旧 📅**。本程复盘(用户直觉揪出)发现旧计划的致命空洞:**全程验收皆系统内部自洽**(守恒/ B 不变量 / 求解器当 oracle),**无一处接系统外真值 = 圆形验证未闭**——正是本会话开场那个担忧绕一圈没解。
>
> **两条核心论断(定层依据)**:
> 1. **B 不变量保"不回归",不保"对"**:它定义=保持当前行为,而当前行为含 69% 脏端点 / 2741 TempUser;完美保持=连 bug 一起保。
> 2. **没真值,分不清"对"和"稳定地错"**:干净窗"变干净"、求解器"95%"——无外部尺,皆信仰。
> 故前置一层【真值地基】,贯穿全程当标尺。

### 4 条铁律(贯穿所有阶段)
1. **反循环**:动作层=主真值;端点栈/底池=**独立**交叉校验;禁端点验端点。
2. **冻结 R-5**:碰冻结代码 = 立 §5 case → Win soak → 回冻。
3. **验而不臆**:每刀前 `grep -n` 复核 B 不变量行号;信守恒前先确认切手对;**一切"变好"对真值量,不对自身量**。
4. **未验不 commit**。

### 阶段表

| 阶段 | 做什么 | 验收(对真值) | 冻结 | 机器 |
|---|---|---|---|---|
| **-1 真值地基**(贯穿) | 标一批代表性手真值(覆盖常规+野桌 Q4);**法子已定死=`record_frames` 录像 + 逐帧人工标**(2026-06-15 审计"62/62"同法,转常设)。⚠️**2026-06-23 确认:观战模式无 app 手牌历史 → 截图誊录路被否,只剩逐帧标**。**前置:`record_frames.py` 头部标"未验证",需 Win 先确认能跑** | 真值集 ≥N 手成型 → 成为 0/1/3 的标尺;每阶段**预设通过线** | 否(`tools/`) | Win 录,标注离线 |
| **0 干净窗捕获**(=HandSegmenter 第一刀) | 总底池 latch 定稿(抓 final 端点)→ 5s/~70tick 窗**多帧众数**采【端点栈+玩家ID+空座/基线】→ 按钮起新手;**补无结算手回退**(实测 ~11/705 fold 到底无总底池→仍按钮单点采) | 对真值:端点准确率↑(基线 recon 干净 31%)、TempUser 率↓(基线 2741/2d);无结算手不漏;**A/B 顺手量出"时序占噪声多少"** | **是**(§5 case) | Win soak |
| **1 求解器 + 动态筹码表** | 动作层主锚重建 + 三独立信源守恒(动作和/底池/端点);赢家与净额**从端点搬到动作层**;消化 rebuy(下游)、MAPPING_GAP | 对真值:**真捕获率(非自报)达 95% 纲领**;赢家归属准确率 | 否(新模块) | Linux 设计 + Win 验 |
| **2 #227 定型** | 求解器当 `TickObservation` 首个真实消费者→接口收口;`grep` 复核 B-48 行号;**reconcile P-6 代码(solver 加法补跟)↔ 契约(invariants.md 降"仅守门")冲突** | 接口锁定;B-48 行号全验;P-6 二选一定案 | 否 | Linux |
| **3 大重构**(绞杀榕余 7 刀) | §4.3 顺序逐刀;顺手清 2 个 latent bug(PlayerID 零副作用洞 / `_avatar_swap_count` 泄漏) | 每刀:B 不变量过 + **对真值正确性不回归** + Win soak 仪表盘 | **是**(逐刀 §5) | Linux + Win |
| **延后尾**(早先共识) | 公共牌(45% 矛盾)→ 摊牌底牌(0%,最难) | —— | —— | —— |

### 排序判断点(已与用户定)
- **0 先动手**(去噪求解器最依赖的锚+身份、ID 收益独立铁证、小刀练解冻流程);**1 设计可并行起步**(冻结豁免、各住各代码),编码建在 0 的干净地基上。

### 开工前必答 —— 已答(2026-06-23)
- ~~WePoker 有无 app 内手牌记录?~~ **答:无(观战模式无牌局历史)** → 真值路径定死=`record_frames` 录像 + 逐帧人工标。
- **新前置(转 Win 侧)**:`record_frames.py` 从未执行验证 → 阶段 -1 实际起点 = **先在 Win 上把 record_frames 跑通**(头部 [WIN 验证 CHECKLIST]),再录代表性场次、人工标。Linux 侧可先建**真值 schema + 评分对账 harness**(纯逻辑、可测,不依赖录制)。
