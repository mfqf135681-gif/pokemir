# 主题:跨座识别(avatar 跨侧/跨座认人)

> **2026-06-14 立**。识别层独立专项(座位主键落地后从求解器主线剥离)。
> **状态:🅿️ 搁置**(2026-06-14 用户拍板暂缓)。结论:OCR 名字跨侧已够稳(baseline 实证),
> avatar 跨座低 ROI;唯一未做的便宜实验=flip 确诊(见 §2),留待将来顺手,不阻塞主线。
> **边界**:本主题管"跨座/跨侧把同一物理人认出来",服务**跨手画像聚合**,非对账(对账已纯座位)。
> **关联**:[[player-id-recognition-approach]](2026-06-04 已探索:phash 跨侧崩、OCR 管跨侧)· `contracts/recognition-freeze.md`(碰冻结=解冻 case)· [[主题-识别栈]]

---

## §-1 已有探索(2026-06-04,建本主题前的历史,2026-06-14 核对补记)

⚠️ **建本主题时我漏查了这段历史,在根因/flip 上连续凭半核对下结论被用户纠正两次**
(数据驱动失守,记此为戒)。核对 [[player-id-recognition-approach]] + `tools/id_phash.py` 后的实情:

- **换座实验做过**:自定义房,用户+朋友各从左座换右座,`--frames` 比干净帧 → **跨侧崩**
  (同人 41-50 ≈ 不同人 50-62,完全重叠);游戏状态已排除,框已验镜像对称(非框锅)。
- **根因未确诊**:当时记"**大概率背景 felt 纹理**(名牌区透桌面绿,felt 随座变)**或** 镜像渲染"
  —— 是**推测、二选未定**,不是定论。
- **flip 从未执行**(2026-06-14 代码核对:`id_phash.py` 0 处 flip/翻转;`--cross` 是时序比对非翻转)
  → **flip 是真正还没验证的那一步**,也正是确诊"纹理 vs 镜像"的实验。
- **当时决策**:救它=文字抠背景纹理(= +xx 二值化同款硬问题,low ROI)→ **不救,跨侧走 OCR**。

---

## §0 定位:画像层第二通道,不是主线卡点

座位主键落地后(2026-06-13,解冻 case #2),**对账层已纯座位、不碰头像**;跨座识别只服务
**跨手画像聚合**(同一物理人坐过的不同座位归一起),而这件事**主要靠 OCR 名字**(文字不镜像、
跨座能读)。所以跨座 avatar phash 是"名字之外的第二通道"= 冗余增强。
→ **投入前先量化 OCR 名字通道的缺口(§1);缺口小则本主题可判"暂缓"。**

---

## §1 BASELINE 量化(2026-06-14,今天 14:29 场 123 手 / 1723 真动作)

**目的**:OCR 名字作跨座/跨手 key 有多脏?脏才值得 avatar 第二通道补。

| 指标 | 数值 | 解读 |
|---|---|---|
| 真动作数 / unique 名字 | 1723 / 42 | — |
| **脏名占比** | 纯数字 143(8.3%)+ Player_ 11(0.6%)+ TempUser 2(0.1%)≈ **9%** | OCR 完全没读出真名(读爆成数字/占位)= 硬洞 |
| **真名漂移伪重复** | 38 真名,difflib≥0.5 **仅 1 对**(`5UZ`/`SUZ`,S↔5 误读) | **OCR 真名通道比预期干净**——canonicalize+多帧众数已治掉大部分漂移 |
| **跨座聚合失败率** | ⏳ 待补 | 需 `seat_index`(座位主键 2026-06-13 刚落库,历史数据无)→ 收口测试新局后量化 |

### 🔴 反转性结论(数据驱动,可能降级本主题)
1. **OCR 真名通道已较干净**(38 真名仅 1 对漂移)→ avatar 第二通道补"真名漂移"的边际价值**低**;
2. **真正的洞是那 9% 纯数字/占位**(OCR 读爆)——但 **avatar 跨座本身崩(镜像),救不了这 9%**;
   且这 9% 属 OCR 读爆,归 #239 垃圾名护栏 + 数字识别,**不是跨座 phash 的菜**;
3. 故 avatar 跨座识别的**唯一**真价值 = "同人换座、OCR 名字也对、但要把不同座位归一物理人做
   跨手画像"——而这恰恰需 `seat_index` 才能量化失败率,**现在数据答不了**。
→ **暂不投入造识别器**;先做 §2(便宜)+ 等收口新局的 seat_index 跨座聚合失败率,缺口不大就封存本主题。

---

## §2 唯一未做的便宜实验:flip 确诊(搁置中,将来顺手)

**flip 从未执行**(见 §-1)。它是确诊"纹理 vs 镜像"的实验,也是这个悬案唯一没验的一步:
同一玩家左/右座头像 crop,**一侧 `np.fliplr` 翻转**再比 phash hamming —
- 掉到 0-8 → 是**镜像**(当年错怪纹理),flip 归一化几行可救;
- 仍 41-50 → 确诊**纹理**(当年推测对),avatar 跨座这条路死透。
**可离线复现**:`id_phash.py --frames "文件:座"` 已能对历史帧直接比;若 2026-06-04 换座录像帧
还在(Win `data/recordings/`),加几行 `fliplr` 重跑即可,**不需重录、不碰 4000 常客**。
**ROI 定调**:即便确诊镜像可救,baseline 已证 OCR 名字跨侧够稳 → avatar 跨座边际价值仍低;
flip 价值=**了结悬案**(免再凭印象争),非解锁高价值功能 → 故搁置,不阻塞主线。

## §3 候选特征谱(若 §2 非纯镜像,按成本排)
- **颜色直方图**:天然对水平镜像不变(翻转不改颜色分布);区分度低,做粗筛/配 phash;
- **flip-phash 组合**:min(hash, flip后hash) 的 hamming;
- **行为指纹(非视觉)**:换座时栈带走 → 上手末栈 ≈ 下手初栈 + 同时消失/出现(弱,独立通道);
- **CNN embedding**:重,YAGNI,前面都不行再上。

## §4 验证基建(已有)
自定义房可控换座录制 + `--frames` 直接比同人不同座的干净帧([[player-id-recognition-approach]] 记此法)。
live 无法控制谁换座 → 这是跨座识别唯一能干净验证的方法,任何方案前置。

## §5 红线
- **R-1 image-only**:头像 crop = 截图,合规;
- **反 dim-creep**(圈梁纪律):先证 1 维 ROI 再扩,别一口气上 CNN;
- **R-5 冻结**:接入 live 识别 = 解冻 case,走流程(归因→登记→修→回归→回冻);只补 alias、不当独立 key。

## §6 待办(🅿️ 搁置,解冻条件)
本主题暂缓。**重启触发**任一:① 画像跨手聚合实测出现大量"同人被拆"且归因到换座(非 9% 读爆);
② 有空顺手做 §2 flip 了结悬案(历史帧在则零成本)。否则维持搁置。
- [ ] (低优先)§2 flip 确诊:历史换座帧 + `fliplr` 重跑 → 确诊纹理/镜像,盖棺
- [ ] (并行已知)那 9% 读爆数字名 → 归数字识别 + #239 垃圾名护栏(**非本主题**,跨座救不了)
- [ ] (现状结论)跨侧/跨场聚合走 OCR 名字,baseline 已证够稳;avatar 仅同座确认
