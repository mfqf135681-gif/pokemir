# §5.3 安全列表扩展：加入 `rois/*`

- **完成时间**：2026-05-22 21:56
- **关联需求讨论**：`requirement-discussions/2026-05-22_20-23-00_DEV结束自动commit和push.md`（confirmed）+ 本会话用户对扩展提案的 OK（REQ 快速路径）
- **关联前次 change-log**：`change-logs/2026-05-22_20-30-00_落地auto-commit-push策略至rules-dev_5.3.md`（建 §5.3 的原始 change-log）+ `change-logs/2026-05-22_21-28-00_ROI_profile_window_title改为WePoker.md`（触发本次扩展的现场案例）
- **触发红线**：无（用户已 OK 该 `.agents/*` 改动）
- **无关红线已检查**：R-1, R-2, R-3, R-4, R-5, R-6, R-7, R-8, R-9, R-10

## 1. 任务概述

- **用户原始需求**：上次改 `rois/party_poker.json` 命中护栏 1（rois/* 不在自动名单）→ 用户授权扩展安全列表，让今后 ROI 改动也走自动 commit + push
- **设计判断**：`rois/*` 内容 = ROI 坐标 + 窗口标题；非密钥、非契约、非治理规则；R-7 关注"结构一致性"而非"是否可自动 commit"；扩展进白名单零安全风险

## 2. 假设清单

| # | 假设 | 出处 |
|---|---|---|
| 1 | 用户在线 OK 视为 .agents/* 改动的 "REQ 明示批准"，无需新建独立 REQ doc | 与上次落地 §5.3 自身的处置同模式（用户 OK 即批准）；rules-req §2 单轮可解 + 无 trade-off → 快速路径合规 |
| 2 | 仅扩展 `rois/*`，不一次性扩到所有 grey zone | 最小实现 §3；其余 grey zone（如 `assets/`、`models/` 等当前不存在）按需到时再加 |

## 3. 文件变更清单

| 文件路径 | 变更类型 | 变更说明 |
|---|---|---|
| `.agents/rules-dev.md` | 修改（-1/+1） | §5.3 护栏 1 安全列表加 `· rois/*（ROI 配置 = 项目数据，非密钥非契约）` |

### 附带修复（5 分钟规则）

无。

## 4. 契约一致性检查

- 是否涉及 `contracts/` 变更：**否**
- 涉及 `.agents/*` 治理规则改动：是——用户在线 OK 明示批准

## 5. 红线合规动作

无触发。

## 6. 测试结果

- **验证路径**：快速验证（治理规则单行扩展；无可执行代码）

- **章节内一致性**：§5.3 护栏 1 的允许列表扩展，禁止列表未变；与 §5.3 其它条款（secrets / 规模 / pytest / change-log / working dir）不冲突 ✅

- **rules-dev §5.2 判定**：N/A（文档/治理规则改动）

## 7. 手动操作提醒

无。

## 8. 潜在影响范围

- **正向**：未来用户在 Win 跑 `roi_config.py` 重配 ROI 后我帮 commit，或我代改 `window_title` / `resolution`，无需护栏停手——直接自动走完
- **行为变化**：本次起，rois/* 改动按 §5.3 完整 6 项护栏（仅护栏 1 调整，其余不变）
- **关联待办**：更新记忆 `auto-commit-push-policy.md` 的安全列表段（本次同会话一并做）

## 9. 违规标注

无。
