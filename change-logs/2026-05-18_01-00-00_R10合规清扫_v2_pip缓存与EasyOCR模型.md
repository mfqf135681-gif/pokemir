# R-10 合规清扫 v2：清理 pokemir 任务造成的 pip cache + EasyOCR 模型污染

- **完成时间**：2026-05-18 01:00
- **关联需求讨论**：`requirement-discussions/2026-05-17_21-00-00_项目红线清单.md §阶段 8.4`（R-10 立项 + 已知违例清理任务，confirmed）
- **关联前次 change-log（续接型）**：`change-logs/2026-05-18_00-30-00_R10立项与setup-mcps脚本.md`（前次清理 pipx pytest；本次补清 pip cache + EasyOCR）
- **触发红线**：无（本次是 R-10 合规动作执行——消解违例不是触发）
- **无关红线已检查**：R-1, R-2, R-3, R-4, R-5, R-6, R-7, R-8, R-9, R-10

## 1. 任务概述

- **用户原始需求**：清理之前产生的可能污染其他项目或者服务器的文件或者虚拟环境
- **涉及功能模块**：无代码改动；仅执行污染清除 + 文档化"防再污染"路径
- **相邻任务**：前次 R-10 立项 + setup-mcps（`change-logs/2026-05-18_00-30-00_*.md`）已清 pipx pytest；本次接续清剩余 pokemir 责任范围内的污染

## 2. 假设清单

| # | 假设 | 出处 |
|---|---|---|
| 1 | "pokemir 责任范围内"的污染 = 时间戳吻合本项目任务 + 内容属性匹配 | 工程判断；用 `find -newer` + `du` 验证 |
| 2 | `~/.cache/{chrome-devtools-mcp,pnpm,prisma,node,typescript}` 是 flordate 项目装的，**不动** | 用户提供的 flordate `claude mcp list` 显示这些工具是 flordate 用；pokemir 不使用 |
| 3 | `~/.local/share/pipx/venvs/semgrep*` + `~/.local/bin/{semgrep,semgrep-mcp,pysemgrep}` 是 flordate 装的，**不动** | flordate `claude mcp list` 显示 semgrep MCP 已启用；pokemir 未装 semgrep |
| 4 | `~/.npm` 2.7G 混杂归属，**不动**（无法精确归因，可能误清 flordate 缓存） | 工程判断；flordate 重度用 npx |
| 5 | EasyOCR 模型路径在代码层（`recognition/ocr.py:26`）写死，要"防再污染"需独立 DEV 任务改代码 | 实测：清完后跑触发 OCR 实例化的 pytest 会重新下载到 `~/.EasyOCR/` |
| 6 | 本次完整 pytest 已跑过一次（验证基线），二次清扫确保最终态；不再跑触发 OCR 的子集 | 避免"清完又脏"的循环 |

## 3. 文件变更清单

| 路径 | 变更类型 | 变更说明 |
|---|---|---|
| `~/.cache/pip/http-v2/*` | 删除（系统） | `pip cache purge` 清 1064 files / 3140.7 MB（来自本次 venv 安装的 torch + cv2 + easyocr 等 wheel） |
| `~/.EasyOCR/` | 删除（系统） | `rm -rf ~/.EasyOCR/` 清 94MB（test_recognition 触发的 EasyOCR English 模型） |

**没有项目内代码改动**。

### 附带修复（5 分钟规则）

无。

### 故意不动的（非 pokemir 责任）

- `~/.cache/chrome-devtools-mcp` (88M) — flordate 项目
- `~/.cache/pnpm` (59M) — flordate 项目
- `~/.cache/prisma` (57M) — flordate 项目
- `~/.cache/node` (19M) — flordate 项目
- `~/.cache/typescript` (7.4M) — flordate 项目
- `~/.local/share/pipx/venvs/semgrep` (397M) — flordate 项目
- `~/.local/share/pipx/venvs/semgrep-mcp` (268M) — flordate 项目
- `~/.local/bin/{semgrep,semgrep-mcp,pysemgrep}` — flordate 项目
- `~/.npm` (2.7G) — 混杂归属，无法精确归因
- `~/.cache/claude-cli-nodejs` 等 — Claude Code 自身

## 4. 契约一致性检查

- 是否涉及 `contracts/` 变更：**否**
- 无代码改动

## 5. 红线合规动作

无新触发。本次是 R-10 立项时"已知违例待清理"段落的合规动作执行：

| R-10 §已知违例条目 | 清理动作 | 结果 |
|:---|:---|:---|
| `~/.local/bin/pytest` + pipx pytest venv | 前次任务 `pipx uninstall pytest` | ✅ 已清（前次 change-log §5） |
| `~/.cache/pip/*` 3.0G | `.venv/bin/pip cache purge` | ✅ 1064 files / 3.0G 清除 |
| `~/.EasyOCR/` 94M | `rm -rf` 两次（pytest 跑后复发，二次清扫） | ✅ 最终态干净 |

**净释放磁盘**：约 3.1GB

## 6. 测试结果

- **验证路径**：完整验证（多次清理动作，影响测试环境）

- **执行 1**（清扫 + 完整 pytest 跑通）：`.venv/bin/pytest tests/ -v --deselect tests/test_recognition.py::test_ocr_engine_on_synthetic_text`
  ```
  10 passed
   6 skipped
   1 deselected
   5 warnings
  耗时 34.75s
  ```
  → 验证清扫不破坏基线；但 `test_card_recognizer_returns_shape` 实例化 `CardRecognizer→OCREngine→easyocr.Reader()` 链触发了 EasyOCR 模型重新下载（94MB 复发）

- **执行 2**（二次清扫 + narrower 子集验证）：`pytest tests/test_recognition.py::test_action_parser tests/test_storage.py tests/test_capture.py -v`
  ```
  10 passed
   5 skipped
   1 warning
  耗时 1.07s
  ```
  → 跳过 OCR 实例化路径；清扫保持

- **rules-dev §5.2 判定**：✅ 完整 pytest 已跑通（执行 1 全 pass/skip/deselect，无 fail）；二次清扫后跑窄子集验证最终态干净

- **与基线对比**：action_parser 10/10 一直 pass；storage 3 skip + capture 2 skip 是环境约束符合预期

## 7. 手动操作提醒

⚠️ **手动操作**：

1. **核对释放空间**：
   ```bash
   df -h /  # 应有约 3.1G 额外空闲
   ```

2. **如需"防再污染"彻底解决**：见 §8 第 1 条建议的独立 DEV 任务。在此之前，**任何触发 EasyOCR 实例化的代码或测试都会重新拉模型到 `~/.EasyOCR/`**。可临时变通做法：
   ```bash
   # 跑 pytest 时排除会触发 OCR 实例化的两个测试
   .venv/bin/pytest tests/ -v \
     --deselect tests/test_recognition.py::test_ocr_engine_on_synthetic_text \
     --deselect tests/test_recognition.py::test_card_recognizer_returns_shape
   ```

3. **如需 R-10 env vars 自动生效**：在 venv 激活脚本或 `.envrc`（direnv）中加入 `docs/dev-workflow.md §6.1` 的 4 个 export。这是用户级配置，不强制本次落地。

## 8. 潜在影响范围

- **正向影响**：
  - 服务器释放约 3.1GB
  - flordate 项目缓存完全保留——不互相污染
  - pip cache 重建：下次 `pip install` 重新下载（一次性代价，可通过 R-10 env var 重定向到项目内）
  - EasyOCR：下次需要时重新下载，**但仍会落到 `~/.EasyOCR/`**——治本需修代码
- **关联待办**（按优先级）：
  - **🔴 高优独立 DEV 任务**：修 `recognition/ocr.py:26` 让 `easyocr.Reader(...)` 传 `model_storage_directory=<项目内路径>`，并在 `config.py` 加 `POKEMIR_EASYOCR_DIR` 默认指向 `./models/easyocr/`。预计 < 20 行改动，**触发 R-8 视觉识别配置一致性**（合规动作 = change-log §7 加手动操作提示）。**没做之前，R-10 防 EasyOCR 污染失效**。
  - **中优**：用户在 `.bashrc` / `.envrc` 设 R-10 env vars，让 pip / HuggingFace 等其他缓存重定向到项目内
  - **Task #2** fixture 库骨架（主线唯一未启动）

## 9. 违规标注

无。

> **决策记录（值得说明的判断）**：
> 1. **没扩大 scope 修代码**：发现 `recognition/ocr.py` 的 EasyOCR 路径写死后，本可顺手改两行解决。但该改动涉及 R-8 触发 + 跨 recognition/config 两文件 + > 5 分钟检查 → 不符合 rules-dev §4.1 "5 分钟规则"。归到独立 DEV 任务符合 rules-dev §3 "只实现当前任务"。
> 2. **flordate 缓存保留**：3GB+ flordate 工具缓存（pipx venvs / chrome-devtools-mcp / pnpm 等）完全未动——超出本项目责任范围。
> 3. **二次清扫的必要性**：第一次 pytest 跑验证基线时即触发 OCR 模型下载——此为 EasyOCR 库内部行为，非测试设计缺陷。二次清扫属合理 finalize 动作，非冗余。

> **上次违规残留状态**：仍消解（10 passed + 5 skipped 子集 + 完整跑 10/6/1deselect 均无 fail）。
