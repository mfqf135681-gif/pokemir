# Backlog 全做收尾:11 项精准提升一次性合并

- **完成时间**：2026-05-25 22:12
- **关联 REQ**：`requirement-discussions/2026-05-25_19-11-00_交叉验证架构_4层金字塔_path_B衔接.md`(confirmed)
- **关联讨论**:2026-05-25 22:00 用户决议"待办全做"
- **关联记忆**:[[cross-validation-architecture-pending]] → 后续更新为"全部完成"
- **关联前次 change-log**：`change-logs/2026-05-25_20-41-00_backlog全做_OCR鲁棒性_P3_P4_avatar指纹.md`
- **触发红线**：**R-7（pipeline / schema 协调一致)** + **R-10(`data/` 本地工件)**
- **无关红线已检查**：R-1 到 R-6, R-8, R-9

## 1. 任务概述

用户原则"精准是项目生存线",一次性实施 backlog 11 项 + 2 sanity fix:

| # | 内容 | 类别 |
|:---:|:---|:---|
| #1 | Multi-actor 物理方程修正 | 🔴 critical |
| #2 | 守恒律 SQL view + rake 容忍 | 🔴 critical |
| #3 | Fuzzy 大小写标准化 | 🟡 medium |
| #4 | 重复牌检测 | 🟡 medium |
| #5 | ID consensus(双 OCR 取长) | 🟡 medium |
| #6 | Tick 间隔 500→250ms | 🟡 medium |
| #7 | Seat-swap 检测(avatar hash 突变) | 🟡 medium |
| #8 | OCR ensemble(双 scale 择优) | 🟢 long |
| #10 | player registry 持久化 JSON | 🟢 long |
| #12a | 保险推断(stack 模式) | 🔴 critical |
| #12b | 保险 stat SQL view | 🔴 critical |
| ➕ sanity | stack OCR 漏位检测(reject 10x 突变) | bug fix |
| ➕ sanity | P3 all_in 触发收紧(stack_after≈0) | bug fix |

跳过:**#9 ROI anchor**(需要 template image 用户提供,且改动大)、**#11 远期 rake 反推**(post-MVP)。

## 2. 假设清单

| # | 假设 | 出处 |
|---|---|---|
| 1 | 多人 tick 中 pot_delta > 单 seat stack_delta 是正常物理现象 | 扑克物理学 |
| 2 | 保险赔付永远非整数(pot − rake − 保险费随机)| 用户洞察 |
| 3 | rebuy preset 是 50/100/200/...常见档位 | WePoker UI 经验 |
| 4 | tick 250ms 不影响 GPU 性能(8 座 × 7 OCR per tick 仍 < 250ms) | 用户机器 5070 Ti 富余 |
| 5 | data/ 本地存玩家 fingerprint 不入 git;Win 与 VPS 独立 | R-10 + 用户决断 |
| 6 | #9 ROI anchor 风险大 + 需 template,本次跳过 | scope 边界 |

## 3. 文件变更清单

| 文件 | 变更 |
|---|---|
| `config.py` | `CAPTURE_INTERVAL_MS` 默认 500 → 250(#6)|
| `pipeline/orchestrator.py` | json/Path import;Action allowlist(已存);_is_round_rebuy helper(#12a);PLAYER_REGISTRY_PATH + load/save(#10);_capture_player_ids 加大小写 fuzzy(#3)+ seat-swap 检测(#7)+ 双 OCR consensus(#5);_process_seat_actions stack OCR digit-miss sanity + action OCR ensemble=True(#8);went_all_in tracker(#12a 标记);_process_community_cards 加重复牌检测(#4);_end_current_hand 加 _infer_insurance 调用(#12a);_save_player_registry 落盘(#10)|
| `pipeline/detector.py` | StateTracker 加 `_went_all_in_this_hand: set[int]`(#12a);start_new_hand 重置 |
| `events/normalizer.py` | compute_confidence:multi-actor aware(diff 有符号判定)(#1);infer_action_from_delta:all_in 用 stack_after≈0 判定 |
| `recognition/ocr.py` | read_text 加 `ensemble=True` 参数(双 scale 择优)(#8);_read_one + _preprocess 加 scale 参数 |
| `contracts/views.sql` | 新 3 view:v_hand_conservation(#2)、v_player_insurance_stats(#12b)、v_low_confidence_events(P4 辅助)|
| `.gitignore` | 加 `data/`(#10 持久化目录) |
| PG 执行 | `psql -f contracts/views.sql` 三个 view 已落库 |

### 附带修复(5 分钟规则)

stack OCR 10x 突变拒收 + P3 all_in 收紧(各 ~5 行,自然属于精准改进 scope)。

## 4. 契约一致性检查

- 是否涉及 `contracts/` 变更:**是**(新增 `contracts/views.sql`)
- schema 不变(只加 view);现有 hands/action_events 行不动
- `data/` 是本地存储路径,不属于 contracts 范畴

## 5. 红线合规动作

**R-7** 触发:
- [x] 改动跨多模块(pipeline / events / recognition / contracts)— 一一对齐
- [x] view 使用 `CREATE OR REPLACE`,幂等可重跑
- [x] tick 间隔变化 — env var 允许用户覆盖,默认更激进但可回退

**R-10** 触发:
- [x] `data/` 加入 `.gitignore`,JSON 文件本地化,不污染 git

## 6. 测试结果

- **语法**:4 模块 AST OK ✓
- **infer_action_from_delta smoke**:
  - all-in 仅在 stack_after≈0 时触发(stack_after=300 → raise,不是 all_in) ✓
- **compute_confidence smoke**:
  - multi-actor(pot=150, stack=50)→ 0.9 ✓(允许)
  - pot 滞后(pot=0, stack=50)→ 0.7 ✓
- **_is_round_rebuy smoke**:100/200/1000/50 → True;999/1234/78 → False ✓
- **PG views**:3 个 view CREATE OR REPLACE 成功 ✓
- **pytest**:14 passed / 3 skipped / 0 failed ✓

## 7. 手动操作提醒

⚠️ **Win 端用户(下次启动 pipeline 前)**:

### A. `git pull`

```powershell
cd D:\project\pokemir
git pull
```

### B. 跑 pipeline 观察新行为

```powershell
.\.venv\Scripts\python.exe main.py pipeline --profile party_poker_8
```

**预期改善**:
| 维度 | 之前 | 现在 |
|:---|:---|:---|
| Tick 频率 | 500ms | 250ms(timing race 减半) |
| confidence 0.3 占比 | 22.7% | 应大幅下降(multi-actor 现归 0.9) |
| 同名大小写漂移(zhixingheyi/Zhixingheyi)| 两个不同 player_name | 合并 |
| 重复牌(`['9s','9s']`)| 入库污染 | 警告 + reject |
| 玩家换座 | 旧名继续 | 检测到 avatar 突变 → unlock + 重 OCR |
| 玩家昵称识别 | 单次 OCR | 双 OCR 取长(consensus) |
| Action OCR 精度 | 单 scale | 双 scale(2x + 3x)择优 |
| all_in 判定 | stack_delta 大就触发 | 必须 stack_after≈0 才触发 |
| stack OCR 漏位(3001001) | 进库 | 检测 + 用 prev 值 |
| 保险买入 | 未知 | hand.raw_data.insurance_inferred 自动推断 |
| 跨 session player 记忆 | 重启清零 | data/player_registry.json 持久 |

### C. SQL 查保险 / 守恒情况(我 VPS 端做)

```sql
-- 保险 buy rate per player
SELECT * FROM v_player_insurance_stats LIMIT 20;

-- 守恒律异常 hand
SELECT * FROM v_hand_conservation WHERE status = 'CHECK_REQUIRED' LIMIT 10;

-- 低 confidence event review
SELECT * FROM v_low_confidence_events LIMIT 15;
```

### D. 如果 Win GPU 扛不住 250ms tick

回退:
```powershell
$env:POKEMIR_CAPTURE_INTERVAL_MS = "500"
.\.venv\Scripts\python.exe main.py pipeline --profile party_poker_8
```

## 8. 潜在影响范围

- **正向**:
  - 数据精准度全面提升(11 维度联合)
  - SQL view 提供即时分析入口
  - 玩家 fingerprint 持久化,跨 session 玩家身份稳定
  - 保险检测(用户独创"stack 模式倒推"假说实施)
- **行为变化**:
  - tick 间隔减半 → CPU/GPU 负担 ×2
  - 多 OCR ensemble + consensus → 单 hand 内 OCR 调用增加 ~30%
  - hands.raw_data 含更多键(player_stacks_initial/final/insurance_inferred)
  - 玩家换座后 player_id_map 自动 unlock(预期行为)
- **关联待办**(post-MVP):
  - #9 ROI anchor(动态校准)
  - #11 远期 rake 反推 + 净胜负 stat
  - Path B 真正开做(VPIP/PFR/AF 等)

## 9. 违规标注

无。

## Router 第四步自检

- 模式:DEV(用户明示"全部完工")
- 产出物:6 文件改 + 1 新文件(views.sql)+ 3 PG view 部署 + 本 change-log + 1 commit + 1 push
- 红线状态:R-7 + R-10 触发,合规执行
- pytest:14 passed
- 5-min 附带修主动忍住:#9 ROI anchor 留待 template 准备后做
