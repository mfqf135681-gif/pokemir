# T91:Phase 1.5 v3.2 Step 2.2 — Mirror 写状态机

- **完成时间**:2026-05-31 01:30 UTC(北京 09:30)
- **关联需求**:`requirement-discussions/2026-05-30_phase-1-5-attention-mechanism-design.md` §11.4 Step 2 sub-step 2.2
- **触发红线**:无

## 1. 任务概述

Step 2 sub-step 2.2 **Mirror 写**:

- `StateTracker.mirror_seat_state(sidx, target)` helper(shadow 模式 best-effort)
- 在 4 个 legacy set 写点同步 mirror 调用:
  1. `_empty_seats.add` (avatar zero) → SITTING_OUT
  2. `_empty_seats.add` (dup phash T63) → SITTING_OUT
  3. `_went_all_in_this_hand.add` → ALL_IN
  4. `_folded_seats.add` → FOLDED
- ATTENTION_MODE=0 默认 → 旧 path 行为 100% 不变,新状态机仅 shadow

后续 sub-step:
- 2.3:Switch 读(ATTENTION_MODE=1 时 skip 逻辑从 seat_lifecycle.is_skippable 读)

## 2. 假设清单

1. 4 个 mutation site 涵盖所有 lifecycle-relevant 写点(T79 audit 确认)
2. mirror helper 异常吞噬安全:transition_to 失败时仅 log debug
3. auto-promote EMPTY→ACTIVE 解决初始状态问题(避 EMPTY→FOLDED 非法转换)

## 3. 文件变更清单

| 文件 | 变更 | 行数 |
|:---|:---|:---:|
| `pipeline/detector.py` | import SeatLifecycle + `mirror_seat_state` helper(28 行) | +28 |
| `pipeline/orchestrator.py` | module-level import + 4 mirror calls(共 6 行) | +6 |
| `tests/test_detector_state_machine_wire.py` | 加 6 个 mirror integration tests | +52 |
| `change-logs/.../T91_*.md` | 本文件 | - |

**总计 +86 行**(纯新增,**旧 set 写入不变**)

## 4. 契约一致性检查

- ✅ 不动 schema / DB / pipeline 主路径
- ✅ 4 个 legacy set 写入未删除 — 旧 path 仍 authoritative
- ✅ mirror 失败 → log debug,绝不抛
- ✅ ATTENTION_MODE 不被读取(本 sub-step 仅 shadow 写)
- ✅ 41/41 unit tests pass(35 旧 + 6 新 mirror)
- ✅ orchestrator module import OK(无循环依赖)

## 5. 红线合规动作

无触发。Router 4 步:
- ✅ Mode 声明(用户明示 "开发模式 B" 继续 Step 2 → DEV)
- ✅ Step 2.2 = 增量 sub-step
- ✅ 红线核验(R-1/R-7/R-3 全 NO)
- ✅ 自检:Linux unit/integration verify;Win 端 runtime smoke 在下次 session

## 6. 测试结果

```
$ python -m pytest tests/test_detector_state_machine_wire.py tests/test_state_machines.py tests/test_io_queues.py
============================== 41 passed in 3.22s ==============================

$ python -c "import pipeline.orchestrator; print('orchestrator module OK')"
orchestrator module OK
```

测试覆盖 6 mirror 模式:
- EMPTY → SITTING_OUT(直接)
- EMPTY → ALL_IN(auto-promote 经 ACTIVE)
- EMPTY → FOLDED(auto-promote 经 ACTIVE)
- ACTIVE → FOLDED(已 promote 后)
- LEAVING → ACTIVE 非法转换吞噬
- 同状态 noop

## 7. 手动操作提醒(Win 端)

**ATTENTION_MODE=0 默认** → Win 端 pipeline 行为 100% 不变,仅 seat_lifecycle 多
shadow 写 + log debug.

下次 Win 端 sync:
```powershell
cd D:\project\pokemir
git pull
.venv\Scripts\activate
python -m pytest tests/test_detector_state_machine_wire.py tests/test_state_machines.py tests/test_io_queues.py -v
# 预期 41/41 pass
```

## 8. 潜在影响范围

- ✅ pipeline 运行时 **行为 0 变化**(旧 path 仍 authoritative)
- 🟡 性能影响极小:每 fold/empty/all-in 事件加 1 次状态机 transition(~μs)
- 🟡 后续:
  - Step 2.3 Switch 读:`if sidx in tracker._folded_seats` → ATTENTION_MODE-gated
  - Step 8:删旧 _folded_seats / _empty_seats / _went_all_in_this_hand sets

## 9. 违规标注

无违规。

---

## 任务完成自检 checklist

- ✅ change-log 已写
- ✅ Linux 41/41 own tests pass
- ✅ orchestrator module import 验证
- ✅ §1.6 校准:本 sub-step shadow 写 only,2.3 switch 读 在下一 sub-step
- ✅ §11 模式守卫(用户明示 "开发模式 B" → DEV)
- ✅ §11.3 加法陷阱自检:transitional dual presence 是设计允许(Step 8 删旧),mirror helper 不构成双轨永久
