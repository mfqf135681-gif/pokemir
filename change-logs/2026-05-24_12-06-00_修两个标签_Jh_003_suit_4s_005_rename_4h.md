# 修两个 fixture 标签错误: Jh_pm_003 suit + 4s_pm_005 rename to 4h_pm_004

- **完成时间**：2026-05-24 12:06
- **关联需求讨论**：本会话用户视觉复检后明示（REQ 快速路径）："Jh_pm_003 实际红桃J;4s_pm_005 实际红桃4"
- **关联前次 change-log**：`change-logs/2026-05-24_10-54-00_修5h_pm_002标签_rank_2_改为_5.md`（前一次同类标签 bug）
- **触发红线**：无
- **无关红线已检查**：R-1 到 R-10 全无触发

## 1. 任务概述

诊断显示 7 个 pytest fail,用户视觉复检结果:
- **Jh_pm_003**: 图实际是**红桃J（Jh）**, 但 JSON 写 suit="s" → 修 JSON
- **4s_pm_005**: 图实际是**红桃4（4h）**, 但文件名 "4s_pm_005" + JSON suit="s" 都错 → **重命名 + 修 JSON**
- 其余 5 个 fail (Ts_pm_002-005, Th_pm_001) 用户确认是真实图(标签对)→**留给后续模型补强**

本次仅处理标签错,不动模型 / fixture 集结构。

## 2. 假设清单

| # | 假设 | 出处 |
|---|---|---|
| 1 | 用户视觉判断可信 | 用户实地 inspect 图像后报告 |
| 2 | 4s_pm_005 重命名为 4h_pm_004（next_seq）合规 | 当前 4h 已 001/002/003,顺接 004 |
| 3 | 用 `git mv` 而不是 rm+add 保留 history | git 跟踪重命名,blame 历史保留 |
| 4 | JSON 内容必须与文件名一致 | record_card.py 协议:文件名 = `<rank><suit>_<source>_<seq>` 由 JSON 内容生成 |

## 3. 文件变更清单

| 文件路径 | 变更类型 | 变更说明 |
|---|---|---|
| `tests/fixtures/cards/Jh_pm_003.json` | 修改（1 行）| `"suit": "s"` → `"suit": "h"` |
| `tests/fixtures/cards/4s_pm_005.png` → `tests/fixtures/cards/4h_pm_004.png` | rename | `git mv` 保留 history |
| `tests/fixtures/cards/4s_pm_005.json` → `tests/fixtures/cards/4h_pm_004.json` | rename + 修改 | `git mv` 后内容 `"suit": "s"` → `"suit": "h"` |

### 附带修复（5 分钟规则）

无（不扩散扫其它 fixture,因这种错难自动检测 [filename ↔ JSON 一致性已验,但 filename ↔ 图像内容仅人眼能判]）。

## 4. 契约一致性检查

- 是否涉及 `contracts/` 变更：**否**
- fixture JSON schema 不变

## 5. 红线合规动作

无触发。

## 6. 测试结果

- **验证路径**：快速验证（字段值改 + 文件 rename）

- **修后状态**：
  - `Jh_pm_003.json` → `{"rank":"J","suit":"h"}` ✓ 与文件名一致
  - `4h_pm_004.json` → `{"rank":"4","suit":"h"}` ✓ 与新文件名一致
  - 旧 4s_pm_005.* 不存在 ✓
  - 4h 系列序号现连续 001/002/003/004

- **pytest 影响（预测,不重训）**：4s_pm_005 测试 ID 不存在了（被改名）→ pytest 集合自动用新 ID `4h_pm_004`；模型上次训练时 4s_pm_005 是错的,4h_pm_004 是新参数化,可能 PASS（模型已经"对"了那张图）。Jh_pm_003 同理。**实际数字下次训完才能定**。

- **rules-dev §5.2 判定**：N/A（标签修复，不动模型）

## 7. 手动操作提醒

⚠️ **Win 用户**:
1. `git pull` 拉本次标签修复 + rename
2. **不需要重训**（标签变了但模型还能用——只是 ground truth 更准确,下次重训会自动用新标签）
3. 想立即看 pytest 数字: 跑 `pytest tests/test_recognition_fixtures.py --tb=no -q` 看 fail 数应该从 7 降到 5

## 8. 潜在影响范围

- **正向**：
  - 训练集 / 验证集 ground truth 修正(2 张)
  - 下次重训时数据更干净,4h 增加 1 张（从 3→4）, 4s 减少 1 张（从 5→4）
  - Jh 数量不变（仍 8 张），但内部标签纠正
- **行为变化**：
  - 下次跑训练时 `4h` 数据 +1, `4s` 数据 -1 → fixture 矩阵微调
  - 仍剩 5 个真模型错（Ts ×4 + Th ×1）—— 这些是模型对 T 与 6 视觉混淆,需后续补 fixture 或调训练
- **关联待办**：
  - **下一轮 DEV/REQ**：处理 Ts/Th 真模型错 → 用户多录 Ts/Th (各 3-5 张) 或调 rare-threshold 让 Ts 也进 rare-weight 集合

## 9. 违规标注

无。
