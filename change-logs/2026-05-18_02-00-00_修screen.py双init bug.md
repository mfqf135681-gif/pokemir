# 修 capture/screen.py 双 __init__ 重复定义（明显错误 / P0 bug）

- **完成时间**：2026-05-18 02:00
- **关联需求讨论**：无（独立 DEV 任务，源自项目初始分析中识别的 P0 bug 之一）
- **关联前次 change-log**：`change-logs/2026-05-18_01-30-00_修EasyOCR模型路径R10治本.md`（无业务延续；同期治理收尾的连续 P0 修复链中第一个）
- **触发红线**：无
- **无关红线已检查**：R-1, R-2, R-3, R-4, R-5, R-6, R-7, R-8, R-9, R-10

## 1. 任务概述

- **用户原始需求**：修 `capture/screen.py` 双 `__init__` bug
- **bug 本质**：`ScreenCapturer` 类同时定义了两个 `__init__` 方法（行 33-36 与 46-50）。Python 用后定义的覆盖前者——第一个静默失效，是死代码。第二个含 `_found_title` 属性，是实际生效版本
- **涉及功能模块**：`capture/screen.py` 单文件
- **相邻任务**：业务层 P0 bug 修复链的第一个；后续还有 cards.py 红黑不分、`_split_card_regions` 边界反转、`_process_pot` 死代码

## 2. 假设清单

| # | 假设 | 出处 |
|---|---|---|
| 1 | bug = Python 后定义 `__init__` 覆盖前者（已被分析阶段识别） | 项目初始三层分析 + 实际代码 33-50 行 |
| 2 | 修复 = 保留含 `_found_title` 的完整版本，合并到类首部（Python 惯例 `__init__` 在前） | 第二个是真实生效版；合并到首部更符 Python 习惯 |
| 3 | 用户明确要求修复 = rules-dev §4 "保留历史代码"例外条款"用户明确要求"成立 | 用户原话"修 screen.py 双 __init__" |
| 4 | `ScreenCapturer.__init__()` 公共签名 `(self)` 不变 | 两个旧 `__init__` 签名相同 |
| 5 | 行为不变：合并后实例属性集合与之前生效版（含 `_found_title`）完全一致 | 已实测 `_found_title=''` / `_sct=None` / `_window_rect=None` / `_monitor=None` |
| 6 | 验证 = 完整 pytest（rules-dev §5.1 ④ 不严格满足，保险跑） | rules-dev §5 默认完整路径 |

## 3. 文件变更清单

| 文件路径 | 变更类型 | 变更说明 |
|---|---|---|
| `capture/screen.py` | 修改（-5 行） | 删除第一个失效 `__init__` 块（原行 33-36）；保留含 `_found_title` 的完整 `__init__`（原行 46-50）并上移到类首部；`_get_sct` 与 `# ── Window tracking ──` 注释结构保留 |

### 附带修复（5 分钟规则）

无。

## 4. 契约一致性检查

- 是否涉及 `contracts/` 变更：**否**
- `ScreenCapturer.__init__()` 公共签名 `(self)` 未变；调用方（`tools/roi_config.py:70` / `pipeline/orchestrator.py:31` / `tests/test_capture.py` 等）零感知

## 5. 红线合规动作

无触发。

## 6. 测试结果

- **验证路径**：完整验证

- **执行 1**：直接 import + 实例化验证
  ```bash
  python -c "from capture.screen import ScreenCapturer; c = ScreenCapturer(); ..."
  ```
  输出：
  ```
  _found_title: ''
  _sct: None
  _window_rect: None
  _monitor: None
  ```
  → ✅ 单个 `__init__` 在行 33；4 属性都正确初始化

- **执行 2**：完整 pytest
  ```
  .venv/bin/pytest tests/ -v
  → 11 passed, 6 skipped, 0 failed, 25.64s
  ```
- **与基线对比**：与前次 EasyOCR 治本任务后 11/6/0/41.70s 行为完全一致（pytest 缩短到 25s 是因 EasyOCR 模型已缓存到 `./models/easyocr/`，无需重新下载）

- **R-10 治本验证**：跑完后 `~/.EasyOCR/` 仍不存在 ✅；`./models/easyocr/` 保持 94MB

- **rules-dev §5.2 判定**：✅ 通过

## 7. 手动操作提醒

无。本次为纯代码 bug 修复，不涉及环境变量 / 部署 / 依赖。

## 8. 潜在影响范围

- **正向影响**：
  - 消除 Python `flake8` / `pyright` 等静态扫描器对该文件的 `redefined-outer-name` 警告
  - 类结构更清晰，新读者不会困惑"哪个 `__init__` 才是真的"
  - 行为 0 变化（生效的本来就是含 `_found_title` 的版本）
- **环境/调用方影响**：无
- **关联待办**（项目初始分析中识别的剩余 P0 bug 链）：
  - `cards.py:109-115` **红黑不分**（heuristic 永远返回 h/c，无法区分 ♥/♦ 或 ♣/♠）
  - `recognition/cards.py:152-177` **`_split_card_regions` 边界反转**（社区牌切分把"间隙"当成"卡片"）
  - `pipeline/orchestrator.py:220-224` **`_process_pot` 死代码**（OCR 读了 pot 金额但从未写回）
  - `pipeline/orchestrator.py:255-261` **`_build_facing_action` 永远返回 None**（preflop 3-bet/4-bet 判定全失效）
  - `events/normalizer.py:101` **`monotone == False` 比较**（应改 `not monotone`）
  - **Task #2** fixture 库骨架（让上述识别 bug 修复有量化对照）

## 9. 违规标注

无。

> **上次违规残留状态**：仍消解（连续 5 个任务 11 passed + 6 skipped + 0 failed 基线稳定）。
