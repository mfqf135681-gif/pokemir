# 修 `5h_pm_002.json` 标签错误：rank "2" → "5"

- **完成时间**：2026-05-24 10:54
- **关联需求讨论**：本会话用户对话明示（REQ 快速路径）："5h_pm_002 实际是红桃5,可能是我手动复检纠偏时搞错的。你改,我录"
- **关联前次 change-log**：`change-logs/2026-05-24_06-22-00_清空fixture库全部数据_准备重录.md`（重录起点）
- **触发红线**：无（仅修 fixture 元数据标签）
- **无关红线已检查**：R-1, R-2, R-3, R-4, R-5, R-6, R-7, R-8, R-9, R-10

## 1. 任务概述

- **bug 现象**：诊断脚本 + pytest 揭示 `5h_pm_002.json` 内 `rank` 字段标的是 "2"（红桃 2），但**文件命名是 5h**（红桃 5）、图像内容**用户肉眼复检也确认是红桃 5**。
- **判定**：JSON 标签错,文件名和图像内容正确,**JSON 改写**为唯一修法。
- **可能成因**（用户自述）：手动复检纠偏时输入错误。

## 2. 假设清单

| # | 假设 | 出处 |
|---|---|---|
| 1 | 文件名是 ground truth (因为 record_card.py 命名 = JSON content) | 用户视觉确认 5h_pm_002.png 内容是红桃 5,匹配文件名 |
| 2 | 不需要重命名文件,只改 JSON | 因为文件名已对,只是 JSON 漂移 |
| 3 | suit "h" 字段不变 | 红桃 = h,与文件名一致,无需改 |

## 3. 文件变更清单

| 文件路径 | 变更类型 | 变更说明 |
|---|---|---|
| `tests/fixtures/cards/5h_pm_002.json` | 修改（1 行）| `"rank": "2"` → `"rank": "5"` |

### 附带修复（5 分钟规则）

无（暂不主动扫描其它 fixture JSON,因为这种错很可能是孤立事件；如果重训后还有奇怪误判,再考虑全量扫一遍 `<filename>` vs `<JSON rank+suit>` 一致性）。

## 4. 契约一致性检查

- 是否涉及 `contracts/` 变更：**否**
- fixture JSON schema 不变（仍 `expected.rank` + `expected.suit` + `source`）

## 5. 红线合规动作

无触发。

## 6. 测试结果

- **验证路径**：快速验证（单字段值改）

- **执行**：`cat tests/fixtures/cards/5h_pm_002.json` → 显示 `rank` 现为 "5" ✓

- **pytest 状态**：本次改动**不重训模型**,但 pytest 测试断言会用新 rank "5"。预期 `5h_pm_002` 现在 PASS（因为上一轮训的模型已 recognize 它为 "5"）→ pytest 4 failed 减为 3 failed。

- **rules-dev §5.2 判定**：通过——本次修标签,模型识别本来就是对的（认为是 5）;修后 ground truth 与模型对齐。

## 7. 手动操作提醒

⚠️ **Win 用户**:
1. `git pull` 拉本次 JSON 修复
2. 录制 4h ×3 + Jh ×4（按上一轮建议）
3. commit + push 后我重训

## 8. 潜在影响范围

- **正向**：
  - fixture 集 ground truth 修正
  - 重训时 5h_pm_002 不会再 mislead 模型(变成"5h 学样本"反向加强,而非误导模型学成 2h)
  - pytest fail 减少 1 个（94.87% → 略升）
- **行为变化**：无运行时改动
- **关联待办**：用户补 4h + Jh fixture,然后我重训

## 9. 违规标注

无。

---

## 元思考：手动复检 vs record_card.py 的 trade-off

`record_card.py` 录制时**完全相信用户输入**（不二次校验）。这次的 bug 暴露了一个潜在风险：**用户疲劳时输入错误**会在 fixture 集中沉淀,并在训练时反向污染模型。

未来 P2 优化（不在本次 scope）：可以让 record_card.py 录完一张后**立刻调用现有模型**给出预测,跟用户输入对比；不一致就再让用户复看。但这需要模型已经存在（鸡生蛋问题），且会拖慢录制节奏。**暂不实施**。
