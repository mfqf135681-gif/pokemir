# T98:Phase 1.5 v3.2 Step 3.3a — focus seat helper + OCR-2 capture skeleton

- **完成时间**:2026-05-31 06:30 UTC(北京 14:30)
- **关联需求**:`requirement-discussions/2026-05-30_phase-1-5-attention-mechanism-design.md` §2.4 Pattern D / §11.4 Step 3
- **触发红线**:无

## 1. 任务概述

Step 3.3 整体 ~0.9w 工程量,本 sub-step 3.3a 实施**最小可 ship 部分**:

- `PipelineOrchestrator.get_focus_seat() -> int | None`:从 `tracker._pointer_state["current_seat"]` 读 timer seat(T48 shadow 现成数据源)
- `PipelineOrchestrator._capture_focus_seat_ocr(rois, focus_seat) -> dict`:用 `self.ocr_focus` 抓 focus seat 的 action / amount / chip,返回 dict
- **未 wire 进 `_tick`**(Step 3.3b 接入)
- ATTENTION_MODE=0 / ocr_focus None / focus_seat None → 返空 dict,**legacy path 完全不受影响**

后续 sub-step:
- 3.3b:_tick 加 attention 分支,调用 _capture_focus_seat_ocr
- 3.3c:Pattern D merge logic + cross-validate + Ring beam 仲裁
- 3.5:Win 端实测 verify

## 2. 假设清单

1. `_pointer_state["current_seat"]` 在 T48 shadow scan 后填充(已现成)
2. OCR-2 抓 focus seat 3 个 ROI(action_area, amount, stack_area)~10ms 总
3. legacy path(`self.ocr.read_text_batch` 在 `_pre_batch_action_amount_ocr`)继续工作,**OCR-2 不替代,仅 additional**

## 3. 文件变更清单

| 文件 | 变更 | 行数 |
|:---|:---|:---:|
| `pipeline/orchestrator.py` | 加 `get_focus_seat` + `_capture_focus_seat_ocr` helper methods | +57/-0 |
| `tests/test_orchestrator_ocr_wire.py` | 加 `TestPatternDFocusSeatT98`(5 gating tests) | +49 |
| `change-logs/.../T98_*.md` | 本文件 | - |

**总计 +106 行**(纯新增 helpers,**0 改动**现有 read path)

## 4. 契约一致性检查

- ✅ 不动 schema / pipeline 主路径 / DB
- ✅ `_tick` 不改 — helpers ready 但未 wire(3.3b 才接)
- ✅ Mode=0 行为 100% 不变(`_capture_focus_seat_ocr` ATTENTION_MODE gate 早 return)
- ✅ 59/59 unit tests pass(54 旧 + 5 新 focus seat gating)
- ✅ orchestrator module import 验证 OK

## 5. 红线合规动作

无触发。Router 4 步:
- ✅ Mode 声明(用户明示 "A" 继续 Step 3.3 → DEV)
- ✅ Step 3.3a scope:helpers 最小化 ship,_tick 不动
- ✅ 红线核验(R-1/R-7/R-3 全 NO)
- ✅ 自检:per [[dev-rule-validate-blind-spots]] (a) — gating 测试覆盖 Linux 可验范围,真 OCR 抓 ROI 行为 Win 端 Step 3.5 验

## 6. 测试结果

```
$ python -m pytest tests/test_orchestrator_ocr_wire.py -v
tests/test_orchestrator_ocr_wire.py::TestOrchestratorOCRWire ... 3 passed
tests/test_orchestrator_ocr_wire.py::TestPatternDFocusSeatT98 ... 5 passed
============================== 8 passed in 1.06s ===============================

$ python -m pytest tests/test_ocr_multi_instance.py tests/test_state_machines.py \
    tests/test_io_queues.py tests/test_detector_state_machine_wire.py \
    tests/test_orchestrator_ocr_wire.py
============================== 59 passed in 2.97s ==============================
```

5 个新 gating tests:
- `test_get_focus_seat_no_active_hand`(无 hand 返 None)
- `test_get_focus_seat_reads_pointer_state`(读 _pointer_state["current_seat"])
- `test_capture_focus_seat_ocr_attention_off`(mode=0 早 return)
- `test_capture_focus_seat_ocr_no_focus_seat`(focus_seat None 早 return)
- `test_capture_focus_seat_ocr_no_ocr_focus`(ocr_focus None 防御性早 return)

## 7. 手动操作提醒(Win 端)

**本次无 Win 端必做** — helpers 不在 _tick 中调用,runtime 行为不变。

下次 Win 端 Step 3.3b 接入后才真 verify。

## 8. 潜在影响范围

- ✅ pipeline 运行时 **行为 0 变化**(helpers 未 wire)
- 🟡 后续 sub-step 3.3b 接入 _tick 时:
  - 每 tick attention mode 多 1 个 OCR-2 call(focus seat 3 ROI ~10ms)
  - mode=0 不影响

## 9. 违规标注

无违规。

---

## 任务完成自检 checklist

- ✅ change-log 已写
- ✅ Linux 5 gating tests + 54 旧 = 59/59 pass
- ✅ §1.6 校准:helpers ready,3.3b 接入 _tick
- ✅ §11 模式守卫(用户明示 "A" → DEV)
- ✅ §11.3 加法陷阱自检:**helpers 未 wire 不构成双轨**,3.3b 接入是设计允许的 transitional dual presence(Step 8 收编单 path)
