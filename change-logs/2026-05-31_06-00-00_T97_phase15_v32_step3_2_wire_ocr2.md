# T97:Phase 1.5 v3.2 Step 3.2 — Wire OCR-2 进 orchestrator

- **完成时间**:2026-05-31 06:00 UTC(北京 14:00)
- **关联需求**:`requirement-discussions/2026-05-30_phase-1-5-attention-mechanism-design.md` §2.4 Pattern D 双 OCR
- **触发红线**:无

## 1. 任务概述

Step 3 sub-step 3.2 — **OCR-2 instance wire 进 orchestrator(ATTENTION_MODE gated)**:

- OCR-1 自 `self.ocr` 升级:加 `name="global"` + `default_allowlist`(attention mode 时 "弃牌跟注让牌加下0123456789",legacy mode 空)
- 新增 `self.ocr_focus: OCREngine | None`:
  - `ATTENTION_MODE=1` → 实例化 `OCREngine(name="focus")`
  - `ATTENTION_MODE=0` → `None`(**不分配 VRAM,mode=0 行为 100% 不变**)
- 仅 wire,**Pattern D 协作 read path 不变**(Sub-step 3.3 接入)

## 2. 假设清单

1. 5070 Ti 16GB 余 13GB,双 EasyOCR Reader (~1.5GB × 2) 不冲 VRAM(Win 端 Step 3.5 真验)
2. `OCREngine.__init__` 仅 set 字段,不 init Reader(lazy via `_init`)→ 安全 wire
3. legacy mode 行为通过 ATTENTION_MODE=0 → `ocr_focus=None` + `default_allowlist=""` 保证

## 3. 文件变更清单

| 文件 | 变更 | 行数 |
|:---|:---|:---:|
| `pipeline/orchestrator.py` | `__init__` OCR 实例化重构:OCR-1 加 name+allowlist,OCR-2 lazy gated | +19/-1 |
| `tests/test_orchestrator_ocr_wire.py` | 新建 — 3 wire integration tests | +60 |
| `change-logs/.../T97_*.md` | 本文件 | - |

**总计 +79 行**(纯新增 wire,无 read path 改动)

## 4. 契约一致性检查

- ✅ 不动 schema / pipeline 主路径 / DB
- ✅ `self.ocr` 仍 `OCREngine` 实例(改 name 默认不破坏调用)
- ✅ Mode=0 行为 100% 不变(`ocr_focus=None` + `ocr.default_allowlist=""`)
- ✅ Mode=1 wire 完整(双 instance + name 区分 + VRAM allocated)
- ✅ 54/54 unit tests pass(51 旧 + 3 新 wire tests)

## 5. 红线合规动作

无触发。Router 4 步:
- ✅ Mode 声明(用户明示 "A" 继续 Step 3.2 → DEV)
- ✅ Step 3.2 scope:wire only,read path 不动
- ✅ 红线核验(R-1/R-7/R-3 全 NO)
- ✅ 自检:per [[dev-rule-validate-blind-spots]] (a) — Linux 3 wire tests cover instance 隔离,VRAM 真行为 Win 端 Step 3.5 验

## 6. 测试结果

```
$ python -m pytest tests/test_orchestrator_ocr_wire.py -v
tests/test_orchestrator_ocr_wire.py::TestOrchestratorOCRWire::test_attention_mode_off_no_focus_ocr PASSED
tests/test_orchestrator_ocr_wire.py::TestOrchestratorOCRWire::test_attention_mode_on_focus_ocr_wired PASSED
tests/test_orchestrator_ocr_wire.py::TestOrchestratorOCRWire::test_attention_mode_ocr_instances_independent PASSED
============================== 3 passed in 1.02s ===============================

$ python -m pytest tests/test_ocr_multi_instance.py tests/test_state_machines.py \
    tests/test_io_queues.py tests/test_detector_state_machine_wire.py tests/test_orchestrator_ocr_wire.py
============================== 54 passed in 2.92s ==============================
```

## 7. 手动操作提醒(Win 端)

**ATTENTION_MODE=0 默认** → pipeline 行为 100% 不变,**VRAM 不变**。

下次 Win 端 sync 后启用 mode=1 可验:
```powershell
git pull
$env:POKEMIR_ATTENTION_MODE = "1"
python main.py pipeline --profile party_poker_8 --observer
# 启动 log 应见:
#   OCREngine[global] init: gpu=True
#   OCREngine[focus] init: gpu=True (首次 _init 触发后)
#   Phase 1.5 v3.2 双 OCR wired: ocr=global / ocr_focus=focus
# nvidia-smi 期望 VRAM ~3 GB(原 1.5 GB)
```

若 OOM / 不稳:
```powershell
$env:POKEMIR_ATTENTION_MODE = "0"   # 1 行回滚 → ocr_focus None
```

## 8. 潜在影响范围

- ✅ pipeline 运行时(ATTENTION_MODE=0)**行为 0 变化 + VRAM 0 变化**
- 🟡 ATTENTION_MODE=1:VRAM +1.5GB(OCR-2 Reader)— Win 验
- 🟡 后续:
  - 3.3 Pattern D tick-aligned merge logic(真双 OCR 协作)
  - 3.4 Ring beam 仲裁 integration
  - 3.5 Win 端实测 verify

## 9. 违规标注

无违规。

---

## 任务完成自检 checklist

- ✅ change-log 已写
- ✅ Linux 3 wire tests + 51 旧 = 54/54 pass
- ✅ §1.6 校准:本 sub-step wire only,Pattern D logic 是 3.3
- ✅ §11 模式守卫(用户明示 "A" 继续 DEV)
- ✅ §11.3 加法陷阱自检:**ATTENTION_MODE gate 是设计允许的 transitional dual presence**,Step 8 时删旧 self.ocr 单 instance + 直接换名 self.ocr_global / self.ocr_focus
