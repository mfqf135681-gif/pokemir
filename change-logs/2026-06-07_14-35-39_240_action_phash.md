# #240 实装动作识别:text-shape phash 二元桩(替 action OCR)

- **完成时间**:2026-06-07 14:35
- **关联需求讨论**:`requirement-discussions/2026-06-07_action-recognition-text-shape-phash.md`(方法实测定型)
- **触发红线**:无
- **无关红线已检查**:R-1~R-10 均不触发。**Image-only(红线)合规**——仍是屏幕截图 + 图像 hash(色盲文字形状),无 OCR/DOM/抓包。

## 1. 任务概述
动作识别从 EasyOCR(242ms/27% tick,且 idle 被 allowlist 逼出垃圾)改为**色盲+位置无关的归一化文字形状 phash 二元桩**。
依据:探针实测 4 动作各自干净(check34/raise37无bet混/call18/bet5全对),且解决"下注实测多色 + 各座 ROI 相对位置不一 + idle 噪声"。
结构同 digit 配方(reader 类 + JSON 参考 + flag + builder),flag-gated 默认关。

**§2.0 四步前置**:深读(digit配方/action parser词表/动作读取注入点 grep 实证)→ 评估(复用配方架构 + parse吃中文词 + harness/live须同源text_shape_hash)→ 分析(import _hamming 成环→本地定义;BGRA/BGR都吃)→ 修正(共享模块 + flag-gated 回退)。

## 2. 假设清单
1. `ActionRecognizer.parse` 吃中文词 过牌/加注/跟注/下注 → matcher 返中文词即接入,下游不动 —— 出处:`recognition/actions.py:25-52`。
2. action_area 坐标帧内相对、live 帧 BGRA —— 出处:`capture/screen.py:147/159`(text_shape_hash 两种通道都吃)。
3. text-shape phash 方法已实测验收 —— 出处:requirement-discussions doc §3 + tools/probe_action_phash。

## 3. 文件变更清单
| 文件 | 变更 | 说明 |
|:---|:---|:---|
| `pipeline/action_phash.py` | **新建** | `text_norm_img`/`text_shape_hash`(色盲抠白字+bbox归一+resize+8×8 aHash,Win-only)· `hamming`/`match_hash`(纯逻辑)· `ActionPhashReader.load/.match`(→中文词或None)· `LABEL_TO_WORD` |
| `tools/build_action_refs.py` | **新建** | 锚(frame:seat:label)→ 参考 → `rois/action_refs_<profile>.json`;复用模块 text_shape_hash |
| `tools/probe_action_phash.py` | 改 | **删本地 text_norm_img/text_shape_hash,改 import 模块同一份**(防 harness/live 漂移,`is` 验证为同一对象) |
| `config.py` | 新增 | `ACTION_PHASH_LIVE`(默认 0;需 action_refs 文件,缺则回退 OCR) |
| `pipeline/orchestrator.py` | 新增加载 + 注入 1 处 | init 载 `self._action_phash`(哨兵,缺文件→None)· 动作读取:`_action_phash` 非空时 `match(action_area)`→中文词(喂 parse)/落空→""(无动作,**不回退 OCR**),替 action OCR;import 加 `ACTION_PHASH_LIVE` |

**设计**:与 digit 配方同构;flag 关 = 逐字节不变(默认 OCR 路);flag 开但无参考文件 → 自动回退 OCR(零风险);text_shape_hash live/probe/builder **单一实现**(harness=live 铁律)。

## 4. 契约一致性检查
- 涉及 `contracts/`:**否**。matcher 产出中文动作词喂既有 parse,ActionType/事件结构不变。

## 5. 红线合规动作
- 无触发。Image-only 复核:phash on 截图,合规。

## 6. 测试结果
- 验证路径:**完整验证**(多文件 + 新模块 + 热路径注入)。
- `py_compile` 5 文件 ✅。
- `pipeline.action_phash` 纯逻辑单测(`.venv`):hamming / match_hash(命中最近 / 落空 / margin 歧义拦截) / `ActionPhashReader.load+match`(→中文词/None) / LABEL_TO_WORD —— **全过**。
- `orchestrator` + `action_phash` + builder + probe 干净 import ✅。
- **防漂移**:`probe.text_shape_hash is action_phash.text_shape_hash` == **True**(单一实现)。
- 现有套件 `test_orchestrator_ocr_wire + test_detector_state_machine_wire`:**23 passed**。
- ⚠️ **Win owed**(cv2/GPU/真帧,Linux 验不了):① `build_action_refs.py` 建参考文件;② `ACTION_PHASH_LIVE=1` 跑,验动作捕获准度(vs 实际)+ tick 提速(seat_action_ocr ms↓);③ 阈值/误命中(漏动作率、加注/下注是否粘→需 margin)。

## 7. 手动操作提醒
⚠️ Win 端(本任务交付 = flag-gated 路径 + Linux 验;实测留 Win):
1. 建参考:`python tools\build_action_refs.py --frames-dir "data\recordings\<ts>\frames" --profile party_poker_8 --anchors "f_000097:0:check,f_001028:7:raise,f_001028:6:call,f_002175:6:bet" --threshold 10`(多锚=多参考,建议每动作多采几个/不同座抗视觉态)
2. 开:`$env:POKEMIR_ACTION_PHASH_LIVE="1"` → `python main.py pipeline --profile party_poker_8`
3. 我 MCP 读 action_events 验动作捕获 + per-phase seat_action_ocr ms↓

## 8. 潜在影响范围
- flag 关:零影响(默认 OCR 路)。
- flag 开 + 有参考:action 读取走 phash(微秒级),amount 仍 digit 配方,fold/all-in 仍头像区(#235),parse/事件不变。
- flag 开 + 无参考文件:自动回退 OCR(警告日志)。
- 落空不回退 OCR = phash 即权威;若参考不全会漏动作(多参考 + 动作持久态缓解;Win 验漏读率)。

## 9. 违规标注
无。(四步前置走全;text_shape_hash 单一实现守 harness=live;准度/速度盲区诚实标 Win-owed。)
