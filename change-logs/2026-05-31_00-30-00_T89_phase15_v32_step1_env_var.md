# T89:Phase 1.5 v3.2 Step 1 — POKEMIR_ATTENTION_MODE env var

- **完成时间**:2026-05-31 00:30 UTC(北京 08:30)
- **关联需求**:`requirement-discussions/2026-05-30_phase-1-5-attention-mechanism-design.md` §11.4 Step 1
- **触发红线**:无

## 1. 任务概述

Phase 1.5 v3.2 实施 **Step 1 of 9 步执行序**:加 `POKEMIR_ATTENTION_MODE` env var 到 `config.py`,默认 `False`。

为后续 Step 2-9(拆 orchestrator + 双 OCR + 13 规则 + §12 摊牌专项 等)提供:
- ✅ 回滚机制基础设施(env var 切换 vs 双轨代码)
- ✅ 旧 path 100% 不变(false 默认)
- ✅ 后续 step 实施时,新 path 包 `if ATTENTION_MODE:` 守卫

## 2. 假设清单

1. `POKEMIR_USE_GPU=1` + `POKEMIR_OCR_BATCH=1` 是 ATTENTION_MODE 前提(已 ship)
2. 默认 False → 不集成 T80/T82 模块 → 旧 pipeline 行为 0 变化
3. 后续 Step 2-9 在多 session / 多周持续完成,**非本次 ship**

## 3. 文件变更清单

| 文件 | 变更 | 行数 |
|:---|:---|:---:|
| `config.py` | 加 `ATTENTION_MODE` env var + 注释 + 跟 §11.3 陷阱 4 关联 | +8 |
| `change-logs/.../T89_*.md` | 本文件 | - |

**总计 +8 行**(纯新增,**0 改动**现有行为)

## 4. 契约一致性检查

- ✅ 不动 schema / orchestrator / pipeline 运行时
- ✅ 默认 False → 旧 path 完整保留
- ✅ T80/T82 模块仍 standalone(未 import 到 orchestrator)
- ✅ 31/31 现有 unit tests 仍 pass

## 5. 红线合规动作

无触发。Router 4 步:
- ✅ Mode 声明(用户明示 "开发模式 启动 Phase 1.5 v3.2" → DEV)
- ✅ Step 1 of 9 = 最小可 ship 单位
- ✅ 红线核验(R-1/R-7/R-3 全 NO)
- ✅ 自检:scope 明示(本次仅 Step 1,Step 2-9 未启动)

## 6. 测试结果

```
$ python -c "from config import ATTENTION_MODE; print(f'ATTENTION_MODE={ATTENTION_MODE}')"
ATTENTION_MODE=False

$ python -m pytest tests/test_state_machines.py tests/test_io_queues.py -v
============================== 31 passed in 3.00s ==============================
```

## 7. 手动操作提醒(Win 端)

**本次无 Win 端必做** — 默认 False,旧 pipeline 行为 100% 不变。

未来 Step 2-9 实施完成后,启用:
```powershell
$env:POKEMIR_ATTENTION_MODE = "1"
python main.py pipeline ...
```
若新架构 bug → 1 行回滚:
```powershell
$env:POKEMIR_ATTENTION_MODE = "0"
```

## 8. 潜在影响范围

- ✅ pipeline 运行时 **0 变化**
- ✅ 新 env var 暴露为 module-level constant,后续 step 实施可 `from config import ATTENTION_MODE`
- 🟡 后续 Step 2-9:
  - Step 2 拆 orchestrator + 集成 T80 SeatStateMachine/HandPhaseMachine
  - Step 3 双 OCR + Pattern D
  - Step 4 Multi-pot OCR
  - Step 5 13 规则盲点
  - Step 5.5 §12 摊牌专项(CNN throttle 1/s → 4/s)
  - Step 6 Tier 1 提升
  - Step 8 删旧 patch + grep 检测
  - Step 9 ground truth verify

## 9. 违规标注

无违规。

---

## 任务完成自检 checklist

- ✅ change-log 已写
- ✅ Linux smoke verify 通过
- ✅ §1.6 校准:本次仅 Step 1,Step 2-9 是后续多 session 工程
- ✅ §11 模式守卫(用户明示 "开发模式" → DEV)
- ✅ §11.3 加法陷阱自检:**非双轨**(env var 是回滚机制,不是 dual-path code)
