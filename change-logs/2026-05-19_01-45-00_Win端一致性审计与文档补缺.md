# Win 端一致性审计与文档补缺（复合任务链 T+1）

- **完成时间**：2026-05-19 01:45
- **关联需求讨论**：`requirement-discussions/2026-05-17_20-30-00_开发工作流与MCP推荐.md`（confirmed，topology B）
- **关联前次 change-log**：
  - `change-logs/2026-05-18_05-00-00_P0批修monotone比较和process_pot死代码.md`（同复合链 T1+T2）
  - `change-logs/2026-05-18_05-15-00_fixture库骨架建立.md`（同复合链 T3）
- **触发红线**：无
- **无关红线已检查**：R-1, R-2, R-3, R-4, R-5, R-6, R-7, R-8, R-9, R-10

## 1. 任务概述

- **用户原始需求**："按照 1、2、3、0 顺序执行，另，跟新文档和脚本，确认 win 端能够正常执行" 中 "确认 win 端能够正常执行" 子项
- **目标范围**：审计 T1/T2/T3/T0 各产物（代码 + 脚本 + 配置）跨平台兼容性；补 docs 缺口（**只补缺**，不引入未讨论的 Win 部署细节）
- **涉及功能模块**：`docs/`（仅 §2.4 一处增补说明段）

## 2. 假设清单

| # | 假设 | 出处 |
|---|---|---|
| 1 | "Win 端能正常执行" = T1/T2/T3 代码改动在 Win 上不会因路径/编码/平台 API 差异崩溃 | dev-workflow §4 "Windows 副本只读约定"——Win 是 runtime，不是 dev |
| 2 | Win 端 PG 部署属独立 setup 任务、不在本复合链 scope | rules-dev §3 最小实现；引入 Win PG 部署需先做 REQ 讨论（Docker Desktop / 原生 / WSL2 取舍） |
| 3 | 缺口处理策略 = 文档显式声明"不在本节范围"+ 触发条件 + 解耦说明，不写未验证的部署步骤 | rules-doc §2 "只写已验证的事实"；rules-dev §3 反过度设计 |

## 3. 文件变更清单

| 文件路径 | 变更类型 | 变更说明 |
|---|---|---|
| `docs/dev-workflow.md` | 修改（§2.4 内 +1 段说明 blockquote） | 在 "与 Win 端的关系" 表后加 Win PG 部署 scope 声明：Linux A/B/C 步骤**不适用** Win；Win 三条候选路径需独立 REQ 讨论；当前 Win 端 pipeline runtime 默认假设用户已自行配妥 PG，与本仓代码改动解耦 |

### 附带修复（5 分钟规则）

无。

## 4. 契约一致性检查

- 是否涉及 `contracts/` 变更：**否**
- 仅文档增量，无 API / 数据模型 / ORM 影响

## 5. 红线合规动作

无触发。

## 6. 测试结果

- **验证路径**：纯文档变更——无需 pytest；仅 `bash -n` 等价文档语法检查不适用

- **代码兼容性审计结果**（T1+T2+T3 各产物逐项核对）：

  | 产物 | Win 兼容性核对 | 结论 |
  |:---|:---|:---:|
  | `events/normalizer.py:101` (`not texture[...]`) | 纯 Python 布尔语义 | ✅ |
  | `pipeline/detector.py` (`latest_pot_bb` 字段) | 纯 Python attribute | ✅ |
  | `pipeline/orchestrator.py:_process_pot / _process_seat_actions` | 仅赋值/读取 Python 字段 | ✅ |
  | `tests/test_recognition_fixtures.py` 路径用 `pathlib.Path` | 跨平台路径 | ✅ |
  | `tests/test_recognition_fixtures.py` `cv2.imread(str(img_path))` | str 转换处理 Win 反斜杠路径 | ✅ |
  | `tests/test_recognition_fixtures.py` `read_text(encoding="utf-8")` | 显式编码 | ✅ |
  | `tests/fixtures/cards/_README.md` 文件命名 `<rank><suit>_<source>_<seq>` | 全 ASCII，无 Win 文件名禁字符 | ✅ |
  | `tests/fixtures/cards/.gitkeep` | git tracked 空文件 | ✅ |

- **既有脚本 Win 兼容性核对**（前几轮已加入但本轮验证）：

  | 脚本 | Win 兼容性 | 备注 |
  |:---|:---|:---|
  | `tools/setup-mcps.sh` | bash 脚本，Win 上需 git-bash / WSL 执行 | 用 `POKEMIR_FS_PATH` env var 支持自定义路径；用户在 Win 端用 `setx` / git-bash 设此变量即可 |
  | `tools/sync-from-vps.sh` | **设计就是 Win 端运行**（rsync 拉取） | 假设 git-bash + rsync.exe 已装 |
  | `docker-compose.yml` | **Linux dev only**，已在文件 header 注明 | Win 不跑此 compose |
  | `.env.example` | INI 格式，跨平台 | ✅ |

- **rules-test §3 判定**：纯文档+审计任务，无运行时回归——免 pytest 跑全套；按 rules-doc §4.1 用快速验证（grep + 读关键路径核对）

## 7. 手动操作提醒

⚠️ **Win 测试机用户首次部署 PG 时**（不在本任务 scope，但用户后续会触发）：

1. 不要照搬 `docs/dev-workflow.md §2.4` A/B/C——那是 Linux apt + Docker daemon 流程，Win 上不适用
2. 先开 REQ 讨论："Win 端 PG 部署选型——Docker Desktop / 原生 EnterpriseDB 安装包 / WSL2"
3. 选定后才写入 `docs/dev-workflow.md` §2.4 的新子节

## 8. 潜在影响范围

- **正向**：
  - 复合链 T1/T2/T3 全部产物经审计确认 Win 兼容；用户在 Win 端跑 `git pull` + `pytest tests/` 不会因本轮改动崩溃
  - docs §2.4 显式声明 Win PG 部署 scope 边界，避免未来用户误将 Linux Docker 步骤套到 Win 上
- **行为变化**：
  - 无运行时变化
- **关联待办**：
  - 用户在 Win 端首次需要 `pytest tests/test_storage.py` 或开启生产 DB 落库时 → 触发 "Win 端 PG 部署选型" REQ 讨论
  - 用户在 Win 端先做：`git pull` 拉 T1+T2+T3 代码 + fixture skeleton；本轮代码 Win 上免改即可跑

## 9. 违规标注

无。

---

## 复合任务链最终自检（Router 第四步）

**复合链回顾**：用户指令 "按照 1、2、3、0 顺序执行" → 已分解为 T1+T2+T3+T0+T+1。

| 子任务 | 模式 | 产物 | 测试基线 | 红线检查 |
|:---|:---:|:---|:---|:---:|
| T1: `monotone == False` 修正 | DEV | `events/normalizer.py` -1/+1 | 14p/3s/0f 维持 | ✅ R-1..R-10 无触发 |
| T2: `_process_pot` 死代码接通 | DEV | `pipeline/detector.py` +字段；`pipeline/orchestrator.py` 写入+读取 | 14p/3s/0f 维持 | ✅ R-1..R-10 无触发 |
| T3: fixture 库骨架 | DEV | `tests/fixtures/cards/_README.md` + `.gitkeep` + `tests/test_recognition_fixtures.py` | 14p/3s/0f → 14p/4s/0f（+1 placeholder skip） | ✅ R-1..R-10 无触发 |
| T0: postgres MCP smoke test | TEST | `mcp__postgres__query` 验证 | DB 连接 OK；2/5 表（R-6 drift 已记录） | ✅ R-1..R-10 无触发 |
| T+1: Win 一致性审计 + docs 补缺 | DOC | `docs/dev-workflow.md` §2.4 +blockquote | 纯文档；快速验证 | ✅ R-1..R-10 无触发 |

**最终 pytest 基线**：14 passed / 4 skipped / 0 failed（含 T3 的 placeholder skip）

**复合链 deliverable 完整性**：
- ✅ 1、2、3、0 按顺序执行
- ✅ 文档/脚本对应更新（docs/dev-workflow.md §2.4；既有 `tools/*.sh` 经核对 Win 兼容）
- ✅ Win 端确认正常执行（T1+T2+T3 代码跨平台；既有 Win 部署假设未被破坏）

**未办事项**（独立后续任务，不在本链）：
- R-6 ORM drift：3 张表缺 ORM class（`player_stats_cache` / `player_situational_stats` / `replay_corrections`）
- `cards.py:109-115` 红黑不分（等 fixture 库填充）
- `_split_card_regions` 边界反转（等 fixture 库填充）
- `latest_pot_bb` 新 hand 开始时清零（小改）
- Win 端 PG 部署 REQ 讨论（用户首次需要时触发）
- DB 密码轮换：`Lc%40135681` 已在 git 历史中暴露，用户外部执行 `ALTER USER poker_user WITH PASSWORD '<new>';`
