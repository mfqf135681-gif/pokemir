# T27 净胜负 view(v_player_net_winnings,基于 rebuy 识别)

- **完成时间**:2026-05-28 07:15 UTC(北京 15:15)
- **关联需求讨论**:`主题-数据质量` 隐含相关(净胜负 = 画像基础)
- **关联前次 change-log**:`2026-05-28_07-00-46_T26_db_backup_automation.md`(地基组连续推进)
- **触发红线**:无(view 不动 schema / 不动 ORM)
- **无关红线已检查**:R-1, R-2, R-3, R-4, R-5, R-6, R-7, R-8, R-9, R-10
- **关联 commit**:待 push

## 1. 任务概述

- **用户原始需求**(2026-05-28):"找出谁在赢钱"。AI 之前手工 query 算过几个玩家的筹码 trend(常山 -250 / 钢铁侠 +424),需要 view 自动化。
- **涉及模块**:`contracts/views.sql`(加 1 个 view)
- **相邻任务**:跟 T29 跨 session 玩家合并、#LR4 rake 反推 互补完善

## 2. 假设清单

1. **假设**:rebuy 检测 ≥ 50 筹码突跳 + (整数 % 50 OR 标准 buy-in 模板)即可
   - **现实**:5 个 rebuy 被识别(150-500 范围),但 total_net 偏差 +8539 / 147 hands ≈ +58/手,说明**系统性 noise** 仍存
2. **假设**:player_name 跨 session 稳定
   - **现实**:OCR 漂移把同一玩家拆成两个(豺狼I / 豺狼I1,小雨滴答落 / 小雨滴笞落)→ LAG 算不准 → T29 列入待办

## 3. 文件变更清单

| 文件路径 | 变更类型 | 变更说明 |
|:---|:---|:---|
| `contracts/views.sql` | 修改(加 view ~50 行) | 新增 `v_player_net_winnings`:WITH player_last_event + sequenced + deltas + 聚合 |

### 附带修复

无。

## 4. 契约一致性检查

- 是否涉及 `contracts/` 变更:**是**(加 view,**不动 table schema**)
- **R-5 / R-6 不触发**:view 是只读派生,不改 ORM model
- view 已应用到 VPS PG(`psql -f contracts/views.sql`)

## 5. 红线合规动作

无触发。

## 6. 测试结果

- **验证路径**:**完整验证**(应用 view + sanity SQL)
- 应用 view:`docker compose exec postgres psql -f contracts/views.sql` ✅ CREATE VIEW
- sanity:
  - top winner 不河沙不保 +1367 ✅(跟之前手工分析一致)
  - top loser 豺狼I1 -524 ✅
  - rebuy 5 个识别(钢铁侠 / 常山 等之前手工看到的 rebuy 多数命中)
  - **but**:total_net 偏差 +8539 ≠ 0(应零和)→ 已在 view comment 中明确标注"近似算法,trend only"

## 7. 手动操作提醒

```
⚠️ 手动操作:
1. dashboard 用 v_player_net_winnings 显示净胜负 → 标注"近似 trend"
2. 不能用作精确净胜负 / 商业财务级数字
3. 真精确等 #LR4 rake 反推 + T29 跨 session 合并完成
```

## 8. 潜在影响范围

- **下游**:dashboard / 画像分析 / 整桌赢家亏家排序 → 立即可用 trend
- **数据库**:view 是 read-only 计算,**性能开销低**(147 hands 数据集即时算)
- **未来扩展**:rake 反推(#LR4)+ 跨 session 合并(T29)后,精度跳级

## 9. 违规标注

无违规。Router 4 步全走:
- ✅ Mode 声明(REQ → DEV + 用户明示 "开发模式 A")
- ✅ 红线核验(无触发,逐条 ID 列)
- ✅ 末尾自检 + change-log

---

## 任务完成自检 checklist

- ✅ `change-logs/2026-05-28_07-15-00_T27_net_winnings_view.md` 已保存
- ✅ 测试套件:view 应用 + sanity SQL 跑通
- ✅ 触发红线 ID 显式标注:无触发
- ✅ §11 模式漂移遵守(用户单一"A"延续上 turn DEV 授权)
- ✅ **诚实标注 view 局限**(§1.6 校准 — 不 overclaim 精确)
