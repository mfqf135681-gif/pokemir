# Fixture 库骨架建立：tests/fixtures/cards/ + 装载器 + README

- **完成时间**：2026-05-18 05:15
- **关联需求讨论**：`requirement-discussions/2026-05-17_20-30-00_开发工作流与MCP推荐.md`（confirmed，阶段 6 决策 2 = A 现在建 fixture 库）
- **关联前次 change-log**：`change-logs/2026-05-18_05-00-00_P0批修monotone比较和process_pot死代码.md`（同 turn 复合任务链 T2）
- **触发红线**：无
- **无关红线已检查**：R-1, R-2, R-3, R-4, R-5, R-6, R-7, R-8, R-9, R-10

## 1. 任务概述

- **用户原始需求**：复合任务链 T3——建 fixture 库骨架（让识别 bug 修复有量化对照）
- **目标范围**：仅骨架——目录 + 装载器 + README + placeholder skip；真实 fixture 数据由 Win 端后续录制补充
- **涉及功能模块**：`tests/`（新增 fixtures 子目录 + 装载器）；不动业务代码

## 2. 假设清单

| # | 假设 | 出处 |
|---|---|---|
| 1 | 骨架范围 = 目录 + .gitkeep + README + 装载器；不含真实 fixture | rules-dev §3 "只实现当前任务"；用户说"骨架"明确边界 |
| 2 | 文件格式 = `<id>.png` + `<id>.json` 同目录对，JSON 含 expected.rank/suit + 可选元数据 | discussion 阶段 6 阶段提到"20-30 张 PNG + 预期识别结果 JSON"；本次形式化 |
| 3 | 装载器用 `pytest.mark.parametrize` 自动扫描 fixtures/*.json | 让每个 fixture 是独立 test，pytest 输出能直接看到每张图 pass/fail/skip |
| 4 | 空 fixture 集时：parametrize 用 `[None]` placeholder + skip with 引导 README 的 reason | 干净输出（避免 pytest 默认 NOTSET 名字让人困惑）+ 提示用户去录 |
| 5 | 当前基线会从 14p/3s/0f → **14p/4s/0f**（多一个 placeholder skip） | parametrize 默认行为 |
| 6 | 后续录 fixture 时无需改装载器——丢 PNG + JSON 进目录即生效 | 装载器扫描设计 |

## 3. 文件变更清单

| 文件路径 | 变更类型 | 变更说明 |
|---|---|---|
| `tests/fixtures/cards/_README.md` | 新增 | 用途说明 + 文件格式（JSON schema）+ 命名建议 + Win 端录制流程 + 隐私注意（昵称遮挡）+ 运行命令 + 与 R-8 联动展望 + 当前状态 |
| `tests/fixtures/cards/.gitkeep` | 新增（空文件）| 让空目录可入 git tracking |
| `tests/test_recognition_fixtures.py` | 新增 | parametrized fixture 装载器：扫 fixtures/*.json → 加载图 → 跑 CardRecognizer → 断言 rank/suit；空 fixture 集时单一 placeholder skip + 引导 README |

### 附带修复（5 分钟规则）

无。

## 4. 契约一致性检查

- 是否涉及 `contracts/` 变更：**否**
- 仅 tests 目录新增 + docs README，不涉 API / 数据模型 / ORM

## 5. 红线合规动作

无触发。

> **待激活红线 R-X**（discussion §8 标注）：当 fixture 数量 ≥ 30 张稳定基线 + 用户跑 baseline 测得当前识别率后，可激活"识别精度回归保护"红线（限识别模块改动不得让整体识别率下降 > 2%）。当前 0 fixture，尚未到激活时机。详见 `tests/fixtures/cards/_README.md` "与红线 R-8 联动" 段。

## 6. 测试结果

- **验证路径**：完整验证（多文件新增 + 涉及 pytest collect 行为）

- **执行**：`.venv/bin/pytest tests/ -v`
  ```
  18 tests collected
  14 passed
   4 skipped
   0 failed
  耗时 24.97s
  ```

- **新增 1 个 skip**（符合预期）：
  ```
  tests/test_recognition_fixtures.py::test_card_fixture[skeleton_no_fixtures_yet] SKIPPED
  skip reason: no fixtures recorded yet — see tests/fixtures/cards/_README.md ...
  ```

- **基线变化**：14p/3s/0f → **14p/4s/0f**（新增 1 skip 是骨架 placeholder，不是 fail）

- **rules-dev §5.2 判定**：✅ 通过（skip ≠ fail）

## 7. 手动操作提醒

⚠️ **下一步用户在 Win 测试机执行**（fixture 库的真正价值在此）：

1. 装好 Docker / 测试机 PG（已完成）
2. 在 poker 客户端打开真实牌桌
3. 用 `python tools/roi_config.py --verify --name <profile>` 找出 hero/community 卡牌 ROI
4. 简单脚本按 ROI 裁出单卡 PNG（或用现有 mss 抓帧 + cv2 crop）
5. 标注 `<id>.json`（`{"expected":{"rank":"A","suit":"h"},...}`）
6. 录 20-30 张覆盖各 rank × suit
7. ⚠️ **隐私**：截图前移除/遮挡昵称、聊天框等可识别信息——只裁卡牌区域
8. git commit + push fixture（Linux 端下次 rsync 后也享）

录 ≥ 30 张稳定基线后跑一次 baseline：

```bash
.venv/bin/pytest tests/test_recognition_fixtures.py -v
# 输出 "X passed, Y failed, Z skipped" → 识别率 = X / (X+Y+Z)
```

baseline 数字稳定后开 REQ 讨论激活"识别精度回归保护"红线。

## 8. 潜在影响范围

- **正向**：
  - 后续识别相关改动（红黑不分、`_split_card_regions` 边界反转修复等）有量化对照
  - pytest 输出每个 fixture 独立 pass/skip/fail——精确定位哪类卡牌识别还有问题
  - 装载器幂等：用户丢 PNG/JSON 即生效，无需改代码
- **基线影响**：
  - 14p/3s/0f → 14p/4s/0f（新增 placeholder skip）；后续 change-log 的 baseline 期望需更新
- **关联待办**：
  - **Win 端**录 fixture（用户手动操作）
  - 录够 ≥ 30 张后 REQ 讨论激活识别精度回归红线（当前 10/10 红线已满，需先退役一条或扩容讨论）
  - 修 `cards.py:109-115` 红黑不分 / `_split_card_regions` 边界反转——**等 fixture 库填充后再做**（这样才能量化"修了多少"）

## 9. 违规标注

无。
