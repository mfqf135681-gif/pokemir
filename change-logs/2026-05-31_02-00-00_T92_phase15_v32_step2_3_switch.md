# T92:Phase 1.5 v3.2 Step 2.3 — Switch 读 ATTENTION_MODE-gated

- **完成时间**:2026-05-31 02:00 UTC(北京 10:00)
- **关联需求**:`requirement-discussions/2026-05-30_phase-1-5-attention-mechanism-design.md` §11.4 Step 2 sub-step 2.3 — Step 2 完成
- **触发红线**:无

## 1. 任务概述

Step 2 完成 — sub-step 2.3 **Switch 读**:

- `StateTracker.is_skippable_seat(sidx)` helper — ATTENTION_MODE-gated
- 7 个 skip site(orchestrator)统一调用 helper:
  1. line 610 — POST event 注入跳 empty 座
  2. line 1145 — showdown CNN 跳 fold seat
  3. line 1581(原 1582+1584 合并)— shadow_pointer 跳 fold+empty
  4. line 1664 — pre_batch action OCR 跳 fold+empty
  5. line 1720 — pre_batch action OCR (T46-A guard)跳 fold+empty
  6. line 1805 — idle_avatar_hash 仅记非 skippable seat
- line 1487(dup phash 写 protection)**保留** — 不是 skip 判断

**Step 2 全完成** — orchestrator 旧 sets 用于 skip 的所有读点已 gated。

## 2. 假设清单

1. `ATTENTION_MODE=0`(默认):helper 走旧 `_folded_seats ∪ _empty_seats` 判断
2. `ATTENTION_MODE=1`:helper 走 `seat_lifecycle.is_skippable()`
3. mirror 写已保证两模式答案一致(经 T91 Step 2.2 sub-step)
4. line 1487(`sidx not in _empty_seats` 写 protection)是写侧自身保护,不切

## 3. 文件变更清单

| 文件 | 变更 | 行数 |
|:---|:---|:---:|
| `pipeline/detector.py` | `is_skippable_seat` helper(11 行) | +11 |
| `pipeline/orchestrator.py` | 7 skip site → helper(net 净减 2 行因合并 shadow_pointer 2 检查) | +7/-9 |
| `tests/test_detector_state_machine_wire.py` | 3 双模 consistency tests | +60 |
| `change-logs/.../T92_*.md` | 本文件 | - |

**总计 +78 / -9 = +69 净行**

## 4. 契约一致性检查

- ✅ 不动 schema / DB
- ✅ `ATTENTION_MODE=0` 默认 → skip 决策跟改前完全等价
- ✅ `ATTENTION_MODE=1` → skip 决策走 seat_lifecycle(mirror 保证一致)
- ✅ 旧 sets 写入 + 读 都保留(Step 8 才删)
- ✅ 44/44 unit tests pass(41 旧 + 3 新双模 consistency)
- ✅ orchestrator module import 验证 OK

## 5. 红线合规动作

无触发。Router 4 步:
- ✅ Mode 声明(用户明示 "A 继续" → DEV,默认推荐 Step 2.3)
- ✅ Step 2 完成 = 完整 sub-step 闭环
- ✅ 红线核验(R-1/R-7/R-3 全 NO)
- ✅ 自检:双模 consistency tests 覆盖 ATTENTION_MODE=0/1 共识检查

## 6. 测试结果

```
$ python -m pytest tests/test_detector_state_machine_wire.py tests/test_state_machines.py tests/test_io_queues.py
============================== 44 passed in 3.10s ==============================

$ python -c "import pipeline.orchestrator; print('orchestrator module OK')"
orchestrator module OK
```

新增 3 个双模 consistency tests:
- `test_legacy_mode_reads_from_sets`:ATTENTION_MODE=0 读旧 sets,不看 seat_lifecycle
- `test_attention_mode_reads_from_state_machine`:ATTENTION_MODE=1 读 seat_lifecycle,不看旧 sets
- `test_both_modes_consistent_after_full_mirror`:mirror 写后两模式答案一致

## 7. 手动操作提醒(Win 端)

**ATTENTION_MODE=0 默认** → Win 端 pipeline 行为 100% 跟改前等价。

下次 Win 端可测 ATTENTION_MODE=1 切换:
```powershell
git pull
.venv\Scripts\activate
$env:POKEMIR_ATTENTION_MODE = "1"
python main.py pipeline --profile party_poker_9 --observer
# 录 5-10 min,看 silent fold rate / tick / 跟 ATTENTION_MODE=0 baseline 对比
```

若新模式有 bug:
```powershell
$env:POKEMIR_ATTENTION_MODE = "0"  # 1 行回滚
```

## 8. 潜在影响范围

- ✅ pipeline 运行时(ATTENTION_MODE=0)**行为 0 变化**
- 🟡 ATTENTION_MODE=1:skip 决策走 seat_lifecycle,**首次 Win 端实测验证 mirror+state machine 真等价**
- 🟡 后续 Steps:
  - Step 3 双 OCR + Pattern D 协作
  - Step 4 Multi-pot OCR
  - Step 5 13 规则盲点
  - Step 5.5 §12 摊牌专项
  - Step 6 Tier 1 提升
  - Step 8 删旧 `_folded_seats` / `_empty_seats` / `_went_all_in_this_hand` / `_pointer_state` sets + 4 陷阱 grep 检测
  - Step 9 ground truth verify

## 9. 违规标注

无违规。

---

## 任务完成自检 checklist

- ✅ change-log 已写
- ✅ Linux 44/44 own tests pass
- ✅ orchestrator module import 验证
- ✅ §1.6 校准:Step 2 完成(2.1 wire + 2.2 mirror + 2.3 switch)— 进入 Step 3 候选
- ✅ §11 模式守卫(用户明示 "A 继续" → DEV)
- ✅ §11.3 加法陷阱自检:**transitional dual presence 是 Step 8 删旧前的设计允许**;is_skippable_seat helper 在 Step 8 后简化为 seat_lifecycle.is_skippable 单一 path
