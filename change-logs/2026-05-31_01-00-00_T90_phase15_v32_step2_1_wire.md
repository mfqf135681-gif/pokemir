# T90:Phase 1.5 v3.2 Step 2.1 — Wire T80 状态机进 StateTracker

- **完成时间**:2026-05-31 01:00 UTC(北京 09:00)
- **关联需求**:`requirement-discussions/2026-05-30_phase-1-5-attention-mechanism-design.md` §11.4 Step 2(细分 sub-step 2.1)
- **触发红线**:无

## 1. 任务概述

Step 2 of 9 步执行序细分 sub-step 2.1 — **Wire phase**:

- 在 `pipeline/detector.StateTracker` 嵌入 T80 状态机字段
- 仅初始化 + start_new_hand reset,**不读不写其他业务逻辑**
- 旧 `_folded_seats` / `_empty_seats` 仍 authoritative
- ATTENTION_MODE=0(默认)→ 旧 path 100% 不变

后续 sub-step:
- 2.2:Mirror 写(每个 _folded_seats.add() 同步 seat_lifecycle.transition_to)
- 2.3:Switch 读(ATTENTION_MODE=1 时从 seat_lifecycle 读)

## 2. 假设清单

1. T80 SeatStateMachine 9-seat 默认 sync `party_poker_9` profile
2. seat_lifecycle.reset_for_new_hand() 语义跟 _folded_seats.clear() 等价(FOLDED → ACTIVE,EMPTY 不动)
3. HandPhaseMachine 每 hand 用 fresh 实例(BETWEEN_HANDS 起步),不复用

## 3. 文件变更清单

| 文件 | 变更 | 行数 |
|:---|:---|:---:|
| `pipeline/detector.py` | import T80 + 2 个 init 字段 + start_new_hand reset 2 行 | +13/-0 |
| `tests/test_detector_state_machine_wire.py` | 新建 — 4 integration tests | +66 |
| `change-logs/.../T90_*.md` | 本文件 | - |

**总计 +79 行**(纯新增,**0 改动**现有行为)

## 4. 契约一致性检查

- ✅ 不动 schema / orchestrator / DB / DB schema / pipeline 运行时
- ✅ 旧 `_folded_seats` / `_empty_seats` / `_pointer_state` 完整保留
- ✅ ATTENTION_MODE 字段不被读取(本 sub-step 不切 path)
- ✅ T80 状态机模块行为 100% 跟单测一致
- ✅ 35/35 unit tests pass(31 旧 + 4 新)

## 5. 红线合规动作

无触发。Router 4 步:
- ✅ Mode 声明(用户明示 "开发模式 推进 Step 2" → DEV)
- ✅ Step 2.1 = 最小可 ship wire phase
- ✅ 红线核验(R-1/R-7/R-3 全 NO)
- ✅ 自检:per [[dev-rule-validate-blind-spots]] (a),写 integration test 覆盖 Linux 可验范围;Win 端 runtime smoke 在下次 session

## 6. 测试结果

```
$ python -c "
from pipeline.detector import StateTracker
from pipeline.state import SeatLifecycle, HandPhase
t = StateTracker()
print('seat_lifecycle:', type(t.seat_lifecycle).__name__)  # SeatStateMachine
print('hand_phase:', type(t.hand_phase).__name__)         # HandPhaseMachine
print('initial seat_0:', t.seat_lifecycle.get(0))          # empty
print('initial phase:', t.hand_phase.current)              # between_hands
t.start_new_hand('test')
print('after reset seat_0:', t.seat_lifecycle.get(0))      # empty
print('after reset phase:', t.hand_phase.current)          # between_hands
"
✅ all pass

$ python -m pytest tests/test_detector_state_machine_wire.py tests/test_state_machines.py tests/test_io_queues.py -v
============================== 35 passed in 3.21s ==============================
```

## 7. 手动操作提醒(Win 端)

**本次无 Win 端必做** — ATTENTION_MODE=0 默认,旧 pipeline 行为 100% 不变。

下次 Win 端 sync:
```powershell
cd D:\project\pokemir
git pull
.venv\Scripts\activate
python -m pytest tests/ -v   # 验证 Win 端 35 测试也过(EasyOCR import 不阻塞)
```

## 8. 潜在影响范围

- ✅ pipeline 运行时 **0 变化**
- ✅ StateTracker 多 2 个字段(`seat_lifecycle`, `hand_phase`)
- 🟡 后续 sub-step:
  - Step 2.2 Mirror:在 `_folded_seats.add()` / `_empty_seats.add()` 点同步 seat_lifecycle.transition_to(FOLDED / SITTING_OUT)
  - Step 2.3 Switch:把 `if sidx in self._folded_seats` 改为 `if ATTENTION_MODE: tracker.seat_lifecycle.is_skippable(sidx) else: sidx in tracker._folded_seats`
  - Step 8(Phase 1.5 v3.2 后期):删旧 `_folded_seats` / `_empty_seats` / `_pointer_state`

## 9. 违规标注

无违规。

---

## 任务完成自检 checklist

- ✅ change-log 已写
- ✅ Linux 4 integration tests + 31 旧 tests = 35/35 pass
- ✅ §1.6 校准:本次 sub-step 2.1 wire only,Step 2.2/2.3 是后续 session
- ✅ §11 模式守卫(用户明示 "开发模式" → DEV)
- ✅ §11.3 加法陷阱自检:**transitional dual presence 是 Step 2-7 设计允许**(Step 8 删旧),不算双轨;新字段仅 standby
