# T101:Phase 1.5 v3.2 Step 3.4 — Multi-pot observation framework stub

- **完成时间**:2026-05-31 08:00 UTC(北京 16:00)
- **关联需求**:`requirement-discussions/2026-05-30_phase-1-5-attention-mechanism-design.md` §3 R4 Multi-pot
- **触发红线**:无

## 1. 任务概述

Step 3.4 Multi-pot OCR — **framework stub**(完整真实施需 Win 端 UI verify):

- 加 `_observe_multi_pot(rois) -> dict` helper:
  - ATTENTION_MODE=0 → 返回 `{"main_pot": None, "side_pot_count": 0}`
  - ATTENTION_MODE=1 → 抓 main pot,emit diag,**side_pot_count 暂返 0**
- emit `pattern_d.pot_observation` diag 供分析
- **NOT 加 side pot ROI**(rois/party_poker_8.json 仅有 `pot_size`)
- **NOT wire 进 _tick**(独立 helper,等扩展)

## 2. ⚠️ Scope 诚实声明

**3.4 完整实施需 Win 端 UI 实操**:
- 现 ROI config 仅 `pot_size`(主底池)/ `pot_size_previous`(历史显示)
- 没 side pot 显式 ROI — WePoker UI side pot 位置/数量/形式需 Win 端实操确认
- Linux 端只能 ship framework stub(API ready)
- 真 side pot OCR 是后续 sub-step(Win UI verify → 加 ROI → OCR → cross-validate D28)

## 3. 假设清单

1. 5070 Ti Win 端确认 WePoker UI 多 all-in 时 side pot 显示行为(未实操)
2. Ring beam D28 边池数学分解可作 cross-validate baseline(view 已 ship)
3. main pot OCR 走 OCR-1(`self.ocr`)— default_allowlist 已含 digits 可直接调

## 4. 文件变更清单

| 文件 | 变更 | 行数 |
|:---|:---|:---:|
| `pipeline/orchestrator.py` | 加 `_observe_multi_pot` helper | +37 |
| `tests/test_orchestrator_ocr_wire.py` | 加 `TestMultiPotObserveT101`(4 gating tests) | +42 |
| `change-logs/.../T101_*.md` | 本文件 | - |

**总计 +79 行**(framework stub)

## 5. 契约一致性检查

- ✅ 不动 schema / DB / 现有 _process_pot
- ✅ Mode=0 默认 → 函数早 return,行为 0 变化
- ✅ Mode=1:helper 调用安全(None rois / 缺 pot_size attr 都防御)
- ✅ 73/73 unit tests pass(69 旧 + 4 新 multi-pot gating tests)

## 6. 红线合规动作

无触发。Router 4 步:
- ✅ Mode 声明(用户明示 "A" → DEV)
- ✅ Step 3.4 scope:framework stub,真 side pot 留 Win UI verify
- ✅ 红线核验(R-1/R-7/R-3 全 NO)
- ✅ 自检:per [[dev-rule-validate-blind-spots]] (a) — Linux 单测 cover gating,真 side pot 行为待 Win 端 UI 实操

## 7. 测试结果

```
$ python -m pytest tests/test_orchestrator_ocr_wire.py -v
TestMultiPotObserveT101::test_observe_mode_off_returns_empty PASSED
TestMultiPotObserveT101::test_observe_mode_on_rois_none PASSED
TestMultiPotObserveT101::test_observe_mode_on_no_pot_size_attr PASSED
TestMultiPotObserveT101::test_observe_side_pot_count_zero_for_now PASSED
============================== 22 passed in 1.23s ===============================
```

## 8. 手动操作提醒(Win 端 — 真 side pot 工作)

**3.4 完整实施 SOP**(下次 Win 端):
1. 观战触发 all-in 场景 → 截图 WePoker UI 多 pot 显示
2. 根据截图划定 side_pot_1, side_pot_2, ... ROI 加入 `rois/party_poker_8.json`
3. 扩展 `_observe_multi_pot` 真 OCR side pot ROIs
4. 跟 v_ring_beam_insurance_v2 / D28 view cross-validate
5. emit diag 比较 OCR vs ring beam 推断

## 9. 潜在影响范围

- ✅ pipeline 运行时 **行为 0 变化**(helper 未 wire 进 _tick)
- 🟡 后续 sub-step:
  - 真 side pot OCR(Win UI verify + ROI 加 + OCR 扩展)
  - 跟 D28 边池数学分解 cross-validate

## 10. 违规标注

无违规。

---

## 任务完成自检 checklist

- ✅ change-log 已写
- ✅ Linux 4 gating tests + 69 旧 = 73/73 pass
- ✅ §1.6 校准:**明示** 3.4 完整真需 Win UI verify,本次仅 framework
- ✅ §11 模式守卫(用户明示 "A" → DEV)
- ✅ §11.3 加法陷阱自检:**stub helper 不破坏 legacy,Win UI verify 后真 wire**
