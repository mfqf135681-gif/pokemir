# 清空 fixture 库全部数据（PNG + JSON + 诊断快照），准备重录

- **完成时间**：2026-05-24 06:22
- **关联需求讨论**：用户对话明示决定（REQ 快速路径,无独立 doc）："为了确保准确性,我准备全部重新录制和标注"
- **关联前次 change-log**：`change-logs/2026-05-24_06-02-00_record_card支持公共牌录制.md`（脚本升级,支持公共牌录制——清空后重录将主要依赖该升级）
- **触发红线**：无
- **无关红线已检查**：R-1, R-2, R-3, R-4, R-5, R-6, R-7, R-8, R-9, R-10

## 1. 任务概述

- **用户决定**：清空 31 张 hero fixture（混合亮/暗态）+ 31 张诊断快照,准备用升级后的公共牌录制流程**从零录** ~50 张全新数据
- **理由**：旧 fixture 集存在样本数偏少且分布不均（多数 club/heart,缺 J/5 全花色,Ad 只 1 张）—— 与其在旧集合上加增量,不如**重录一套质量更高 + 覆盖均衡的数据**
- **scope**：仅删 fixture 集物理数据;**保留** `_README.md` + `.gitkeep` + 空 `_diagnosis/` 子目录（装载器约定）;不动训练管道 / 推理代码

## 2. 假设清单

| # | 假设 | 出处 |
|---|---|---|
| 1 | 删除已 git tracked 的 fixture 不会破坏装载器 | `tests/test_recognition_fixtures.py:27` 装载器 glob `*.json`,空集合时 fall back 到 placeholder skip ✓ 已验证 |
| 2 | `_README.md` + `.gitkeep` 保留即可让装载器 + git 都正确处理空集合 | placeholder skip 测试已确认 |
| 3 | `_diagnosis/` 空目录保留无害（下次 `diagnose_recognition.py` 跑时自动用） | `OUT.mkdir(exist_ok=True)` |
| 4 | Linux 端无 `models/card_cnn.pth`（已确认 ls 不在）→ 无需额外清模型 | 之前 smoke 测后已删 |
| 5 | Win 端有训练好的 `models\card_cnn.pth`（gitignored）→ 用户应手动删,**新数据重训前模型仍能跑（但准确率反映的是旧分布**） | 用户在 §7 手动操作里清 |
| 6 | 删除是可逆的——所有 fixture 都在 git history 里可恢复 | git log 多次 commit 含完整 fixture set;最坏情况 `git restore --source=<hash>` 可救 |

## 3. 文件变更清单

| 文件路径 | 变更类型 | 变更说明 |
|---|---|---|
| `tests/fixtures/cards/*.png` | **删除 31 文件** | 全部 fixture PNG |
| `tests/fixtures/cards/*.json` | **删除 31 文件** | 全部 fixture 元数据 |
| `tests/fixtures/cards/_diagnosis/*.png` | **删除 31 文件** | 上轮 Stage C 诊断快照——基于旧模型 + 旧数据,失效;下次 `diagnose_recognition.py` 跑时自动重生成 |
| `tests/fixtures/cards/_README.md` | 保留 | 格式文档,不变 |
| `tests/fixtures/cards/.gitkeep` | 保留 | git 空目录占位 |

### 附带修复（5 分钟规则）

无。

## 4. 契约一致性检查

- 是否涉及 `contracts/` 变更：**否**
- 装载器行为：fixture 空集合 → placeholder skip,与初始骨架行为相同

## 5. 红线合规动作

无触发：
- R-3：删自己的本地数据,不外传
- R-7 / R-8：ROI 配置 / 识别模型加载不动

## 6. 测试结果

- **验证路径**：完整验证（多文件删除 + 装载器行为变化检测）

- **清前**：fixture 集 31 png / 31 json / 31 _diagnosis png
- **清后**：fixture 集 0 png / 0 json / 0 _diagnosis png；保留 _README.md + .gitkeep + _diagnosis/ 空目录
- **装载器降级验证**：`pytest tests/test_recognition_fixtures.py -v` → `1 skipped` (placeholder `skeleton_no_fixtures_yet`) ✓
- **全套 pytest**：`pytest tests/ -q` → **14 passed / 4 skipped / 0 failed** in 29.70s
- **§5.2 治理状态**：之前 13 fail（Stage 1 至 Stage C 累计 §5.2 持续违例承袭）→ **本次清空后自动归零**（fail 来自不存在的 fixture 参数化测试）→ **§5.2 现回到 clean 状态**

## 7. 手动操作提醒

⚠️ **Win 用户在 pull 之前**：

```powershell
# 1. 拉本次清空 commit (git 自动删除你 Win 端对应的 fixture 文件)
cd D:\project\pokemir
git pull
```

`git pull` 会**自动从你 Win 端 working tree 删除**所有 tracked fixture PNG + JSON + 诊断快照（因为本 commit 在 git 中删除了它们）—— 你无需手动 `Remove-Item`。

```powershell
# 2. 删除本地 gitignored 的旧模型（让下次跑训练用新数据从零训）
Remove-Item models\card_cnn.pth -ErrorAction SilentlyContinue
```

> 这一步**不强求**——保留旧模型也能跑(只是预测基于旧数据分布,准确率反映旧分布)；删了下次重训时 `train_card_cnn.py` 会保存新版本。

```powershell
# 3. 验证清空成功
Get-ChildItem tests\fixtures\cards\*.png 2>$null
```
应该没有任何输出（空目录,仅有 `_README.md` + `.gitkeep`）

之后按上一轮 `change-logs/2026-05-24_06-02-00_record_card支持公共牌录制.md` §7 指引,重跑 ROI 配置（确保有 5 个 community ROI）+ 录新 fixture（推荐每手只录 flop 3 张稳定路径）。

## 8. 潜在影响范围

- **正向**：
  - 旧不均衡数据清掉,新一轮录制可以**有意识地覆盖均衡**（每个 rank-suit 至少 1-2 张,稀有类如 5 / J 多录）
  - **§5.2 治理状态归零**——继续推进 path A 不再扛着 13 fail 包袱
  - 诊断快照失效,下次 `diagnose_recognition.py` 跑会基于新模型 + 新数据生成有意义的快照
- **行为变化**：
  - `pytest tests/test_recognition_fixtures.py` 暂时只产 1 placeholder skip;新 fixture 提交后自动参数化恢复
  - **Win 端 git pull 时会自动删除旧 fixture 文件**——用户工作树视觉变化大,需提前知晓（已在 §7 提醒）
- **关联待办**：
  - **用户重录 ~50 张 fixture**（每手 flop 3 张策略）
  - 用户 commit + push 新数据
  - 我重训 + 报真实 val_both_acc

## 9. 违规标注

> ⚠️ **本次违反 §5.3 护栏 3**（"累计改动文件 ≤ 20"）——本 commit **删除 93 文件**。
> 
> **违例理由 + 用户授权**：
> 1. 用户在对话中**明示**清空全部 fixture 集："为了确保准确性,我准备全部重新录制和标注,请你清空你那端的数据"
> 2. 删除文件**全在安全列表内**（`tests/fixtures/cards/*` → `tests/*` 路径）
> 3. 删除是 git 可逆的（任何 commit hash 都能 `git restore` 救回）
> 4. 与之前 commit 716aca6（同样护栏 3 命中,用户当时明示授权 31 PNG 新增）形成对称——本次是它的反操作
> 
> 已在 commit message + §9 显式标注违例认知。
