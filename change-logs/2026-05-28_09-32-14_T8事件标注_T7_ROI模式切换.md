# T8 事件标注 + T7 ROI 模式切换

- **完成时间**：2026-05-28 09:32
- **关联需求讨论**：无；本次为用户当前对话直接明确授权的 DEV 任务（T8 主线 + T7 辅线）
- **关联前次 change-log**：无同主题命中；相邻任务为 dashboard / action_events 数据质量链路
- **触发红线**：R-5（数据模型契约变更）、R-6（ORM 与 SQL 模型同步）
- **无关红线已检查**：R-1, R-2, R-3, R-4, R-7, R-8, R-9, R-10

## 1. 任务概述

- **用户原始需求**：优先 T8，新增事件滚动 + 纠偏页面；T8 跑通后做 T7，在设置页提供 4 个 ROI 模式按钮。
- **用户可见结果**：Dashboard 侧边栏新增「事件标注」，能看到最近 50 条 `action_events`，每条可选择 6 种动作之一保存纠偏；设置页新增 Pipeline tab，可把当前模式写成 8/9 人、坐下/观战。
- **数据边界**：纠偏不污染 `action_events.raw_data`；dashboard 不对 `action_events` 执行 UPDATE / DELETE。

## 2. 假设清单

| # | 假设 | 出处 |
|---|---|---|
| 1 | T8 轮询读取 `action_events` 最近 50 条，按 `COALESCE(timestamp, created_at)` 倒序展示 | 用户当前任务明确要求“每 5 秒 polling VPS PG 的 action_events 最近 50 条” |
| 2 | 纠偏动作只允许 fold/check/call/bet/raise/all_in 六项 | 用户当前任务明确列出 6 选项 |
| 3 | 位置保持英文，不做 SB/BB/UTG/HJ/CO/BTN/UTG+1 翻译 | 用户当前任务明确要求 |
| 4 | T7 的 4 个模式使用现有 ROI profile：`party_poker_8` / `party_poker_9`，观战状态写入 `observer_mode` | 用户给定 `pipeline_settings` schema 含 `active_profile` + `observer_mode`；仓库已有 `rois/party_poker_8.json` / `rois/party_poker_9.json` |
| 5 | Pipeline 端暂不消费 `pipeline_settings` | 用户当前任务明确要求“pipeline 端轮询逻辑暂不实现” |

## 3. 文件变更清单

| 文件路径 | 变更类型 | 变更说明 |
|---|---|---|
| `dashboard/pages/labeling.py` | 新增 | T8 页面：5 秒自动刷新、最近 50 条事件中文展示、每条 expander 纠偏、写入 `event_corrections` |
| `dashboard.py` | 修改 | sidebar 页面入口新增「事件标注」 |
| `dashboard/__init__.py` | 修改 | dashboard 页面说明补充 labeling 模块 |
| `dashboard/pages/__init__.py` | 修改 | 导出 `labeling` 页面模块 |
| `dashboard/pages/settings.py` | 修改 | 新增 Pipeline tab；4 个按钮写 `pipeline_settings` singleton id=1；显示当前 active_profile |
| `dashboard/db.py` | 修改 | 更新说明：dashboard 现在只写纠偏/设置辅助表，不改 action_events 原始数据 |
| `contracts/models.sql` | 修改 | 末尾新增 `event_corrections` / `pipeline_settings` schema 与索引 |
| `storage/models.py` | 修改 | 同步新增 `EventCorrectionModel` / `PipelineSettingModel`，并把纠偏关系挂在 `ActionEventModel` |

### 附带修复（5 分钟规则）

无。

## 4. 契约一致性检查

- **R-5 触发**：修改 `contracts/models.sql`，新增两张表和一个索引。
- **R-5 合规依据**：用户当前任务直接给出完整 schema，并明确要求“加到 contracts/models.sql 末尾，我 review”。
- **R-6 触发**：SQL 模型契约新增表后，同步补充 `storage/models.py` 两个 ORM class。
- **ORM 同步范围**：表名、字段名、主键、外键、NOT NULL、默认值、`idx_event_corrections_event` 均已同步。
- **不做内容**：未改 `contracts/api.yaml`，未新增对外 API，未改 pipeline / orchestrator / recognition。

## 5. 红线合规动作

- **R-5（数据模型契约规则）**：已按用户给定 schema 修改 `contracts/models.sql`；实际应用到 VPS PG 仍需用户后续手动跑 psql。
- **R-6（ORM 同步规则）**：已同步 `storage/models.py`，避免“SQL 里有表、Python ORM 不知道”的漂移。
- **R-3（自控基础设施规则）**：本次 dashboard 仍使用现有 `POKEMIR_DB_DSN_SYNC`，目标为用户自控 VPS PG；未接入第三方 SaaS。
- **R-4（API 契约规则）**：未新增/修改 API 契约或对外接口。
- **R-9（屏幕捕获范围规则）**：未触碰 pipeline/capture 运行链路。

## 6. 测试结果

- **语法验证**：
  ```powershell
  .\.venv\Scripts\python.exe -m py_compile dashboard.py dashboard\__init__.py dashboard\db.py dashboard\pages\__init__.py dashboard\pages\labeling.py dashboard\pages\settings.py storage\models.py
  ```
  结果：通过。

- **项目测试套件**：
  ```powershell
  .\.venv\Scripts\python.exe -m pytest tests -v
  ```
  结果：`229 passed, 5 warnings in 24.37s`。

- **修复迭代记录**：首轮测试暴露 ORM relationship 放在 `HandModel` 上导致 `NoForeignKeysError`；已改为挂在 `ActionEventModel`，同一套测试复跑通过。

- **未验证项**：当前 venv 未安装 Streamlit（`ModuleNotFoundError: No module named 'streamlit'`），因此未在本机打开浏览器实测 dashboard；家用电脑按 `requirements-dashboard.txt` 安装后可用 `streamlit run dashboard.py` 验证。

## 7. 手动操作提醒

⚠️ **VPS PG schema 需手动应用**：

- 本次只修改了 `contracts/models.sql`，没有直接写 VPS PG schema。
- 用户后续应用 schema 后，`event_corrections` 和 `pipeline_settings` 表才会真实存在；在应用前，事件标注保存和 Pipeline 设置按钮会提示表不存在。
- 可按你的部署方式执行等价 psql 命令，例如在 VPS 上对目标库执行 `contracts/models.sql`。

## 8. 潜在影响范围

- **正向**：
  - 用户可以边录数据边在 dashboard 看最近 50 条动作，低置信事件可人工纠偏。
  - 纠偏数据独立存表，后续可做训练/回放修正，不破坏原始 `action_events`。
  - 4 模式 ROI 设置先落库，pipeline 端后续只需消费 `pipeline_settings`。
- **运行限制**：
  - T8 的 5 秒刷新基于 Streamlit 页面重跑；用户正在打开纠偏下拉时，自动刷新可能打断当前选择，可临时关闭页面上的“每 5 秒自动刷新”。
  - T7 只写设置表；Win 桌面机 pipeline 暂不会自动切 profile，这是本次任务明确边界。
- **工作区备注**：提交前发现 `rois/party_poker_8.json` 与 `tests/fixtures/showdown/` 已处于非本任务未提交状态；本次不纳入提交。

## 9. 违规标注

无。