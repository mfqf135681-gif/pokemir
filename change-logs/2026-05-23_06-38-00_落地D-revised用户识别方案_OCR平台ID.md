# 落地 D-revised 用户识别方案：hand-start OCR 平台 ID

- **完成时间**：2026-05-23 06:38
- **关联需求讨论**：`requirement-discussions/2026-05-23_05-12-00_用户识别方案_跨手稳定player_id.md`（confirmed，Q1 = D-revised；Q2 用户实测确认 hand-start ID 不被遮挡）
- **关联前次 change-log**：`change-logs/2026-05-22_21-28-00_ROI_profile_window_title改为WePoker.md`（前置 ROI profile WePoker 化）+ `change-logs/2026-05-19_02-05-00_latest_pot_bb新hand清零.md`（同模式新 hand reset 状态字段的先例）
- **触发红线**：**R-7（ROI 配置结构一致性）**——SeatROI 加 `id_area` 字段 + roi_config.py 增 prompt + JSON schema 加 `id` 字段
- **无关红线已检查**：R-1, R-2, R-3, R-4, R-5, R-6, R-8, R-9, R-10

## 1. 任务概述

- **用户原始需求**：跨手稳定识别同一玩家，让 player_stats_cache 等画像表能正确聚合
- **落地方案**：D-revised（hand-start 窗口 OCR 平台 ID 数字，作为 `player_name`）
- **scope 严格遵循 REQ 阶段 4 scope 边界**：只做 ID 抓取；不抓 nickname；不改 DB schema；不加头像 hash 兜底；严守 [[image-only-compliance-constraint]]（无浏览器 hook / 无插件 / 无抓包）

## 2. 假设清单

| # | 假设 | 出处 |
|---|---|---|
| 1 | hand-start = `StateTracker.check_hero_cards == True` 瞬间 | pipeline/detector.py 既有信号；REQ 阶段 2.7 论证 |
| 2 | hand-start 时 SB/BB 位置 ID 不被 "Posting SB/BB" 遮挡 | 用户 2026-05-23 实测确认（"不会遮挡"）|
| 3 | OCR 数字用现有 easyocr 英文模型即可（无需 ch_sim）| ID 是阿拉伯数字串；easyocr 默认含英文（含数字）|
| 4 | id_area 缺失时优雅退化为 `Player_{seat_idx}` | 向后兼容旧 ROI profile（如 party_poker.json 当前 `seats: []`）|
| 5 | 不动 DB schema（player_name 字段仍 TEXT，存的内容从 "Player_0" 改成 "12345678"）| REQ 阶段 4 scope 边界；避免触发 R-5/R-6 |
| 6 | id_map per hand 而非全局缓存 | 玩家可能离桌 / 换桌 / 换座；每手重新 OCR 抓最准；OCR 成本可忽略（每手 1 次，6 张小图）|

## 3. 文件变更清单

| 文件路径 | 变更类型 | 变更说明 |
|---|---|---|
| `capture/roi.py` | 修改（+3 行；SeatROI / to_dict / from_dict 各 +1）| SeatROI 加 `id_area: ROIRegion \| None = None`；to_dict 写 `"id"`；from_dict 读 `"id"`（向后兼容缺失字段）|
| `tools/roi_config.py` | 修改（+2 行）| 主循环里每座位 prompt 多加 `id_rect = select_roi(...)` + 写进 result["seats"][].id；预览 `_draw_rois` 多画 ID 框 |
| `pipeline/detector.py` | 修改（+3 行）| `StateTracker` 加 `player_id_map: dict[int, str]` 字段 + `start_new_hand` 清零 |
| `pipeline/orchestrator.py` | 修改（+18 行 / -2 行）| `_start_new_hand` 加调 `_capture_player_ids()`；新增 `_capture_player_ids` 方法（遍历 seat_regions，OCR 每个 id_area，存入 tracker.player_id_map）；`hand.seats` 用 id_map 填；`_process_seat_actions` 内 `player_name` 改读 id_map（退化到 `Player_{i}`）|

### 附带修复（5 分钟规则）

无。

## 4. 契约一致性检查

- 是否涉及 `contracts/` 变更：**否**
- ROI JSON schema 加可选字段 `seats[].id`（向后兼容；缺失则 fall back）→ 不触发 R-5
- DB schema：player_name 字段类型 + nullability 不变，只是写入内容从座位号字符串换成 ID 数字字符串 → 不触发 R-5/R-6

## 5. 红线合规动作

**R-7 触发**——按合规动作清单：
- [x] 修改 `capture/roi.py` SeatROI 结构（加 id_area）
- [x] 同步修改 `tools/roi_config.py` 增 prompt + 预览渲染
- [x] change-log §7 标注"⚠️ 用户必须重跑 roi_config.py 重配 ROI（每座位多 1 个 ID 框）"

其余红线无触发。R-1 / [[image-only-compliance-constraint]] 严守：实现仅用 mss 截图 + capture_roi + easyocr——零 DOM / 插件 / 抓包 / 内存读取。

## 6. 测试结果

- **验证路径**：完整验证（4 文件改动 + 跨模块新字段）

- **执行 1**：`.venv/bin/pytest tests/ -q`
  ```
  14 passed, 4 skipped, 0 failed in 32.14s
  ```
  与基线一致 ✅

- **执行 2**：SeatROI 序列化往返 smoke（Python 内联）
  ```
  serialized seat: {'seat_index': 0, ..., 'id': (50, 50, 80, 12)}
  deserialized id_area: ROIRegion(name='seat_id', left=50, top=50, width=80, height=12)
  ```
  ✅ to_dict + from_dict 对称

- **执行 3**：向后兼容验证——旧 profile（`seats: []` 或 seats 中无 "id" 字段）通过 `from_dict` 不报错；新代码遍历 seat_regions 时 id_area=None 触发 fall-back 到 `Player_{i}` ✅（代码逻辑核验）

- **rules-dev §5.2 判定**：✅ 通过

## 7. 手动操作提醒

⚠️ **Win 端用户必须重跑 ROI 配置**（R-7 合规动作）：

1. Chrome 打开 WePoker 任意牌桌（观战即可，发牌瞬间所有座位 ID 可见即够用）
2. PowerShell：
   ```powershell
   .\.venv\Scripts\python.exe tools\roi_config.py --name party_poker
   ```
3. 按顺序框完所有 ROI；**新增的 ID 框出现在每个座位最后一项**（"Seat N — User ID"）
4. 每个 ID 框要**圈在玩家 ID 数字显示的位置**（小心避开昵称区，只圈数字）
5. 没圈的座位会 fall-back 到 `Player_<seat_idx>` 旧逻辑，跨手统计仍失效——所以 6 个座位都要圈
6. 配完 verify：
   ```powershell
   .\.venv\Scripts\python.exe tools\roi_config.py --verify --name party_poker
   ```
   绿框应包住每个座位的 ID 数字

## 8. 潜在影响范围

- **正向**：
  - 跨手统计聚合从"完全失忆"变成"100% 正确"——Phase B 对手画像可以基于 ID 稳定运行
  - 单手内同一玩家多次行动也用相同 player_name（自动正确）
  - 后续 D++ 升级（叠加 nickname for display）只需加 nickname_area + 一段 OCR，零回归风险
- **行为变化**：
  - **未配 id_area 的座位**：player_name 仍是 `Player_{seat_idx}`（旧行为）→ 平滑回退
  - **配了 id_area 但 OCR 返回空**：player_name 仍是 `Player_{seat_idx}`（如该座位空位 / OCR 失败时）
  - **OCR 出非 ID 字符串**（比如 ROI 框偏到昵称）：会原样存为 player_name → 统计仍能聚合但 key 不是数字
- **关联待办**：
  - 用户重跑 roi_config（必做，否则 D-revised 无效）
  - 用户在 Win 实测时验证 hand-start 抓 ID 的实际窗口稳定性（若发现 BB post 仍偶有遮挡，加 0.5s 延迟逻辑）
  - 后续视统计数据决定要不要升 D++（叠 nickname OCR for display）

## 9. 违规标注

无。
