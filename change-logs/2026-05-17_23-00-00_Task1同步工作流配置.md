# Task #1：同步工作流配置（git + rsync 双轨 + Win API 隔离约束落地）

- **完成时间**：2026-05-17 23:00
- **关联需求讨论**：`requirement-discussions/2026-05-17_20-30-00_开发工作流与MCP推荐.md`（status: confirmed，阶段 6） + `requirement-discussions/2026-05-17_21-00-00_项目红线清单.md`（status: confirmed，阶段 7.4 不入项→docs 落地）
- **关联前次 change-log**：`change-logs/2026-05-17_22-45-00_重建project-constraints红线清单.md`（前置任务：红线清单建立）
- **触发红线**：无
- **无关红线已检查**：R-1, R-2, R-3, R-4, R-5, R-6, R-7, R-8, R-9

## 1. 任务概述

- **用户原始需求**：开发工作流讨论的 Task #1 —— Linux VPS 主开发、Windows 本地测试机的代码同步基础设施
- **涉及功能模块**：开发基础设施（`tools/` + `docs/`），不涉业务代码
- **相邻任务**：与红线清单建立（前次 change-log）平行；后续 Task #2 fixture 库 + Task #3 MCP 配置依赖本次基础设施

## 2. 假设清单

| # | 假设 | 出处 |
|---|---|---|
| 1 | 同步方式 = git + rsync 双轨；Win 主动 pull（拓扑 B） | `2026-05-17_20-30-00_开发工作流与MCP推荐.md §阶段 6` |
| 2 | Win 端 rsync 工具 = Git for Windows 自带 Git Bash | 同上"实现细节（确认）" |
| 3 | Win 副本只读约定写入 `docs/dev-workflow.md` | `2026-05-17_21-00-00_项目红线清单.md §7.4 不入项`（R-6 候选转 docs） |
| 4 | Win API 隔离约束写入 `docs/dev-workflow.md` | 同上（我 R-7 转 docs） |
| 5 | sync 脚本位置 `tools/sync-from-vps.sh` | 工程惯例（与 `tools/roi_config.py` 同级） |
| 6 | rsync exclude 集合：`.git` / `__pycache__` / `*.pyc` / `tests/output/` / `.env` / `models/` / `.venv/` / `.pytest_cache/` | `.env` 机器特定保留 Win 本地；`models/` HF 缓存机器特定；其余为缓存/输出 |
| 7 | `tests/fixtures/` 同步遵循双向流转（Linux→Win rsync；Win→Linux git）—— 详细流程留 Task #2 | 当前 fixture 库未建，先占位 |

## 3. 文件变更清单

| 文件路径 | 变更类型 | 变更说明 |
|---|---|---|
| `tools/sync-from-vps.sh` | 新增 | Win 端 rsync 拉取脚本；env var 配置；read-only 警告；功能性 stub-safe（无 env var 即报错退出，不会误推） |
| `docs/dev-workflow.md` | 新增 | 7 节工作流文档：拓扑 / 首次配置 / 日常循环 / Win 副本只读约定 / Win API 隔离约束（迁移自 REQ 我 R-7）/ 故障排查 / 相关文件 |
| `~/.local/bin/pytest`（环境，非 git） | 新增（pipx 安装） | 通过 pipx 安装 pytest 9.0.3 到 Linux dev env，为后续 DEV 任务建立测试 baseline |

### 附带修复（5 分钟规则）

无。

## 4. 契约一致性检查

- 是否涉及 `contracts/` 变更：**否**
- 仅添加 dev infra 文件，不涉 API 或数据模型

## 5. 红线合规动作

无触发。本次任务为 dev infra 建立，不涉 9 条红线任何条目。

## 6. 测试结果

- **验证路径**：完整验证（rules-dev §5.1 ② ≤20 行不满足 / ④ 非纯 UI 或单行不满足）

- **执行 1**：`~/.local/bin/pytest tests/test_storage.py -v`
  ```
  3 skipped in 0.04s
  ```
  → ✅ pytest framework 工作正常；test_storage 3 用例按设计 skip（PostgreSQL 不可达，per rules-dev §5.2 "skip ≠ fail"）

- **执行 2**：`~/.local/bin/pytest tests/test_capture.py tests/test_recognition.py --collect-only`
  ```
  ERROR tests/test_capture.py — ModuleNotFoundError: No module named 'numpy'
  ERROR tests/test_recognition.py — ModuleNotFoundError: No module named 'cv2'
  ```
  → ⚠️ ImportError during collection，**不是 Task #1 引入**（本次仅添加 `.sh` + `.md` 文件，无任何 Python 改动）；属 Linux dev env 缺项目运行时依赖（numpy / cv2 / easyocr 等）的**预存环境问题**

- **manual verification**：
  - ✅ `tools/sync-from-vps.sh` shell 语法通过（`bash -n`）
  - ✅ 脚本可执行位已设置（`chmod +x`）
  - ✅ 脚本在缺 env var 时正确退出（错误信息清晰，附配置示例）
  - ✅ `docs/dev-workflow.md` markdown 结构完整（7 节）
  - ✅ 内容与 REQ confirmed 决策一致（拓扑 B、Win 主动 pull、Git Bash、副本只读、Win API 隔离）

- **完整环境验证建议**（用户 Win 测试机执行）：
  - 端到端冒烟：跑 sync 脚本 + `pytest tests/ -v`，预期 test_recognition 全 pass、test_storage 视 PG 状态 pass/skip、test_capture pass

## 7. 手动操作提醒

⚠️ **手动操作**：

1. **Windows 测试机** 首次配置（参考 `docs/dev-workflow.md §2.1`）：
   - 装 Git for Windows
   - 生成 SSH key 并加入 VPS authorized_keys
   - 在 `~/.bashrc` 配置 `POKEMIR_VPS_HOST` / `POKEMIR_VPS_USER` / `POKEMIR_VPS_PATH`
   - 首次 `git clone` 到 `C:\pokemir-test`
   - `pip install -r requirements.txt`

2. **Windows 测试机** 首次同步验证：
   - 打开 Git Bash → `cd /c/pokemir-test`
   - `bash tools/sync-from-vps.sh` 应无报错完成
   - `pytest tests/ -v` 跑通

3. **Linux VPS** 确认 SSH user 对 `~/project/pokemir/` 有读权限；`apt install rsync` 若未装

## 8. 潜在影响范围

- **正向影响**：建立 Linux↔Win 跨机开发循环；后续 Task #2/#3 可基于此 ship
- **环境副作用**：Linux dev env 现有 pytest（pipx 安装）；test_storage 已可本地跑（3 skipped）
- **未解决的环境问题**：test_capture / test_recognition 在 Linux 无法 collect（缺 numpy/cv2）→ 需独立任务建项目 venv 装完整依赖
- **关联待办**：
  - 独立 DEV 任务建议：`在Linux建项目venv装完整依赖以解锁纯逻辑测试本地化` —— 用 `python3 -m venv .venv && .venv/bin/pip install -r requirements.txt`，挂入 `~/.local/bin/pytest` 或新 venv 的 pytest；将解锁 test_recognition 在 Linux 本地跑
  - Task #2 fixture 库：依赖本次同步基础设施
  - Task #3 MCP 配置：依赖 Win 测试机准备就绪
  - `config.py:9,13` 凭据清理：R-2 已知违例独立任务

## 9. 违规标注

> ⚠️ **本次有违规残留**：rules-dev §5.2「测试套件必须全通过」部分未满足 —— **原因**：`tests/test_capture.py` / `tests/test_recognition.py` 在当前 Linux dev env 因缺 numpy/cv2 等运行时依赖发生 collection ImportError（**非本次任务引入，属预存环境问题**） —— **补救动作**：(1) Task #1 副带成果——已通过 pipx 安装 pytest 9.0.3，test_storage 可正常 collect+skip；(2) §8 已建议下一独立 DEV 任务建项目 venv 装完整依赖（`python3 -m venv .venv` + `pip install -r requirements.txt`）解锁这两个测试模块；(3) **下次 DEV 任务**若未先处理 venv，仍会以"上次违规残留"开头。

> **上次违规复盘结果**：前次 change-log（`2026-05-17_22-45-00_重建project-constraints红线清单.md §9`）记录的"pytest 无法运行"违规，本次任务**已部分修正**——pytest 现在可在 Linux dev env 跑（pipx 安装），test_storage 已通过；剩余 test_capture/test_recognition 因项目依赖问题仍未解锁，需独立 venv 任务彻底解决。
