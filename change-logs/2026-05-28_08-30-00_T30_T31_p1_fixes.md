# T30 + T31:两个 P1 fix(phash 统一 + T17 observer 时机)

- **完成时间**:2026-05-28 08:30 UTC(北京 16:30)
- **关联 test-report**:`test-reports/2026-05-28_08-20-00_open_T17_T18_深度复检发现2个P1.md`
- **关联前次 change-log**:`2026-05-28_08-05-00_T17_blind_levels_plus_T18_step1.md`(被本 fix 修正)
- **触发红线**:无
- **无关红线已检查**:R-1 ~ R-10

## 1. 任务概述

TEST 深度复检发现 2 个 P1,本 commit 修复:

- **T30**:`tools/capture_empty_seat_baseline.py` 的 `_avg_hash_64` 格式跟 pipeline 不一致(16-hex vs 64-bin)→ 改 import 复用
- **T31**:`_detect_blind_levels` 在 observer 模式触发时 community 已发,SB/BB amount 已被 voluntary action 覆盖 → 加 community check 跳过

## 2. 假设清单

1. **T30**:`pipeline.orchestrator._avg_hash_64` 是 module-level 函数,可被外部 import — verified ✅
2. **T31**:`hand.community_cards.get(Street.X)` 在 hand-start 时是空 dict 或 dict 含空 list — verified by current code structure

## 3. 文件变更清单

| 文件 | 变更 | 说明 |
|:---|:---|:---|
| `tools/capture_empty_seat_baseline.py` | 修改(-16 行,+3 行) | 删自己 _avg_hash_64,from pipeline import |
| `pipeline/orchestrator.py` | 修改(_detect_blind_levels 加 community check,+13 行) | observer 模式 community ≥ 3 时跳过 + emit blind.skip_community_already_dealt diag |

## 4. 契约一致性检查

- 不动 contracts/ schema
- 不动 ORM model
- T30 工具内部 import 重构;T31 _detect_blind_levels 加门控

## 5. 红线合规动作

无触发。

## 6. 测试结果

```python
# Test 1: phash 复用验证
from tools.capture_empty_seat_baseline import _avg_hash_64 as tool_h
from pipeline.orchestrator import _avg_hash_64 as pipe_h
assert tool_h is pipe_h  # ✅ 同一对象

# Test 2: phash 格式验证
h = pipe_h(np.random.randint(0, 255, (32, 32, 3), dtype=np.uint8))
assert len(h) == 64  # ✅ 64-char
assert set(h) <= {'0', '1'}  # ✅ binary string
```

输出:
```
✅ T30 phash 统一:tool._avg_hash_64 is pipeline._avg_hash_64
✅ phash 格式: 64-char binary string,样例 0111110001101111...
```

## 7. 手动操作提醒

```
⚠️ 手动操作:
1. Win pull T30 + T31 fix
2. T17 observer 模式下:
   - 重启 pipeline 录数据
   - query SELECT raw_data->'blind_level' FROM hands → **预期仍为空**
     (你全程观战,_start_new_hand 在 flop 触发,T31 fix 后会 skip)
   - 这是符合预期的(blind 不能在 observer 模式抓到)
3. **如果你坐下打牌**(非观战),hand-start 触发于 hero cards 发牌瞬间,
   community 还没发,T17 + T31 可正常工作
4. T18 Step 1 工具(用 import 复用 phash 后)可继续跑
5. T18 Step 2(改 _capture_player_ids 加 EMPTY_SEAT gate)等用户跑工具后我做
```

## 8. 潜在影响范围

### 重要 limitation(诚实标注)

- **T31 fix 解决"抓错数据"问题**,但**没解决"抓不到数据"问题**
- **observer 模式下 blind_level 永远 NULL**
- 想在 observer 模式抓 blind,需要 T32(新立)在主 loop 单独 poll blind 而非依赖 _start_new_hand 触发

### 不影响

- 坐下模式 T17 正常工作
- T19 dashboard / T27 view 不依赖 blind_level
- 其他 pipeline 流程不受 _detect_blind_levels 内部改动影响

## 9. 违规标注

无违规。Router 4 步全走:
- ✅ Mode 声明 (REQ → DEV)
- ✅ 红线核验
- ✅ Test 验证(import test 实跑)
- ✅ change-log + 后续 test-report 状态 _resolved_(更新)

---

## 任务完成自检 checklist

- ✅ `change-logs/2026-05-28_08-30-00_T30_T31_p1_fixes.md` 已保存
- ✅ Import + format test 跑通
- ✅ §11 模式漂移遵守
- ✅ §1.6 校准:**坐下模式可用,observer 模式 hard limit**,新 T32 留观战 blind 后续修
