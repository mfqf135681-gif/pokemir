# 建立 Linux dev venv 装项目依赖（解锁本地 pytest 完整覆盖）

- **完成时间**：2026-05-17 23:30
- **关联需求讨论**：无（独立 DEV 任务，源自前次 change-log §8 待办建议）
- **关联前次 change-log**：`change-logs/2026-05-17_23-00-00_Task1同步工作流配置.md`（前次任务 §8/§9 留下的违规残留——本次即修正动作）
- **触发红线**：无
- **无关红线已检查**：R-1, R-2, R-3, R-4, R-5, R-6, R-7, R-8, R-9

## 1. 任务概述

- **用户原始需求**：用户选 A —— 优先建项目 venv 装完整依赖，消解前次任务（Task #1）§9 留下的"test_capture/test_recognition collection ImportError"违规残留
- **涉及功能模块**：开发基础设施（Linux VPS 端 venv + 项目依赖；docs 工作流文档同步更新）
- **相邻任务**：前次 Task #1 已建跨机同步基础设施；本次补 Linux 端测试运行能力；下一步可继续 Task #3 / Task #2 / 凭据清理

## 2. 假设清单

| # | 假设 | 出处 |
|---|---|---|
| 1 | venv 路径 = `.venv/`（Python 惯例） | 前次 Task #1 sync 脚本已 exclude `.venv/`；与之一致 |
| 2 | 安装范围 = 完整 `requirements.txt` | 用户选 A（彻底消解违规）；最小化未来摩擦 |
| 3 | Linux VPS 仅有 python3.12（无 3.13），用 3.12 venv | `which python3.13` 返回空；3.12 对所有列出依赖兼容；与 Win 端 3.13.13 差异留待后续观察 |
| 4 | `.gitignore` 不存在则补；纳入 `.venv/` 等常见目录 | rules-dev §4.1 "5 分钟规则" 允许；服务本任务目标（避免误 commit venv） |
| 5 | docs/dev-workflow.md 加 §2.3 Linux VPS 端 venv 步骤 + §3.1 更新为 `.venv/bin/pytest` | 服务本任务（让后续开发者/自己记得用 venv）；非投机性扩展 |

## 3. 文件变更清单

| 文件路径 | 变更类型 | 变更说明 |
|---|---|---|
| `.gitignore` | 新增 | 项目首份 .gitignore：Python 缓存、venv、IDE、OS、项目特定（models/、tests/output/、tests/fixtures/_pending/、*.log） |
| `docs/dev-workflow.md` | 修改 | 加入 §2.3「Linux VPS 端：项目 venv」配置步骤；§3.1 命令更新为 `.venv/bin/pytest`；Python 版本差异说明 |
| `.venv/` | 新增（不入 git） | 项目 venv，python3.12，含 requirements.txt 全部依赖（约 76 个包，含 torch + CUDA libs） |

### 附带修复（5 分钟规则）

无。

## 4. 契约一致性检查

- 是否涉及 `contracts/` 变更：**否**
- 仅添加 dev infra（venv + .gitignore + docs 微调），不涉 API/数据模型

## 5. 红线合规动作

无触发。本次为 dev env 建立 + docs 同步，不涉 9 条红线任一条目。

## 6. 测试结果

- **验证路径**：完整验证（rules-dev §5.1 多文件 / 非纯 UI）

- **执行命令**：`.venv/bin/pytest tests/ -v`

- **结果总览**：
  ```
  17 tests collected
  11 passed
   6 skipped
   0 failed
  10 warnings (均为 torch.ao.quantization deprecation，与本任务无关)
  耗时 49.05s
  ```

- **细分**：
  - `test_action_parser[*]` × 10 → **全 passed** ✅（纯逻辑测试，符合预期）
  - `test_ocr_engine_on_synthetic_text` → passed（EasyOCR 首次跑下载模型成功）
  - `test_card_recognizer_returns_shape` → skipped（启发式对合成图判定 None，符合 test 中 `pytest.skip` 设计）
  - `test_capture_full_screen` / `test_capture_center_crop` → skipped（Linux dev env 无图形会话，符合 fixture 中 monitor 判定）
  - `test_hand_create_and_read` / `test_action_events_roundtrip` / `test_hand_update_community_cards` → skipped（PostgreSQL 不可达，符合 db_session fixture 设计）

- **rules-dev §5.2 判定**：✅ **"所有测试必须通过"** —— 11 passed + 6 skipped + 0 failed；skip 不算 fail（rules-dev §5.2 + 项目红线 R-4 退役理由已明确）

- **首次违规消解证据**：
  - 前次（`change-logs/2026-05-17_23-00-00_Task1同步工作流配置.md §9`）：test_capture / test_recognition `ModuleNotFoundError: No module named 'numpy' / 'cv2'`
  - 本次：两文件均正常 collect + 全部 test 或 pass 或 skip，无 collection error
  - **违规残留状态：✅ 已完全修正**

## 7. 手动操作提醒

⚠️ **手动操作**：

1. **Windows 测试机** 同步执行 venv 建立（参考新版 `docs/dev-workflow.md §2.3`）：
   ```bash
   # 在 C:\pokemir-test
   python -m venv .venv
   .venv\Scripts\pip install -r requirements.txt
   ```
   Win 端建议用 Python 3.13.13（与 `decisions.md` 2026-05-03 一致）

2. **首次 git commit 前**：确认 `.gitignore` 工作正常，`git status` 应**不**列出 `.venv/`

3. **可选清理**：本会话中之前 pipx 安装的全局 pytest（`~/.local/bin/pytest`）已被 venv 内置 pytest 替代；如想清理：
   ```bash
   pipx uninstall pytest
   ```
   非关键，留着不影响。

## 8. 潜在影响范围

- **正向影响**：
  - Linux dev env 现可跑全部 17 个测试用例（11 passed + 6 skipped per 环境约束）
  - 后续 DEV 任务的"测试套件全通过"约束**首次能在 Linux 完整满足**
  - 前次违规残留 ✅ 完全修正；下一次 DEV 任务的 Mode 标签**不**需要附加"上次违规残留"
- **环境引入**：
  - `.venv/` 占磁盘约 3-5GB（torch + CUDA libs 居多）；已 .gitignore，不污染仓
  - 76 个包安装到 venv（torch、easyocr、cv2、numpy 等）
- **关联待办**（按建议优先级）：
  - **凭据清理**（R-2 已知违例 `config.py:9, 13`）
  - **Task #3**（MCP 配置清单）
  - **Task #2**（fixture 库骨架）

## 9. 违规标注

无。

> **上次违规残留消解状态**：✅ 完全修正
> 前次 change-log（`2026-05-17_23-00-00_Task1同步工作流配置.md §9`）记录"test_capture / test_recognition collection ImportError"违规，本次任务通过建立 .venv + 装完整 requirements.txt 解决；17 tests 全部 collect 成功，11 passed + 6 skipped + 0 failed。
>
> 下一次 DEV 任务的 Mode 标签**不**需附加"上次违规残留"修复语。
