# T17 + T18 深度复检 — 发现 2 个 P1

- **诊断时间**:2026-05-28 08:20 UTC(北京 16:20)
- **状态**:**open**(2 个 P1 待修)
- **关联前次 change-log**:`2026-05-28_08-05-00_T17_blind_levels_plus_T18_step1.md`
- **触发红线**:无
- **无关红线已检查**:R-1 ~ R-10
- **严重等级汇总**:**P0: 0 / P1: 2 / P2: 0 / P3: 1**

> **用户原话**:"顺利的不像话" — 主动揭盲信号,我深挖后果然找到。

---

## 阶段 1:测试执行(主动找问题)

### Q1 — T17 调用时机 + button_seat 依赖

```
line 344  self._detect_button_position()
line 348  self._detect_blind_levels()
```

→ 都在 `_start_new_hand` 里。**但 `_start_new_hand` 触发时机分两路径**:
- 坐下:`hero_card_1` 出现 = 发牌瞬间
- 观战:`community_cards` 出现 = **flop deal**(SB/BB 已被 voluntary 覆盖)

### Q2 — `_avg_hash_64` 格式比对

```
pipeline/orchestrator.py:38  return 64-char "0"/"1" string
tools/capture_empty_seat_baseline.py:39  return 16-char hex (val:016x)
```

**格式不一致**。

### Q3 — dashboard 多页面 import

```
✅ 6 pages all importable(profile + coach + labeling + live + replay + settings)
```

### Q4 — T29 漏 alias 检查(降 cutoff)

```
cutoff 0.55: 0 候选(33 玩家)
```

但人眼可见 "周润发csc(11) vs 周润法发发(24)" 大概率是同一玩家。LCS ratio ≈ 0.5(中文部分字错读 ratio 太低)。

### Q5 — T17 实际效果

```sql
SELECT COUNT(*) FROM hands WHERE raw_data ? 'blind_level';
→ 0
```

用户未 pull 重启,无新数据 — **即使重启录,observer 模式仍抓错值**(P1 #2)。

---

## 阶段 2:发现问题

### 🔴 P1 #1:`_avg_hash_64` 工具 vs pipeline 格式不一致

**现象**:T18 工具返回 16-char hex,pipeline 返回 64-char bin string。
**严重度**:**P1** — Step 2 改 `_capture_player_ids` 时空位永远检测不到(字符串永不等)。
**当前可用性**:Step 1 ready 但 Step 2 必爆,**T18 整体不可用**直到修。

### 🔴 P1 #2:T17 在 observer 模式时机错

**现象**:`_detect_blind_levels` 调用时机在 community cards 出现后,**SB/BB amount 已被 voluntary action 覆盖**。
**严重度**:**P1** — **用户全程观战录制**,T17 数据 **100% 错**。
**当前可用性**:T17 在生产环境实际**写错数据**(等用户重启 pipeline 录数据后会出现)。

### 🟡 P3:T29 cutoff 算法漏中文部分字错读

**现象**:"周润发csc / 周润法发发" 等中文部分错读 ratio < 0.55,cutoff 降到 0.55 仍找不到。
**严重度**:P3 已知 limitation。
**修复路径**:换算法(中文专用 fuzzy match 库)或人眼审核 → 远期。

---

## 阶段 3:原因分析

| 问题 | 根因 |
|:---|:---|
| P1 #1 | 我**自造轮子**,没 `from pipeline import _avg_hash_64` — [[dev-rule-validate-blind-spots]] 同类教训复发 |
| P1 #2 | **设计 T17 时没考虑 observer 时序**,假设 _start_new_hand 触发时 SB/BB 仍为 blind — 用户 "全程观战" 这个关键前提我**没主动揭盲** |
| P3 | LCS 算法对中文 OCR 漂移天然不敏感(字符层而非 token 层)— 工程限制 |

---

## 阶段 4:修复方案草案

### T30(立)— phash 统一

```python
# tools/capture_empty_seat_baseline.py
from pipeline.orchestrator import _avg_hash_64
# 删除 line 39-53 自己的 _avg_hash_64 实现
```

工程量:~5 行改动。

### T31(立)— T17 改时机

```python
# pipeline/orchestrator.py _detect_blind_levels
def _detect_blind_levels(self) -> None:
    ...
    # T31 fix: 只在 preflop(community = 0)抓 blind
    community = self.tracker.current_hand.community_cards.get(Street.PREFLOP)
    if community and len(community) > 0:
        return  # community 已发牌,SB/BB amount 可能被覆盖
    # 或更稳:check raw_data['player_stacks_initial'] 是否含 SB/BB 标记
    ...
```

工程量:~10 行改动。

### P3 不修(远期 #LR)

---

## 阶段 5:全局影响评估

**T30**:工具内部改动,零生产影响。
**T31**:`_detect_blind_levels` 加门控,observer 模式下 blind_level 才能真出数据。

**追加测试**:T31 fix 后,用户 Win 重启 + 录数据 → query `SELECT raw_data->'blind_level' FROM hands` 应该非空(至少在 community 还没发的早期 hand)。

---

## 切换提示

> 🔴 2 个 P1 发现,**T17 + T18 当前都不可用**(P1 修前)。
>
> 👉 你接下来:
> - "开发模式 T30 + T31" → 立刻修 2 个 P1 fix
> - "讨论模式 / 暂不动" → 讨论 P3 / 等用户决定

---

## 任务完成自检 checklist

- ✅ `test-reports/2026-05-28_08-20-00_open_T17_T18_深度复检发现2个P1.md` 已保存
- ✅ INDEX.md 即将同步(加新条目到 🔴 Open)
- ✅ 状态段 `_open_`(2 个 P1 待修)
- ✅ §11 模式漂移遵守(用户后明示 "测试模式" 优先)
- ✅ §9 主动揭盲真生效 — **用户提示 "顺利的不像话",我深挖找到真 bug**
- ✅ §1.6 校准:T17 / T18 之前 commit "全过" 是 overclaim,**这次复检改写结论**
